<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE document PUBLIC "+//IDN docutils.sourceforge.net//DTD Docutils Generic//EN//XML" "http://docutils.sourceforge.net/docs/ref/docutils.dtd">
<!-- Generated by Docutils 0.9 -->
<document source="manual.rst"><comment xml:space="preserve">Automatically generated reST file from Doconce source
(http://code.google.com/p/doconce/)</comment><section ids="doconce-description" names="doconce\ description"><title>Doconce Description</title><field_list><field><field_name>Author</field_name><field_body><paragraph>Hans Petter Langtangen</paragraph></field_body></field><field><field_name>Date</field_name><field_body><paragraph>Aug 11, 2013</paragraph></field_body></field></field_list><comment xml:space="preserve">lines beginning with # are doconce comment lines</comment><comment xml:space="preserve">(documents can also have mako comment lines)</comment><target refid="what-is-doconce"/></section><section ids="id1 what-is-doconce" names="what\ is\ doconce? what:is:doconce"><title>What Is Doconce?</title><paragraph>Doconce is a very simple and minimally tagged markup language that
looks like ordinary ASCII text, much like what you would use in an
email, but the text can be transformed to numerous other formats,
including HTML, Sphinx, LaTeX, PDF, reStructuredText (reST), Markdown,
MediaWiki, Google wiki, Creole wiki, blogger.com, wordpress.com,
Epytext, and also plain (untagged) text for email.
From reST or Markdown you can go to XML, OpenOffice, MS
Word, HTML, LaTeX, PDF, DocBook, GNU Texinfo, and more.</paragraph><paragraph>Doconce supports a working strategy of never duplicating information.
Text is written in a single place and then transformed to a number of
different destinations of diverse type: scientific reports, software
manuals, books, thesis, software source code, wikis, blogs, emails,
etc.  The slogan is: &quot;Write once, include anywhere&quot;.</paragraph><paragraph>Here are some Doconce features:</paragraph><block_quote><bullet_list bullet="*"><list_item><paragraph>Doconce addresses small and large documents containing
<emphasis>text with much computer source code and
LaTeX mathematics</emphasis>, where the output is desired in different formats
such as LaTeX, pdfLaTeX, Sphinx, HTML,
MediaWiki, blogger.com, and wordpress.com.
A piece of Doconce text can enter (e.g.) a classical
science book, an ebook, a web document, and a blog.</paragraph></list_item><list_item><paragraph>Doconce targets in particular large book projects where many different
pieces of text and software can be assembled and published in different
formats for different devices.</paragraph></list_item><list_item><paragraph>Doconce enables authors who write for many times of media
(blogs, wikis, LaTeX manuscripts, Sphinx, HTML) to use a common
source language such that lots of different pieces can easily be
brought together later to form a coherent (big) document.</paragraph></list_item><list_item><paragraph>Doconce has good support for copying computer code
directly from the source code files via regular expressions
for the start and end lines.</paragraph></list_item><list_item><paragraph>Doconce first runs two preprocessors (Preprocess and Mako), which
allow programming constructs (includes, if-tests, function calls)
as part of the text. This feature makes it easy to write <emphasis>one text</emphasis>
with different flavors: long vs short text, Python vs Matlab code
examples, experimental vs mature content.</paragraph></list_item><list_item><paragraph>Doconce can be converted to plain <emphasis>untagged</emphasis> text,
often desirable for email and computer code documentation.</paragraph></list_item><list_item><paragraph>Doconce markup does include tags, so the format is more tagged than
Markdown, but less than reST, and very much less than
LaTeX and HTML.</paragraph></list_item><list_item><paragraph>Compared to the related tools Sphinx and Markdown, Doconce
allows more types of equations (especially systems of
equations with references), has more flexible
inclusion of source code, integrates preprocessors, has
special support for exercises, and produces
cleaner LaTeX and HTML output.</paragraph></list_item></bullet_list></block_quote><paragraph><emphasis>History.</emphasis> Doconce was developed in 2006 at a time when most popular
markup languages used quite some tagging.  Later, almost untagged
markup languages like Markdown and the Pandoc translator became
popular. Doconce is not a replacement of Pandoc, which is a
considerably more sophisticated project. Moreover, Doconce was
developed mainly to fulfill the needs for a flexible source code base
for books with much mathematics and computer code.</paragraph><paragraph><emphasis>Disclaimer.</emphasis> Doconce is a simple tool, largely based on interpreting
and handling text through regular expressions. The possibility for
tweaking the layout is obviously limited since the text can go to
all sorts of sophisticated markup languages. Moreover, because of
limitations of regular expressions, some formatting of Doconce syntax
may face problems when transformed to HTML, LaTeX, Sphinx, and similar
formats.</paragraph></section><section ids="installation-of-doconce-and-its-dependencies" names="installation\ of\ doconce\ and\ its\ dependencies"><title>Installation of Doconce and its Dependencies</title><paragraph>Below, we explain the manual installation of all software that may be
needed when working with Doconce documents.
The impatient way to install what is needed is to run the
<reference name="doconce_install_all.sh" refuri="doconce_install_all.sh">doconce_install_all.sh</reference><target ids="doconce-install-all-sh" names="doconce_install_all.sh" refuri="doconce_install_all.sh"/> script.</paragraph><section ids="doconce" names="doconce"><title>Doconce</title><paragraph>Doconce itself is pure Python code hosted at <reference name="http://code.google.com/p/doconce" refuri="http://code.google.com/p/doconce">http://code.google.com/p/doconce</reference><target ids="http-code-google-com-p-doconce" names="http://code.google.com/p/doconce" refuri="http://code.google.com/p/doconce"/>.  Its installation from the
Mercurial (<literal>hg</literal>) source follows the standard procedure:</paragraph><literal_block xml:space="preserve"># Doconce
hg clone https://code.google.com/p/doconce/ doconce
cd doconce
sudo python setup.py install
cd ..</literal_block><paragraph>Since Doconce is frequently updated, it is recommended to use the
above procedure and whenever a problem occurs, make sure to
update to the most recent version:</paragraph><literal_block xml:space="preserve">cd doconce
hg pull
hg update
sudo python setup.py install</literal_block></section><section ids="dependencies" names="dependencies"><title>Dependencies</title><paragraph>Producing HTML documents, plain text, pandoc-extended Markdown,
and wikis can be done without installing any other
software. However, if you want other formats as output
(LaTeX, Sphinx, reStructuredText) and assisting utilities such
as preprocesors, spellcheck, file differences, bibliographies,
and so on, the software below must be installed.</paragraph><comment xml:space="preserve">Make a debpkg_doconce.txt file with everything that is needed on Debian</comment><section ids="preprocessors" names="preprocessors"><title>Preprocessors</title><paragraph>If you make use of the <reference name="Preprocess" refuri="http://code.google.com/p/preprocess">Preprocess</reference><target ids="preprocess" names="preprocess" refuri="http://code.google.com/p/preprocess"/>
preprocessor, this program must be installed:</paragraph><literal_block xml:space="preserve">svn checkout http://preprocess.googlecode.com/svn/trunk/ preprocess
cd preprocess
cd doconce
sudo python setup.py install
cd ..</literal_block><paragraph>A much more advanced alternative to Preprocess is
<reference name="Mako" refuri="http://www.makotemplates.org">Mako</reference><target dupnames="mako" ids="mako" refuri="http://www.makotemplates.org"/>. Its installation is most
conveniently done by <literal>pip</literal>:</paragraph><literal_block xml:space="preserve">pip install Mako</literal_block><paragraph>This command requires <literal>pip</literal> to be installed. On Debian Linux systems,
such as Ubuntu, the installation is simply done by:</paragraph><literal_block xml:space="preserve">sudo apt-get install python-pip</literal_block><paragraph>Alternatively, one can install from the <literal>pip</literal> <reference name="source code" refuri="http://pypi.python.org/pypi/pip">source code</reference><target ids="source-code" names="source\ code" refuri="http://pypi.python.org/pypi/pip"/>.</paragraph><paragraph>Mako can also be installed directly from
<reference name="source" refuri="http://www.makotemplates.org/download.html">source</reference><target ids="source" names="source" refuri="http://www.makotemplates.org/download.html"/>: download the
tarball, pack it out, go to the directory and run
the usual <literal>sudo python setup.py install</literal>.</paragraph></section><section ids="image-file-handling" names="image\ file\ handling"><title>Image file handling</title><paragraph>Different output formats require different formats of image files.
For example, PostScript or Encapuslated PostScript is required for <literal>latex</literal>
output, while HTML needs JPEG, GIF, or PNG formats.
Doconce calls up programs from the ImageMagick suite for converting
image files to a proper format if needed. The <reference name="ImageMagick suite" refuri="http://www.imagemagick.org/script/index.php">ImageMagick suite</reference><target ids="imagemagick-suite" names="imagemagick\ suite" refuri="http://www.imagemagick.org/script/index.php"/> can be installed on all major platforms.
On Debian Linux (including Ubuntu) systems one can simply write:</paragraph><literal_block xml:space="preserve">sudo apt-get install imagemagick</literal_block><paragraph>The convenience program <literal>doconce combine_images</literal>, for combining several
images into one, will use <literal>montage</literal> and <literal>convert</literal> from ImageMagick and
the <literal>pdftk</literal>, <literal>pdfnup</literal>, and <literal>pdfcrop</literal> programs from the <literal>texlive-extra-utils</literal>
Debian package. The latter gets installed by:</paragraph><literal_block xml:space="preserve">sudo apt-get install texlive-extra-utils</literal_block><paragraph>Automatic image conversion from EPS to PDF calls up <literal>epstopdf</literal>, which
can be installed by:</paragraph><literal_block xml:space="preserve">sudo apt-get install texlive-font-utils</literal_block></section><section ids="spellcheck" names="spellcheck"><title>Spellcheck</title><paragraph>The utility <literal>doconce spellcheck</literal> applies the <literal>ispell</literal> program for
spellcheck. On Debian (including Ubuntu) it is installed by:</paragraph><literal_block xml:space="preserve">sudo apt-get install ispell</literal_block></section><section ids="bibliography-1" names="bibliography\ (1)"><title>Bibliography  (1)</title><paragraph>The Python package <reference name="Publish" refuri="https://bitbucket.org/logg/publish">Publish</reference><target ids="publish" names="publish" refuri="https://bitbucket.org/logg/publish"/> is needed if you use a bibliography
in your document. On the website, click on <emphasis>Clone</emphasis>, copy the
command and run it:</paragraph><literal_block xml:space="preserve">hg clone https://bitbucket.org/logg/publish</literal_block><paragraph>Thereafter go to the <literal>publish</literal> directory and run the <literal>setup.py</literal> script
for installing Publish:</paragraph><literal_block xml:space="preserve">cd publish
sudo python setup.py</literal_block></section><section ids="ptex2tex-for-latex-output" names="ptex2tex\ for\ latex\ output"><title>Ptex2tex for LaTeX Output</title><paragraph>To make LaTeX documents with very flexible choice of typesetting of
verbatim code blocks you need <reference name="ptex2tex" refuri="http://code.google.com/p/ptex2tex">ptex2tex</reference><target ids="ptex2tex" names="ptex2tex" refuri="http://code.google.com/p/ptex2tex"/>,
which is installed by:</paragraph><literal_block xml:space="preserve">svn checkout http://ptex2tex.googlecode.com/svn/trunk/ ptex2tex
cd ptex2tex
sudo python setup.py install</literal_block><paragraph>It may happen that you need additional style files, you can run
a script, <literal>cp2texmf.sh</literal>:</paragraph><literal_block xml:space="preserve">cd latex
sh cp2texmf.sh  # copy stylefiles to ~/texmf directory
cd ../..</literal_block><paragraph>This script copies some special stylefiles that
that <literal>ptex2tex</literal> potentially makes use of. Some more standard stylefiles
are also needed. These are installed by:</paragraph><literal_block xml:space="preserve">sudo apt-get install texlive</literal_block><paragraph>on Debian Linux (including Ubuntu) systems. TeXShop on Mac comes with
the necessary stylefiles (if not, they can be found by googling and installed
manually in the <literal>~/texmf/tex/latex/misc</literal> directory).</paragraph><paragraph>Note that the <literal>doconce ptex2tex</literal> command, which needs no installation
beyond Doconce itself, can be used as a simpler alternative to the <literal>ptex2tex</literal>
program.</paragraph><paragraph>The <emphasis>minted</emphasis> LaTeX style is offered by <literal>ptex2tex</literal> and <literal>doconce ptext2tex</literal>
and popular among many
users. This style requires the package <reference name="Pygments" refuri="http://pygments.org">Pygments</reference><target ids="pygments" names="pygments" refuri="http://pygments.org"/>
to be installed. On Debian Linux:</paragraph><literal_block xml:space="preserve">sudo apt-get install python-pygments</literal_block><paragraph>Alternatively, the package can be installed manually:</paragraph><literal_block xml:space="preserve">hg clone ssh://hg@bitbucket.org/birkenfeld/pygments-main pygments
cd pygments
sudo python setup.py install</literal_block><paragraph>One can also do the simple:</paragraph><literal_block xml:space="preserve">pip install sphinx</literal_block><paragraph>which also installs pygments.</paragraph><paragraph>If you use the minted style together with <literal>ptex2tex</literal>, you have to
enable it by the <literal>-DMINTED</literal> command-line argument to <literal>ptex2tex</literal>.
This is not necessary if you run the alternative <literal>doconce ptex2tex</literal> program.</paragraph><paragraph>All use of the minted style requires the <literal>-shell-escape</literal> command-line
argument when running LaTeX, i.e., <literal>latex -shell-escape</literal> or <literal>pdflatex
-shell-escape</literal>.</paragraph><paragraph>Inline comments apply the <literal>todonotes</literal> LaTeX package if the <literal>ptex2tex</literal>
or <literal>doconce ptex2tex</literal> command is run with <literal>-DTODONOTES</literal>.  The
<literal>todonotes</literal> package requires several other packages: <literal>xcolor</literal>,
<literal>ifthen</literal>, <literal>xkeyval</literal>, <literal>tikz</literal>, <literal>calc</literal>, <literal>graphicx</literal>, and <literal>setspace</literal>. The
relevant Debian packages for installing all this are listed below.</paragraph></section><section ids="latex-packages" names="latex\ packages"><title>LaTeX packages</title><paragraph>Many LaTeX packages are potentially needed (depending on various
preprocessor variables given to <literal>ptex2tex</literal> or <literal>doconce ptex2tex</literal>.  The
standard packages always included are <literal>relsize</literal>, <literal>epsfig</literal>, <literal>makeidx</literal>,
<literal>setspace</literal>, <literal>color</literal>, <literal>amsmath</literal>, <literal>amsfonts</literal>, <literal>xcolor</literal>, <literal>bm</literal>,
<literal>microtype</literal>, <literal>titlesec</literal>, and <literal>hyperref</literal>.  The <literal>ptex2tex</literal> package (from
<reference name="ptex2tex" refuri="http://code.google.com/p/ptex2tex">ptex2tex</reference><target dupnames="ptex2tex" ids="id2" refuri="http://code.google.com/p/ptex2tex"/>) is also included, but
removed again if <literal>doconce ptex2tex</literal> is run instead of the <literal>ptex2tex</literal>
program, meaning that if you do not use <literal>ptex2tex</literal>, you do not need
<literal>ptex2tex.sty</literal>. Optional packages that might be included are <literal>minted</literal>,
<literal>fontspec</literal>, <literal>xunicode</literal>, <literal>inputenc</literal>, <literal>helvet</literal>, <literal>mathpazo</literal>, <literal>wrapfig</literal>,
<literal>calc</literal>, <literal>ifthen</literal>, <literal>xkeyval</literal>, <literal>tikz</literal>, <literal>graphicx</literal>, <literal>setspace</literal>, <literal>shadow</literal>,
<literal>disable</literal>, <literal>todonotes</literal>, <literal>lineno</literal>, <literal>xr</literal>, <literal>framed</literal>, <literal>mdframe</literal>,
<literal>movie15</literal>, <literal>a4paper</literal>, and <literal>a6paper</literal>.</paragraph><paragraph>Relevant Debian packages that gives you all of these LaTeX packages are:</paragraph><literal_block xml:space="preserve">texlive
texlive-extra-utils
texlive-latex-extra
texlive-font-utils</literal_block><paragraph>On old Ubuntu 12.04 one has to do <literal>sudo add-apt-repository ppa:texlive-backports/ppa</literal> and <literal>sudo apt-get update</literal> first, or alternatively install these as well:</paragraph><literal_block xml:space="preserve">texlive-math-extra
texlive-bibtex-extra
texlive-xetex
texlive-humanities
texlive-pictures</literal_block><paragraph>Alternatively, one may pull in <literal>texlive-full</literal> to get all available
style files.</paragraph><paragraph>If you want to use the <emphasis>anslistings</emphasis> code environment with <literal>ptex2tex</literal>
(<literal>.ptex2tex.cfg</literal> styles <literal>Python_ANS</literal>, <literal>Python_ANSt</literal>, <literal>Cpp_ANS</literal>, etc.) or
<literal>doconce ptex2tex</literal> (<literal>envir=ans</literal> or <literal>envir=ans:nt</literal>), you need the
<literal>anslistings.sty</literal> file. It can be obtained from
the <reference name="ptex2tex source" refuri="https://code.google.com/p/ptex2tex/source/browse/trunk/latex/styles/with_license/anslistings.sty">ptex2tex source</reference><target ids="ptex2tex-source" names="ptex2tex\ source" refuri="https://code.google.com/p/ptex2tex/source/browse/trunk/latex/styles/with_license/anslistings.sty"/>. It should get installed
by the <literal>cp2texmf.sh</literal> script executed above.</paragraph></section><section ids="restructuredtext-rest-output" names="restructuredtext\ (rest)\ output"><title>reStructuredText (reST) Output</title><paragraph>The <literal>rst</literal> output from Doconce allows further transformation to LaTeX,
HTML, XML, OpenOffice, and so on, through the <reference name="docutils" refuri="http://docutils.sourceforge.net">docutils</reference><target ids="docutils" names="docutils" refuri="http://docutils.sourceforge.net"/> package.  The installation of the
most recent version can be done by:</paragraph><literal_block xml:space="preserve">svn checkout \
  http://docutils.svn.sourceforge.net/svnroot/docutils/trunk/docutils
cd docutils
sudo python setup.py install
cd ..</literal_block><paragraph>The command:</paragraph><literal_block xml:space="preserve">pip install sphinx</literal_block><paragraph>installs Docutils along with Sphinx and Pygments.</paragraph><paragraph>To use the OpenOffice suite you will typically on Debian systems install:</paragraph><literal_block xml:space="preserve">sudo apt-get install unovonv libreoffice libreoffice-dmaths</literal_block><paragraph>There is a possibility to create PDF files from reST documents
using ReportLab instead of LaTeX. The enabling software is
<reference name="rst2pdf" refuri="http://code.google.com/p/rst2pdf">rst2pdf</reference><target ids="rst2pdf" names="rst2pdf" refuri="http://code.google.com/p/rst2pdf"/>. Either download the tarball
or clone the svn repository, go to the <literal>rst2pdf</literal> directory and
run the usual <literal>sudo python setup.py install</literal>.</paragraph></section><section ids="sphinx-output" names="sphinx\ output"><title>Sphinx Output</title><paragraph>Output to <literal>sphinx</literal> requires of course the
<reference name="Sphinx software" refuri="http://sphinx.pocoo.org">Sphinx software</reference><target ids="sphinx-software" names="sphinx\ software" refuri="http://sphinx.pocoo.org"/>,
installed by:</paragraph><literal_block xml:space="preserve">hg clone https://bitbucket.org/birkenfeld/sphinx
cd sphinx
sudo python setup.py install
cd ..</literal_block><paragraph>An alternative is:</paragraph><literal_block xml:space="preserve">pip install sphinx</literal_block><paragraph>Doconce comes with many Sphinx themes that are not part of the
standard Sphinx source distribution. Some of these themes require
additional Python/Sphinx modules to be installed:</paragraph><block_quote><bullet_list bullet="*"><list_item><paragraph>cloud and redcloud: <reference name="https://bitbucket.org/ecollins/cloud_sptheme" refuri="https://bitbucket.org/ecollins/cloud_sptheme">https://bitbucket.org/ecollins/cloud_sptheme</reference><target ids="https-bitbucket-org-ecollins-cloud-sptheme" names="https://bitbucket.org/ecollins/cloud_sptheme" refuri="https://bitbucket.org/ecollins/cloud_sptheme"/></paragraph></list_item><list_item><paragraph>bootstrap: <reference name="https://github.com/ryan-roemer/sphinx-bootstrap-theme" refuri="https://github.com/ryan-roemer/sphinx-bootstrap-theme">https://github.com/ryan-roemer/sphinx-bootstrap-theme</reference><target ids="https-github-com-ryan-roemer-sphinx-bootstrap-theme" names="https://github.com/ryan-roemer/sphinx-bootstrap-theme" refuri="https://github.com/ryan-roemer/sphinx-bootstrap-theme"/></paragraph></list_item><list_item><paragraph>solarized: <reference name="https://bitbucket.org/miiton/sphinxjp.themes.solarized" refuri="https://bitbucket.org/miiton/sphinxjp.themes.solarized">https://bitbucket.org/miiton/sphinxjp.themes.solarized</reference><target ids="https-bitbucket-org-miiton-sphinxjp-themes-solarized" names="https://bitbucket.org/miiton/sphinxjp.themes.solarized" refuri="https://bitbucket.org/miiton/sphinxjp.themes.solarized"/></paragraph></list_item><list_item><paragraph>impressjs: <reference name="https://github.com/shkumagai/sphinxjp.themes.impressjs" refuri="https://github.com/shkumagai/sphinxjp.themes.impressjs">https://github.com/shkumagai/sphinxjp.themes.impressjs</reference><target ids="https-github-com-shkumagai-sphinxjp-themes-impressjs" names="https://github.com/shkumagai/sphinxjp.themes.impressjs" refuri="https://github.com/shkumagai/sphinxjp.themes.impressjs"/></paragraph></list_item></bullet_list></block_quote><paragraph>These must be downloaded or cloned, and <literal>setup.py</literal> must be run as shown
above.</paragraph></section><section ids="markdown-and-pandoc-output" names="markdown\ and\ pandoc\ output"><title>Markdown and Pandoc Output</title><paragraph>The Doconce format <literal>pandoc</literal> outputs the document in the Pandoc
extended Markdown format, which via the <literal>pandoc</literal> program can be
translated to a range of other formats. Installation of <reference name="Pandoc" refuri="http://johnmacfarlane.net/pandoc/">Pandoc</reference><target ids="pandoc" names="pandoc" refuri="http://johnmacfarlane.net/pandoc/"/>, written in Haskell, is most
easily done by:</paragraph><literal_block xml:space="preserve">sudo apt-get install pandoc</literal_block><paragraph>on Debian (Ubuntu) systems.</paragraph></section><section ids="epydoc-output" names="epydoc\ output"><title>Epydoc Output</title><paragraph>When the output format is <literal>epydoc</literal> one needs that program too, installed
by:</paragraph><literal_block xml:space="preserve">svn co https://epydoc.svn.sourceforge.net/svnroot/epydoc/trunk/epydoc epydoc
cd epydoc
sudo make install
cd ..</literal_block><paragraph><emphasis>Remark.</emphasis> Several of the packages above installed from source code
are also available in Debian-based system through the
<literal>apt-get install</literal> command. However, we recommend installation directly
from the version control system repository as there might be important
updates and bug fixes. For <literal>svn</literal> directories, go to the directory,
run <literal>svn update</literal>, and then <literal>sudo python setup.py install</literal>. For
Mercurial (<literal>hg</literal>) directories, go to the directory, run
<literal>hg pull; hg update</literal>, and then <literal>sudo python setup.py install</literal>.</paragraph></section><section ids="the-doconce-diff-command" names="the\ doconce\ diff\ command"><title>The <literal>doconce diff</literal> command</title><paragraph>The <literal>doconce diff file1 file prog</literal> command for illustrating differences between
two files <literal>file1</literal> and <literal>file2</literal> using the program <literal>prog</literal> requires <literal>prog</literal>
to be installed. By default, <literal>prog</literal> is <literal>difflib</literal> which comes with Python
and is always present if you have Doconce installed. Another choice, <literal>diff</literal>,
should be available on all Unix/Linux systems. Other choices, their
URL, and their <literal>sudo apt-get install</literal> command on Debian (Ubuntu) systems
appear in the table below.</paragraph><table><tgroup cols="3"><colspec colwidth="89"/><colspec colwidth="89"/><colspec colwidth="89"/><thead><row><entry><paragraph>Program</paragraph></entry><entry><paragraph>URL</paragraph></entry><entry><paragraph>Debian/Ubuntu install</paragraph></entry></row></thead><tbody><row><entry><paragraph><literal>pdiff</literal></paragraph></entry><entry><paragraph><reference name="a2ps" refuri="http://www.gnu.org/software/a2ps/">a2ps</reference><target ids="a2ps" names="a2ps" refuri="http://www.gnu.org/software/a2ps/"/> <reference name="wdiff" refuri="http://www.gnu.org/software/wdiff/">wdiff</reference><target ids="wdiff" names="wdiff" refuri="http://www.gnu.org/software/wdiff/"/></paragraph></entry><entry><paragraph><literal>sudo apt-get install a2ps wdiff texlive-latex-extra texlive-latex-recommended</literal></paragraph></entry></row><row><entry><paragraph><literal>latexdiff</literal></paragraph></entry><entry><paragraph><reference name="latexdiff" refuri="http://www.ctan.org/pkg/latexdiff">latexdiff</reference><target ids="latexdiff" names="latexdiff" refuri="http://www.ctan.org/pkg/latexdiff"/></paragraph></entry><entry><paragraph><literal>sudo apt-get install latexdiff</literal></paragraph></entry></row><row><entry><paragraph><literal>kdiff3</literal></paragraph></entry><entry><paragraph><reference name="kdiff3" refuri="http://kdiff3.sourceforge.net/">kdiff3</reference><target ids="kdiff3" names="kdiff3" refuri="http://kdiff3.sourceforge.net/"/></paragraph></entry><entry><paragraph><literal>sudo apt-get install kdiff3</literal></paragraph></entry></row><row><entry><paragraph><literal>diffuse</literal></paragraph></entry><entry><paragraph><reference name="diffuse" refuri="http://diffuse.sourceforge.net/">diffuse</reference><target ids="diffuse" names="diffuse" refuri="http://diffuse.sourceforge.net/"/></paragraph></entry><entry><paragraph><literal>sudo apt-get install diffuse</literal></paragraph></entry></row><row><entry><paragraph><literal>xxdiff</literal></paragraph></entry><entry><paragraph><reference name="xxdiff" refuri="http://xxdiff.sourceforge.net/local/">xxdiff</reference><target ids="xxdiff" names="xxdiff" refuri="http://xxdiff.sourceforge.net/local/"/></paragraph></entry><entry><paragraph><literal>sudo apt-get install xxdiff</literal></paragraph></entry></row><row><entry><paragraph><literal>meld</literal></paragraph></entry><entry><paragraph><reference name="meld" refuri="http://meldmerge.org/">meld</reference><target ids="meld" names="meld" refuri="http://meldmerge.org/"/></paragraph></entry><entry><paragraph><literal>sudo apt-get install meld</literal></paragraph></entry></row><row><entry><paragraph><literal>tkdiff.tcl</literal></paragraph></entry><entry><paragraph><reference name="tkdiff" refuri="https://sourceforge.net/projects/tkdiff/">tkdiff</reference><target ids="tkdiff" names="tkdiff" refuri="https://sourceforge.net/projects/tkdiff/"/></paragraph></entry><entry><paragraph>not in Debian</paragraph></entry></row></tbody></tgroup></table></section></section><section ids="quick-debian-ubuntu-install" names="quick\ debian/ubuntu\ install"><title>Quick Debian/Ubuntu Install</title><paragraph>On Debian (including Ubuntu) systems, it is straightforward to install the
long series of Doconce dependencies:</paragraph><literal_block xml:space="preserve"># Version control systems
sudo apt-get install -y mercurial git subversion

# Python
sudo apt-get install -y idle ipython python-pip python-pdftools texinfo

# These lines are only necessary for Ubuntu 12.04 to install texlive 2012
ubuntu_version=`lsb_release -r | awl '{print $2}'`
if [ $ubuntu_version = &quot;12.04&quot; ]; then
  sudo add-apt-repository ppa:texlive-backports/ppa
  sudo apt-get update
fi
# LaTeX
sudo apt-get install -y texlive texlive-extra-utils texlive-latex-extra texlive-math-extra texlive-font-utils
# or sudo apt-get install -y texlive-full  # get everything
sudo apt-get install -y latexdiff auctex

# Image and movie tools
sudo apt-get install -y imagemagick netpbm mjpegtools pdftk giftrans gv evince smpeg-plaympeg mplayer totem ffmpeg libav-tools

# Misc
sudo apt-get install -y ispell pandoc libreoffice unoconv libreoffice-dmaths curl a2ps wdiff meld xxdiff diffpdf kdiff3 diffuse

# More Python software
pip install sphinx  # install pygments and docutils too
pip install mako
pip install -e svn+http://preprocess.googlecode.com/svn/trunk#egg=preprocess
pip install -e hg+https://bitbucket.org/logg/publish#egg=publish

pip install -e hg+https://bitbucket.org/ecollins/cloud_sptheme#egg=cloud_sptheme
pip install -e git+https://github.com/ryan-roemer/sphinx-bootstrap-theme#egg=sphinx-bootstrap-theme
pip install -e hg+https://bitbucket.org/miiton/sphinxjp.themes.solarized#egg=sphinxjp.themes.solarized
pip install -e git+https://github.com/shkumagai/sphinxjp.themes.impressjs#egg=sphinxjp.themes.impressjs
pip install -e svn+https://epydoc.svn.sourceforge.net/svnroot/epydoc/trunk/epydoc#egg=epydoc

# Doconce itself
rm -rf srclib   # put downloaded software in srclib
mkdir srclib
cd srclib
hg clone https://code.google.com/p/doconce/
cd doconce
sudo python setup.py install -y
cd ../..

# Ptex2tex
cd srclib
svn checkout http://ptex2tex.googlecode.com/svn/trunk/ ptex2tex
cd ptex2tex
sudo python setup.py install -y
cd latex
sh cp2texmf.sh  # copy stylefiles to ~/texmf directory
cd ../../..</literal_block><comment xml:space="preserve">Here are some comment lines that do not affect any formatting</comment><comment xml:space="preserve">these lines are converted to comments in the output format.</comment><comment xml:space="preserve">This may have some side effects, especially in rst and sphinx</comment><comment xml:space="preserve">where lines following the comment may be taken as part of</comment><comment xml:space="preserve">the comment if there are no blank lines after the comment.</comment><comment xml:space="preserve">One can use ## and the mako preprocessor to remove comments</comment><comment xml:space="preserve">*before* doconce sees the text. That can be useful when</comment><comment xml:space="preserve">doconce comments interferes with formatting.</comment><comment xml:space="preserve">The mako tool also supports &lt;%doc&gt; .. &lt;/%doc&gt;</comment></section><section ids="demos" names="demos"><title>Demos</title><paragraph>The current text is generated from a Doconce format stored in the:</paragraph><literal_block xml:space="preserve">docs/manual/manual.do.txt</literal_block><paragraph>file in the Doconce source code tree. We have made a
<reference name="demo web page" refuri="https://doconce.googlecode.com/hg/doc/demos/manual/index.html">demo web page</reference><target ids="demo-web-page" names="demo\ web\ page" refuri="https://doconce.googlecode.com/hg/doc/demos/manual/index.html"/>
where you can compare the Doconce source with the output in many
different formats: HTML, LaTeX, plain text, etc.</paragraph><paragraph>The file <literal>make.sh</literal> in the same directory as the <literal>manual.do.txt</literal> file
(the current text) shows how to run <literal>doconce format</literal> on the
Doconce file to obtain documents in various formats.</paragraph><paragraph>Another demo is found in:</paragraph><literal_block xml:space="preserve">docs/tutorial/tutorial.do.txt</literal_block><paragraph>In the <literal>tutorial</literal> directory there is also a <literal>make.sh</literal> file producing a
lot of formats, with a corresponding
<reference name="web demo" refuri="https://doconce.googlecode.com/hg/doc/demos/tutorial/index.html">web demo</reference><target ids="web-demo" names="web\ demo" refuri="https://doconce.googlecode.com/hg/doc/demos/tutorial/index.html"/>
of the results.</paragraph><comment xml:space="preserve">Example on including another Doconce file:</comment><target refid="doconce2formats"/></section></section><section ids="from-doconce-to-other-formats doconce2formats" names="from\ doconce\ to\ other\ formats doconce2formats"><title>From Doconce to Other Formats</title><paragraph>Transformation of a Doconce document <literal>mydoc.do.txt</literal> to various other
formats apply the script <literal>doconce format</literal>:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format format mydoc.do.txt</literal_block><paragraph>or just:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format format mydoc</literal_block><section ids="generating-a-makefile" names="generating\ a\ makefile"><title>Generating a makefile</title><paragraph>Producing HTML, Sphinx, and in particular LaTeX documents from
Doconce sources requires a few commands. Often you want to
produce several different formats. The relevant commands should
then be placed in a script that acts as a &quot;makefile&quot;.</paragraph><paragraph>The <literal>doconce makefile</literal> can be used to automatically generate
such a makefile, more precisely a Bash script <literal>make.sh</literal>, which
carries out the commands explained below. If our Doconce source
is in <literal>main_myproj.do.txt</literal>, we run:</paragraph><literal_block xml:space="preserve">doconce makefile main_myproj html pdflatex sphinx</literal_block><paragraph>to produce the necessary output for generating HTML, pdfLaTeX, and
Sphinx. Usually, you need to edit <literal>make.sh</literal> to really fit your
needs. Some examples lines are inserted as comments to show
various options that can be added to the basic commands.
A handy feature of the generated <literal>make.sh</literal> script is that it
inserts checks for successful runs of the <literal>doconce format</literal> commands,
and if something goes wrong, the <literal>make.sh</literal> exits.</paragraph></section><section ids="preprocessing" names="preprocessing"><title>Preprocessing</title><paragraph>The <literal>preprocess</literal> and <literal>mako</literal> programs are used to preprocess the
file, and options to <literal>preprocess</literal> and/or <literal>mako</literal> can be added after the
filename. For example:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format latex mydoc -Dextra_sections -DVAR1=5     # preprocess
Terminal&gt; doconce format latex yourdoc extra_sections=True VAR1=5  # mako</literal_block><paragraph>The variable <literal>FORMAT</literal> is always defined as the current format when
running <literal>preprocess</literal> or <literal>mako</literal>. That is, in the last example, <literal>FORMAT</literal> is
defined as <literal>latex</literal>. Inside the Doconce document one can then perform
format specific actions through tests like <literal>#if FORMAT == &quot;latex&quot;</literal>
(for <literal>preprocess</literal>) or <literal>% if FORMAT == &quot;latex&quot;:</literal> (for <literal>mako</literal>).</paragraph></section><section ids="removal-of-inline-comments" names="removal\ of\ inline\ comments"><title>Removal of inline comments</title><paragraph>The command-line arguments <literal>--no_preprocess</literal> and <literal>--no_mako</literal> turn off
running <literal>preprocess</literal> and <literal>mako</literal>, respectively.</paragraph><paragraph>Inline comments in the text are removed from the output by:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format latex mydoc --skip_inline_comments</literal_block><paragraph>One can also remove all such comments from the original Doconce
file by running:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce remove_inline_comments mydoc</literal_block><paragraph>This action is convenient when a Doconce document reaches its final form
and comments by different authors should be removed.</paragraph></section><section ids="notes" names="notes"><title>Notes</title><paragraph>Doconce does not have a tag for longer notes, because implementation
of a &quot;notes feature&quot; is so easy using the <literal>preprocess</literal> or <literal>mako</literal>
programs. Just introduce some variable, say <literal>NOTES</literal>, that you define
through <literal>-DNOTES</literal> (or not) when running <literal>doconce format ...</literal>. Inside
the document you place your notes between <literal># #ifdef NOTES</literal> and
<literal># #endif</literal> preprocess tags. Alternatively you use <literal>% if NOTES:</literal>
and <literal>% endif</literal> that <literal>mako</literal> will recognize. In the same way you may
encapsulate unfinished material, extra material to be removed
for readers but still nice to archive as part of the document for
future revisions.</paragraph></section><section ids="demo-of-different-formats" names="demo\ of\ different\ formats"><title>Demo of different formats</title><paragraph>A simple scientific report is available in <reference name="a lot of different formats" refuri="http://hplgit.github.com/teamods/writing_reports/doconce_commands.html">a lot of different formats</reference><target ids="a-lot-of-different-formats" names="a\ lot\ of\ different\ formats" refuri="http://hplgit.github.com/teamods/writing_reports/doconce_commands.html"/>.
How to create the different formats is explained in more depth
in the coming sections.</paragraph></section><section ids="html" names="html"><title>HTML</title><section ids="basics" names="basics"><title>Basics</title><paragraph>Making an HTML version of a Doconce file <literal>mydoc.do.txt</literal>
is performed by:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format html mydoc</literal_block><paragraph>The resulting file <literal>mydoc.html</literal> can be loaded into any web browser for viewing.</paragraph></section><section ids="typesetting-of-code" names="typesetting\ of\ code"><title>Typesetting of Code</title><paragraph>If the Pygments package (including the <literal>pygmentize</literal> program)
is installed, code blocks are typeset with
aid of this package. The command-line argument <literal>--no_pygments_html</literal>
turns off the use of Pygments and makes code blocks appear with
plain (<literal>pre</literal>) HTML tags. The option <literal>--pygments_html_linenos</literal> turns
on line numbers in Pygments-formatted code blocks. A specific
Pygments style is set by <literal>--pygments_html_style=style</literal>, where <literal>style</literal>
can be <literal>default</literal>, <literal>emacs</literal>, <literal>perldoc</literal>, and other valid names for
Pygments styles.</paragraph></section><section ids="html-styles" names="html\ styles"><title>HTML Styles</title><paragraph>The HTML style can be defined either in the header of the HTML file,
using a named built-in style;
in an external CSS file; or in a template file.</paragraph><paragraph>An external CSS file <literal>filename</literal> used by setting the command-line
argument <literal>--css=filename</literal>. There available built-in styles are
specified as <literal>--html_style=name</literal>, where <literal>name</literal> can be</paragraph><block_quote><bullet_list bullet="*"><list_item><paragraph><literal>solarized</literal>: the famous <reference name="solarized" refuri="http://ethanschoonover.com/solarized">solarized</reference><target ids="solarized" names="solarized" refuri="http://ethanschoonover.com/solarized"/>
style (yellowish),</paragraph></list_item><list_item><paragraph><literal>blueish</literal>: a simple style with blue headings (default),</paragraph></list_item><list_item><paragraph><literal>blueish2</literal>: a variant of <emphasis>bluish</emphasis>,</paragraph></list_item><list_item><paragraph><literal>bloodish</literal>: as <literal>bluish</literal>, but dark read as color.</paragraph></list_item></bullet_list></block_quote><paragraph>Using <literal>--css=filename</literal> where <literal>filename</literal> is a non-existing file makes
Doconce write the built-in style to that file. Otherwise the HTML
links to the CSS stylesheet in <literal>filename</literal>. Several stylesheets can
be specified: <literal>--ccs=file1.css,file2.css,file3.css</literal>.</paragraph></section><section ids="html-templates" names="html\ templates"><title>HTML templates</title><paragraph>Templates are HTML files with &quot;slots&quot; <literal>%(main)s</literal> for the main body
of text, <literal>%(title)s</literal> for the title, and <literal>%(date)s</literal> for the date.
Doconce comes with a few templates. The usage of templates is
described in a <reference name="separate document" refuri="https://doconce.googlecode.com/hg/doc/design/wrapper_tech.html">separate document</reference><target ids="separate-document" names="separate\ document" refuri="https://doconce.googlecode.com/hg/doc/design/wrapper_tech.html"/>. That document describes how you your Doconce-generated
HTML file can have any specified layout.</paragraph><paragraph>The HTML file can be embedded in a template with your own tailored
design, see a &quot;tutorial&quot;: &quot; <reference name="https://doconce.googlecode.com/hg/doc/design/wrapper_tech.html" refuri="https://doconce.googlecode.com/hg/doc/design/wrapper_tech.html">https://doconce.googlecode.com/hg/doc/design/wrapper_tech.html</reference><target ids="https-doconce-googlecode-com-hg-doc-design-wrapper-tech-html" names="https://doconce.googlecode.com/hg/doc/design/wrapper_tech.html" refuri="https://doconce.googlecode.com/hg/doc/design/wrapper_tech.html"/>&quot; on this topic. The template file must contain
valid HTML code and can have three &quot;slots&quot;: <literal>%(title)s</literal> for a title,
<literal>%(date)s</literal> for a date, and <literal>%(main)s</literal> for the main body of text. The
latter is the
Doconce document translated to HTML. The title becomes the first
heading in the Doconce document, or the title (but a title is not
recommended when using templates). The date is extracted from the
<literal>DATE:</literal> line. With the template feature one can easily embed
the text in the look and feel of a website. Doconce comes with
two templates in <literal>bundled/html_styles</literal>. Just copy the directory
containing the template and the CSS and JavaScript files to your
document directory, edit the template as needed (also check that
paths to the <literal>css</literal> and <literal>js</literal> subdirectories are correct - according
to how you store the template files), and run:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format html mydoc --html_template=mytemplate.html</literal_block><paragraph>The template in <literal>style_vagrant</literal> also needs an extra option
<literal>--html_style=vagrant</literal>. With this style, one has nice navigation buttons
that are used if the document contains <literal>!split</literal> commands for splitting
it into many pages.</paragraph></section><section ids="the-html-file-collection" names="the\ html\ file\ collection"><title>The HTML File Collection</title><paragraph>There are usually a range of files needed for an HTML document arising
from a Doconce source. The needed files are listed in
<literal>.basename_html_file_collection</literal>, where <literal>basename</literal> is the filestem of
the Doconce file (i.e., the Doconce source is in <literal>basename.do.txt</literal>).</paragraph></section><section ids="filenames" names="filenames"><title>Filenames</title><paragraph>An HTML version of a Doconce document is often made in different styles,
calling for a need to rename the HTML output file. This is conveniently
done by the <literal>--html_output=basename</literal> option, where <literal>basename</literal> is the
filestem of the associated HTML files. The
<literal>.basename_html_file_collection</literal> file lists all the needed files
for the HTML document. Here is an example on making three versions of
the HTML document: <literal>mydoc_bloodish.html</literal>, <literal>mydoc_solarized</literal>, and
<literal>mydoc_vagrant</literal>:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format html mydoc --html_style=bloodish \
          --html_output=mydoc_bloodish
Terminal&gt; doconce split_html mydoc_bloodish.html
Terminal&gt; doconce format html mydoc --html_style=solarized \
          --html_output=mydoc_solarized \
          --pygments_html_style=perldoc --html_admon=apricot
Terminal&gt; doconce format html mydoc --html_style=vagrant \
          --html_output=mydoc_vagrant --pygments_html_style=default \
          --html_template=templates/my_adapted_vagrant_template.html
Terminal&gt; doconce split_html mydoc_vagrant.html</literal_block></section></section><section ids="blog-posts" names="blog\ posts"><title>Blog Posts</title><paragraph>Doconce can be used for writing blog posts provided the blog site accepts
raw HTML code. Google's Blogger service (<literal>blogger.com</literal> or
<literal>blogname.blogspot.com</literal>) is particularly well suited since it also
allows extensive LaTeX mathematics via MathJax.</paragraph><enumerated_list enumtype="arabic" prefix="" suffix="."><list_item><paragraph>Write the blog text as a Doconce document without any
title, author, and date.</paragraph></list_item><list_item><paragraph>Generate HTML as described above.</paragraph></list_item><list_item><paragraph>Copy the text and paste it into the
text area in the blog (just delete the HTML code that initially
pops up in the text area). Make sure the input format is HTML.</paragraph></list_item></enumerated_list><paragraph>See a <reference name="simple blog example" refuri="http://doconce.blogspot.no">simple blog example</reference><target ids="simple-blog-example" names="simple\ blog\ example" refuri="http://doconce.blogspot.no"/> and
a <reference name="scientific report" refuri="http://doconce-report-demo.blogspot.no/">scientific report</reference><target dupnames="scientific\ report" ids="scientific-report" refuri="http://doconce-report-demo.blogspot.no/"/>
for demonstrations of blogs at <literal>blogspot.no</literal>.</paragraph><warning><paragraph>In the readers' comments after the blog one cannot paste raw HTML
code with MathJax
scripts so there is no support for mathematics in the discussion forum.</paragraph></warning><note><paragraph>Figure files must be uploaded to some web site and the filenames name must
be replaced by the relevant URL. This can be automatically edited:</paragraph><literal_block xml:space="preserve">cp mydoc.do.txt mydoc2.do.txt
url=&quot;https//raw.github.com/someuser/someuser.github.com&quot;
dir=&quot;master/project/dir1/dir2&quot;
for figname in fig1 fig2 fig3; do
  doconce replace &quot;[$figname,&quot; &quot;[$site/$dir/$figname.png,&quot; \
          mydoc2.do.txt
done
doconce format html mydoc2
# Paste mydoc2.html into a new blog page</literal_block></note><paragraph>Blog posts at Google can also be published <reference name="automatically through email" refuri="http://support.google.com/blogger/bin/answer.py?hl=en&amp;answer=41452">automatically through email</reference><target ids="automatically-through-email" names="automatically\ through\ email" refuri="http://support.google.com/blogger/bin/answer.py?hl=en&amp;answer=41452"/>.
A Python program can send the contents of the HTML file
to the blog's email address using the packages  <literal>smtplib</literal> and <literal>email</literal>.</paragraph><system_message backrefs="id3" level="2" line="598" source="manual.rst" type="WARNING"><paragraph>Duplicate explicit target name: &quot;scientific report&quot;.</paragraph></system_message><paragraph>WordPress (<literal>wordpress.com</literal>) allows raw HTML code in blogs,
but has very limited
LaTeX support, basically only formulas. The <literal>--wordpress</literal> option to
<literal>doconce</literal> modifies the HTML code such that all equations are typeset
in a way that is acceptable to WordPress.
Look at a <reference name="simple doconce example" refuri="http://doconce.wordpress.com">simple doconce example</reference><target ids="simple-doconce-example" names="simple\ doconce\ example" refuri="http://doconce.wordpress.com"/>
and a <reference name="scientific report" refuri="http://doconcereportdemo.wordpress.com/">scientific report</reference><target dupnames="scientific\ report" ids="id3" refuri="http://doconcereportdemo.wordpress.com/"/>
to see blogging with mathematics and code on WordPress.</paragraph><paragraph>Speaking of WordPress, the related project <reference name="http://pressbooks.com" refuri="http://pressbooks.com">http://pressbooks.com</reference><target ids="http-pressbooks-com" names="http://pressbooks.com" refuri="http://pressbooks.com"/> can take raw HTML code (from Doconce, for
instance) and produce very nice-looking books.  There is no support
for mathematics in the text, though.</paragraph></section><section ids="pandoc-and-markdown" names="pandoc\ and\ markdown"><title>Pandoc and Markdown</title><paragraph>Output in Pandoc's extended Markdown format results from:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format pandoc mydoc</literal_block><paragraph>The name of the output file is <literal>mydoc.mkd</literal>.
From this format one can go to numerous other formats:</paragraph><literal_block xml:space="preserve">Terminal&gt; pandoc -R -t mediawiki -o mydoc.mwk --toc mydoc.mkd</literal_block><paragraph>Pandoc supports <literal>latex</literal>, <literal>html</literal>, <literal>odt</literal> (OpenOffice), <literal>docx</literal> (Microsoft
Word), <literal>rtf</literal>, <literal>texinfo</literal>, to mention some. The <literal>-R</literal> option makes
Pandoc pass raw HTML or LaTeX to the output format instead of ignoring it,
while the <literal>--toc</literal> option generates a table of contents.
See the <reference name="Pandoc documentation" refuri="http://johnmacfarlane.net/pandoc/README.html">Pandoc documentation</reference><target ids="pandoc-documentation" names="pandoc\ documentation" refuri="http://johnmacfarlane.net/pandoc/README.html"/>
for the many features of the <literal>pandoc</literal> program. The HTML output from
<literal>pandoc</literal> needs adjustments to provide full support for MathJax LaTeX
mathematics, and for this purpose one should use <literal>doconce md2html</literal>:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format pandoc mydoc
Terminal&gt; doconce m2html mydoc</literal_block><paragraph>The result <literal>mydoc.html</literal> can be viewed in a browser.</paragraph><paragraph>Pandoc is useful to go from LaTeX mathematics to, e.g., HTML or MS
Word.  There are two ways (experiment to find the best one for your
document): <literal>doconce format pandoc</literal> and then translating using <literal>doconce
md2latex</literal> (which runs <literal>pandoc</literal>), or <literal>doconce format latex</literal>, and then
going from LaTeX to the desired format using <literal>pandoc</literal>.
Here is an example on the latter strategy:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format latex mydoc
Terminal&gt; doconce ptex2tex mydoc
Terminal&gt; doconce replace '\Verb!' '\verb!' mydoc.tex
Terminal&gt; pandoc -f latex -t docx -o mydoc.docx mydoc.tex</literal_block><paragraph>When we go through <literal>pandoc</literal>, only single equations, <literal>align</literal>, or <literal>align*</literal>
environments are well understood for output to HTML.</paragraph><paragraph>Note that Doconce applies the <literal>Verb</literal> macro from the <literal>fancyvrb</literal> package
while <literal>pandoc</literal> only supports the standard <literal>verb</literal> construction for
inline verbatim text.  Moreover, quite some additional <literal>doconce
replace</literal> and <literal>doconce subst</literal> edits might be needed on the <literal>.mkd</literal> or
<literal>.tex</literal> files to successfully have mathematics that is well translated
to MS Word.  Also when going to reStructuredText using Pandoc, it can
be advantageous to go via LaTeX.</paragraph><paragraph>Here is an example where we take a Doconce snippet (without title, author,
and date), maybe with some unnumbered equations, and quickly generate
HTML with mathematics displayed my MathJax:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format pandoc mydoc
Terminal&gt; pandoc -t html -o mydoc.html -s --mathjax mydoc.mkd</literal_block><paragraph>The <literal>-s</literal> option adds a proper header and footer to the <literal>mydoc.html</literal> file.
This recipe is a quick way of makeing HTML notes with (some) mathematics.</paragraph><section ids="github-flavored-markdown" names="github-flavored\ markdown"><title>GitHub-flavored Markdown</title><paragraph>Adding the command-line option <literal>github-md</literal> turns on the GutHub-flavored
Markdown dialect, which is used for the issue tracker on <reference name="GitHub" refuri="http://github.com">GitHub</reference><target ids="github" names="github" refuri="http://github.com"/>. A special feature is the support of task lists:
unnumbered lists with <literal>[x]</literal> (task done) or <literal>[ ]</literal> (task not done).
(Tables get typeset directly as HTML and the syntax for code highlighting
is different from Pandoc extended Markdown.) Here is an example:</paragraph><literal_block xml:space="preserve">!bquote
===== Problems with a function =====

There is a problem with the `f(x)` function

!bc pycod
def f(x):
    return 1 + x
!ec
This function should be quadratic.
!equote

OK, this is fixed:

!bc pycod
def f(x, a=1, b=1, c=1):
    return a*x**2 + b*x + c
!ec

===== Updated task list =====

   * [x] Offer an `f(x)` function
   * [ ] Extension to cubic functions
   * [x] Allowing general coefficient in the quadratic function

=== Remaining functionality ===

|---------------------------------------------------------------|
| function | purpose                        | state |
|----l-----------l------------------------------l---------------|
|  `g(x)`  | Compute the Gaussian function. | Formula ready. |
|  `h(x)`  | Heaviside function.            | Formula ready. |
|  `I(x)`  | Indicator function.            | Nothing done yet. |
|---------------------------------------------------------------|</literal_block><paragraph>Say this text is stored in a file <literal>mycomments.do.txt</literal>. Running:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format pandoc mycomments --github_md</literal_block><paragraph>produces <literal>mycomments.md</literal> which can be pasted into the Write field of
the GitHub issue tracker. Turning on Preview shows the typesetting of
the quote, compute code, inline verbatim, headings, the task list, and
the table.</paragraph></section></section><section ids="latex" names="latex"><title>LaTeX</title><paragraph>Making a LaTeX file <literal>mydoc.tex</literal> from <literal>mydoc.do.txt</literal> is done in two steps:</paragraph><comment xml:space="preserve">Note: putting code blocks inside a list is not successful in many</comment><comment xml:space="preserve">formats - the text may be messed up. A better choice is a paragraph</comment><comment xml:space="preserve">environment, as used here.</comment><paragraph><emphasis>Step 1.</emphasis> Filter the doconce text to a pre-LaTeX form <literal>mydoc.p.tex</literal> for
the <literal>ptex2tex</literal> program (or <literal>doconce ptex2tex</literal>):</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format latex mydoc</literal_block><paragraph>LaTeX-specific commands (&quot;newcommands&quot;) in math formulas and similar
can be placed in files <literal>newcommands.tex</literal>, <literal>newcommands_keep.tex</literal>, or
<literal>newcommands_replace.tex</literal> (see the section <reference name="Macros (Newcommands)" refid="macros-newcommands">Macros (Newcommands)</reference>).
If these files are present, they are included in the LaTeX document
so that your commands are defined.</paragraph><paragraph>An option <literal>--device=paper</literal> makes some adjustments for documents
aimed at being printed. For example, links to web resources are
associated with a footnote listing the complete web address (URL).
The default, <literal>--device=screen</literal>, creates a PDF file for reading
on a screen where links are just clickable.</paragraph><paragraph><emphasis>Step 2.</emphasis> Run <literal>ptex2tex</literal> (if you have it) to make a standard LaTeX file:</paragraph><literal_block xml:space="preserve">Terminal&gt; ptex2tex mydoc</literal_block><paragraph>In case you do not have <literal>ptex2tex</literal>, you may run a (very) simplified version:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce ptex2tex mydoc</literal_block><paragraph>Note that Doconce generates a <literal>.p.tex</literal> file with some preprocessor macros
that can be used to steer certain properties of the LaTeX document.
For example, to turn on the Helvetica font instead of the standard
Computer Modern font, run:</paragraph><literal_block xml:space="preserve">Terminal&gt; ptex2tex -DHELVETICA mydoc
Terminal&gt; doconce ptex2tex mydoc -DHELVETICA  # alternative</literal_block><paragraph>The title, authors, and date are by default typeset in a non-standard
way to enable a nicer treatment of multiple authors having
institutions in common. However, the standard LaTeX &quot;maketitle&quot; heading
is also available through <literal>-DLATEX_HEADING=traditional</literal>.
A separate titlepage can be generate by
<literal>-DLATEX_HEADING=titlepage</literal>.</paragraph><paragraph>Preprocessor variables to be defined or undefined are</paragraph><block_quote><bullet_list bullet="*"><list_item><paragraph><literal>XELATEX</literal> for processing by <literal>xelatex</literal></paragraph></list_item><list_item><paragraph><literal>PALATINO</literal> for the Palatino font</paragraph></list_item><list_item><paragraph><literal>HELVETICA</literal> for the Helvetica font</paragraph></list_item><list_item><paragraph><literal>A4PAPER</literal> for A4 paper size</paragraph></list_item><list_item><paragraph><literal>A6PAPER</literal> for A6 paper size (suitable for reading PDFs on phones)</paragraph></list_item><list_item><paragraph><literal>MOVIE</literal> for specifying how movies are handled: the value <literal>media9</literal>
implies the <literal>media9</literal> package and the <literal>\includemedia</literal> command (default),
while other values are <literal>movie15</literal> (<literal>\includemovie</literal> command),
<literal>multimedia</literal> (for Beamer-style <literal>\movie</literal> command),
or <literal>href-run</literal> (for the plain <title_reference>h`run:file`_</title_reference> command)</paragraph></list_item><list_item><paragraph><literal>MOVIE_CONTROLS</literal> adds buttons for starting/stopping movies if the
<literal>media9</literal> package is used.</paragraph></list_item><list_item><paragraph><literal>PREAMBLE</literal> to turn the LaTeX preamble on or off (i.e., complete document
or document to be included elsewhere - and note that
the preamble is only included
if the document has a title, author, and date)</paragraph></list_item><list_item><paragraph><literal>MINTED</literal> for inclusion of the minted package for typesetting of
code with the Pygments tool (which requires <literal>latex</literal>
or <literal>pdflatex</literal> to be run with the <literal>-shell-escape</literal> option)</paragraph></list_item><list_item><paragraph><literal>TODONOTES</literal> for using the fancy <literal>todonotes</literal> package for typesetting
inline comments (looks much like track changes in MS Word). This
macro has only effect if inline comments are used (name, colon,
and comment inside brackets).</paragraph></list_item><list_item><paragraph><literal>LINENUMBERS</literal> for inclusion of line numbers in the text.</paragraph></list_item><list_item><paragraph><literal>COLORED_TABLE_ROWS</literal> for coloring every other table rows (set this
variable to <literal>gray</literal> or <literal>blue</literal>)</paragraph></list_item><list_item><paragraph><literal>BLUE_SECTION_HEADINGS</literal> for blue section and subsection headings</paragraph></list_item><list_item><paragraph><literal>LATEX_HEADING</literal> for the typesetting of the title, author, parts of
preamble (values: <literal>traditional</literal> for traditional LaTeX heading,
<literal>titlepage</literal> for a separate titlepage, <literal>Springer_collection</literal> for
edited volumes on Springer, <literal>beamer</literal> for Beamer slides, <literal>doconce_heading</literal>
(default) for listing institutions after names)</paragraph></list_item></bullet_list></block_quote><paragraph>If you are not satisfied with the Doconce preamble, you can provide
your own preamble by adding the command-line option <literal>--latex_preamble=myfile</literal>.
In case <literal>myfile</literal> contains a documentclass definition, Doconce assumes
that the file contains the <emphasis>complete</emphasis> preamble you want (not that all
the packages listed in the default preamble are required and must be
present in <literal>myfile</literal>). Otherwise, <literal>myfile</literal> is assumed to contain
<emphasis>additional</emphasis> LaTeX code to be added to the Doconce default preamble.</paragraph><paragraph>The <literal>ptex2tex</literal> tool makes it possible to easily switch between many
different fancy formattings of computer or verbatim code in LaTeX
documents. After any <literal>!bc</literal> command in the Doconce source you can
insert verbatim block styles as defined in your <literal>.ptex2tex.cfg</literal>
file, e.g., <literal>!bc sys</literal> for a terminal session, where <literal>sys</literal> is set to
a certain environment in <literal>.ptex2tex.cfg</literal> (e.g., <literal>CodeTerminal</literal>).
There are about 40 styles to choose from, and you can easily add
new ones.</paragraph><paragraph>Also the <literal>doconce ptex2tex</literal> command supports preprocessor directives
for processing the <literal>.p.tex</literal> file. The command allows specifications
of code environments as well. Here is an example:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce ptex2tex mydoc -DLATEX_HEADING=traditional \
          -DPALATINO -DA6PAPER \
          &quot;sys=\begin{quote}\begin{verbatim}@\end{verbatim}\end{quote}&quot; \
          fpro=minted fcod=minted shcod=Verbatim envir=ans:nt</literal_block><paragraph>Note that <literal>@</literal> must be used to separate the begin and end LaTeX
commands, unless only the environment name is given (such as <literal>minted</literal>
above, which implies <literal>\begin{minted}{fortran}</literal> and <literal>\end{minted}</literal> as
begin and end for blocks inside <literal>!bc fpro</literal> and <literal>!ec</literal>).  Specifying
<literal>envir=ans:nt</literal> means that all other environments are typeset with the
<literal>anslistings.sty</literal> package, e.g., <literal>!bc cppcod</literal> will then result in
<literal>\begin{c++}</literal>. A predefined shortcut as in <literal>shcod=Verbatim-0.85</literal>
results in denser
vertical spacing (baselinestretch 0.85 in LaTeX terminology), and
<literal>shcod=Verbatim-indent</literal> implies indentation of the verbatim text.
Alternatively, one can provide all desired parameters
<literal>\begin{Verbatim}</literal> instruction using the syntax illustrated for
the <literal>sys</literal> environments above.</paragraph><paragraph>If no environments like <literal>sys</literal>, <literal>fpro</literal>, or the common
<literal>envir</literal> are defined on the command line, the plain <literal>\begin{Verbatim}</literal>
and <literal>\end{Verbatim}</literal> instructions are used.</paragraph><paragraph><emphasis>Step 2b (optional).</emphasis> Edit the <literal>mydoc.tex</literal> file to your needs.
For example, you may want to substitute <literal>section</literal> by <literal>section*</literal> to
avoid numbering of sections, you may want to insert linebreaks
(and perhaps space) in the title, etc. This can be automatically
edited with the aid of the <literal>doconce replace</literal> and <literal>doconce subst</literal>
commands. The former works with substituting text directly, while the
latter performs substitutions using regular expressions.
You will use <literal>doconce replace</literal> to edit <literal>section{</literal> to <literal>section*{</literal>:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce replace 'section{' 'section*{' mydoc.tex</literal_block><paragraph>For fixing the line break of a title, you may pick a word in the
title, say &quot;Using&quot;, and insert a break after than word. With
<literal>doconce subst</literal> this is easy employing regular expressions with
a group before &quot;Using&quot; and a group after:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce subst 'title\{(.+)Using (.+)\}' \
          'title{\g&lt;1&gt; \\\\ [1.5mm] Using \g&lt;2&gt;' mydoc.tex</literal_block><paragraph>A lot of tailored fixes to the LaTeX document can be done by
an appropriate set of text replacements and regular expression
substitutions. You are anyway encourged to make a script for
generating PDF from the LaTeX file so the <literal>doconce subst</literal> or
<literal>doconce replace</literal> commands can be put inside the script.</paragraph><paragraph><emphasis>Step 3.</emphasis> Compile <literal>mydoc.tex</literal>
and create the PDF file:</paragraph><literal_block xml:space="preserve">Terminal&gt; latex mydoc
Terminal&gt; latex mydoc
Terminal&gt; makeindex mydoc   # if index
Terminal&gt; bibitem mydoc     # if bibliography
Terminal&gt; latex mydoc
Terminal&gt; dvipdf mydoc</literal_block><paragraph>If one wishes to run <literal>ptex2tex</literal> and use the minted LaTeX package for
typesetting code blocks (<literal>Minted_Python</literal>, <literal>Minted_Cpp</literal>, etc., in
<literal>ptex2tex</literal> specified through the <literal>*pro</literal> and <literal>*cod</literal> variables in
<literal>.ptex2tex.cfg</literal> or <literal>$HOME/.ptex2tex.cfg</literal>), the minted LaTeX package is
needed.  This package is included by running <literal>ptex2tex</literal> with the
<literal>-DMINTED</literal> option:</paragraph><literal_block xml:space="preserve">Terminal&gt; ptex2tex -DMINTED mydoc</literal_block><paragraph>In this case, <literal>latex</literal> must be run with the
<literal>-shell-escape</literal> option:</paragraph><literal_block xml:space="preserve">Terminal&gt; latex -shell-escape mydoc
Terminal&gt; latex -shell-escape mydoc
Terminal&gt; makeindex mydoc   # if index
Terminal&gt; bibitem mydoc     # if bibliography
Terminal&gt; latex -shell-escape mydoc
Terminal&gt; dvipdf mydoc</literal_block><paragraph>When running <literal>doconce ptex2tex mydoc envir=minted</literal> (or other minted
specifications with <literal>doconce ptex2tex</literal>), the minted package is automatically
included so there is no need for the <literal>-DMINTED</literal> option.</paragraph></section><section ids="pdflatex" names="pdflatex"><title>PDFLaTeX</title><paragraph>Running <literal>pdflatex</literal> instead of <literal>latex</literal> follows almost the same steps,
but the start is:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format latex mydoc</literal_block><paragraph>Then <literal>ptex2tex</literal> is run as explained above, and finally:</paragraph><literal_block xml:space="preserve">Terminal&gt; pdflatex -shell-escape mydoc
Terminal&gt; makeindex mydoc   # if index
Terminal&gt; bibitem mydoc     # if bibliography
Terminal&gt; pdflatex -shell-escape mydoc</literal_block></section><section ids="xelatex" names="xelatex"><title>XeLaTeX</title><paragraph>XeLaTeX is an alternative to pdfLaTeX and is run in almost the
same way, except for the <literal>-DXELATEX</literal> flag to ptex2tex:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format pdflatex mydoc
Terminal&gt; doconce ptex2tex mydoc -DXELATEX
Terminal&gt; ptex2tex -DXELATEX mydoc  # alternative
Terminal&gt; xelatex mydoc</literal_block></section><section ids="plain-ascii-text" names="plain\ ascii\ text"><title>Plain ASCII Text</title><paragraph>We can go from Doconce &quot;back to&quot; plain untagged text suitable for viewing
in terminal windows, inclusion in email text, or for insertion in
computer source code:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format plain mydoc.do.txt  # results in mydoc.txt</literal_block></section><section ids="restructuredtext" names="restructuredtext"><title>reStructuredText</title><paragraph>Going from Doconce to reStructuredText gives a lot of possibilities to
go to other formats. First we filter the Doconce text to a
reStructuredText file <literal>mydoc.rst</literal>:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format rst mydoc.do.txt</literal_block><paragraph>We may now produce various other formats:</paragraph><literal_block xml:space="preserve">Terminal&gt; rst2html.py  mydoc.rst &gt; mydoc.html # html
Terminal&gt; rst2latex.py mydoc.rst &gt; mydoc.tex  # latex
Terminal&gt; rst2xml.py   mydoc.rst &gt; mydoc.xml  # XML
Terminal&gt; rst2odt.py   mydoc.rst &gt; mydoc.odt  # OpenOffice</literal_block><paragraph>The OpenOffice file <literal>mydoc.odt</literal> can be loaded into OpenOffice and
saved in, among other things, the RTF format or the Microsoft Word format.
However, it is more convenient to use the program <literal>unovonv</literal>
to convert between the many formats OpenOffice supports <emphasis>on the command line</emphasis>.
Run:</paragraph><literal_block xml:space="preserve">Terminal&gt; unoconv --show</literal_block><paragraph>to see all the formats that are supported.
For example, the following commands take
<literal>mydoc.odt</literal> to Microsoft Office Open XML format,
classic MS Word format, and PDF:</paragraph><literal_block xml:space="preserve">Terminal&gt; unoconv -f ooxml mydoc.odt
Terminal&gt; unoconv -f doc mydoc.odt
Terminal&gt; unoconv -f pdf mydoc.odt</literal_block><paragraph><emphasis>Remark about Mathematical Typesetting.</emphasis> At the time of this writing, there is no easy way to go from Doconce
and LaTeX mathematics to reST and further to OpenOffice and the
&quot;MS Word world&quot;. Mathematics is only fully supported by <literal>latex</literal> as
output and to a wide extent also supported by the <literal>sphinx</literal> output format.
Some links for going from LaTeX to Word are listed below.</paragraph><block_quote><bullet_list bullet="*"><list_item><paragraph><reference name="http://ubuntuforums.org/showthread.php?t=1033441" refuri="http://ubuntuforums.org/showthread.php?t=1033441">http://ubuntuforums.org/showthread.php?t=1033441</reference><target ids="http-ubuntuforums-org-showthread-php-t-1033441" names="http://ubuntuforums.org/showthread.php?t=1033441" refuri="http://ubuntuforums.org/showthread.php?t=1033441"/></paragraph></list_item><list_item><paragraph><reference name="http://tug.org/utilities/texconv/textopc.html" refuri="http://tug.org/utilities/texconv/textopc.html">http://tug.org/utilities/texconv/textopc.html</reference><target ids="http-tug-org-utilities-texconv-textopc-html" names="http://tug.org/utilities/texconv/textopc.html" refuri="http://tug.org/utilities/texconv/textopc.html"/></paragraph></list_item><list_item><paragraph><reference name="http://nileshbansal.blogspot.com/2007/12/latex-to-openofficeword.html" refuri="http://nileshbansal.blogspot.com/2007/12/latex-to-openofficeword.html">http://nileshbansal.blogspot.com/2007/12/latex-to-openofficeword.html</reference><target ids="http-nileshbansal-blogspot-com-2007-12-latex-to-openofficeword-html" names="http://nileshbansal.blogspot.com/2007/12/latex-to-openofficeword.html" refuri="http://nileshbansal.blogspot.com/2007/12/latex-to-openofficeword.html"/></paragraph></list_item></bullet_list></block_quote></section><section ids="sphinx" names="sphinx"><title>Sphinx</title><paragraph>Sphinx documents demand quite some steps in their creation. We have automated
most of the steps through the <literal>doconce sphinx_dir</literal> command:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce sphinx_dir author=&quot;authors' names&quot; \
          title=&quot;some title&quot; version=1.0 dirname=sphinxdir \
          theme=mytheme file1 file2 file3 ...</literal_block><paragraph>The keywords <literal>author</literal>, <literal>title</literal>, and <literal>version</literal> are used in the headings
of the Sphinx document. By default, <literal>version</literal> is 1.0 and the script
will try to deduce authors and title from the doconce files <literal>file1</literal>,
<literal>file2</literal>, etc. that together represent the whole document. Note that
none of the individual Doconce files <literal>file1</literal>, <literal>file2</literal>, etc. should
include the rest as their union makes up the whole document.
The default value of <literal>dirname</literal> is <literal>sphinx-rootdir</literal>. The <literal>theme</literal>
keyword is used to set the theme for design of HTML output from
Sphinx (the default theme is <literal>'default'</literal>).</paragraph><paragraph>With a single-file document in <literal>mydoc.do.txt</literal> one often just runs:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce sphinx_dir mydoc</literal_block><paragraph>and then an appropriate Sphinx directory <literal>sphinx-rootdir</literal> is made with
relevant files.</paragraph><paragraph>The <literal>doconce sphinx_dir</literal> command generates a script
<literal>automake_sphinx.py</literal> for compiling the Sphinx document into an HTML
document.  One can either run <literal>automake_sphinx.py</literal> or perform the
steps in the script manually, possibly with necessary modifications.
Normally, executing the script works well, but if you are new
to Sphinx and end up producing quite some Sphinx documents, I encourave
you to read the Sphinx documentation and study the <literal>automake_sphinx.py</literal>
file.</paragraph><paragraph><emphasis>Links.</emphasis> The <literal>automake_sphinx.py</literal> script copies directories named <literal>fig*</literal>
over to the Sphinx directory so that figures are accessible
in the Sphinx compilation.  It also examines <literal>MOVIE:</literal> and <literal>FIGURE:</literal>
commands in the Doconce file to find other image files and copies
these too. I strongly recommend to put files
to which there are local links (not <literal>http:</literal> or <literal>file:</literal> URLs) in
a directory named <literal>_static</literal>. The <literal>automake_sphinx.py</literal> copies
<literal>_static*</literal> to the Sphinx directory, which guarantees that the links
to the local files will work in the Sphinx document.</paragraph><paragraph>There is a utility <literal>doconce sphinxfix_localURLs</literal> for checking links to
local files and moving the files to <literal>_static</literal> and changing the links
accordingly. For example, a link to <literal>dir1/dir2/myfile.txt</literal> is changed
to <literal>_static/myfile.txt</literal> and <literal>myfile.txt</literal> is copied to <literal>_static</literal>.
However, I recommend instead that you manually copy
files to <literal>_static</literal> when you want to link to them, or let your
script which compiles the Doconce document do it automatically.</paragraph><paragraph><emphasis>Themes.</emphasis> Doconce comes with a rich collection of HTML themes for Sphinx documents,
much larger than what is found in the standard Sphinx distribution.
Additional themes include
<literal>agni</literal>,
<literal>basicstrap</literal>,
<literal>bootstrap</literal>,
<literal>cloud</literal>,
<literal>fenics</literal>,
<literal>fenics_minimal</literal>,
<literal>flask</literal>,
<literal>haiku</literal>,
<literal>impressjs</literal>,
<literal>jal</literal>,
<literal>pylons</literal>,
<literal>redcloud</literal>,
<literal>scipy_lectures</literal>,
<literal>slim-agogo</literal>, and
<literal>vlinux-theme</literal>.</paragraph><paragraph>All the themes are packed out in the Sphinx directory, and the
<literal>doconce sphinx_dir</literal> insert lots of extra code in the <literal>conf.py</literal>
file to enable easy specification and customization of themes.
For example, modules are loaded for the additional themes that
come with Doconce, code is inserted to allow customization of
the look and feel of themes, etc. The <literal>conf.py</literal> file is a
good starting point for fine-tuning your favorite team, and your
own <literal>conf.py</literal> file can later be supplied and used when running
<literal>doconce sphinx_dir</literal>: simply add the command-line option
<literal>conf.py=conf.py</literal>.</paragraph><paragraph>A script
<literal>make-themes.sh</literal> can make HTML documents with one or more themes.
For example,
to realize the themes <literal>fenics</literal>, <literal>pyramid</literal>, and <literal>pylon</literal> one writes:</paragraph><literal_block xml:space="preserve">Terminal&gt; ./make-themes.sh fenics pyramid pylon</literal_block><paragraph>The resulting directories with HTML documents are <literal>_build/html_fenics</literal>
and <literal>_build/html_pyramid</literal>, respectively. Without arguments,
<literal>make-themes.sh</literal> makes all available themes (!). With <literal>make-themes.sh</literal>
it is easy to check out various themes to find the one that is most
attractive for your document.</paragraph><paragraph>You may supply your own theme and avoid copying all the themes
that come with Doconce into the Sphinx directory. Just specify
<literal>theme_dir=path</literal> on the command line, where <literal>path</literal> is the relative
path to the directory containing the Sphinx theme. You must also
specify a configure file by <literal>conf.py=path</literal>, where <literal>path</literal> is the
relative path to your <literal>conf.py</literal> file.</paragraph><paragraph><emphasis>Example.</emphasis> Say you like the <literal>scipy_lectures</literal> theme, but you want
a table of contents to appear <emphasis>to the right</emphasis>, much in the same style
as in the <literal>default</literal> theme (where the table of contents is to the left).
You can then run <literal>doconce sphinx_dir</literal>, invoke a text editor with the
<literal>conf.py</literal> file, find the line <literal>html_theme == 'scipy_lectures'</literal>,
edit the following <literal>nosidebar</literal> to <literal>false</literal> and <literal>rightsidebar</literal> to <literal>true</literal>.
Alternatively, you may write a little script using <literal>doconce replace</literal>
to replace a portion of text in <literal>conf.py</literal> by a new one:</paragraph><literal_block xml:space="preserve">doconce replace &quot;elif html_theme == 'scipy_lectures':
    html_theme_options = {
        'nosidebar': 'true',
        'rightsidebar': 'false',
        'sidebarbgcolor': '#f2f2f2',
        'sidebartextcolor': '#20435c',
        'sidebarlinkcolor': '#20435c',
        'footerbgcolor': '#000000',
        'relbarbgcolor': '#000000',
    }&quot; &quot;elif html_theme == 'scipy_lectures':
    html_theme_options = {
        'nosidebar': 'false',
        'rightsidebar': 'true',
        'sidebarbgcolor': '#f2f2f2',
        'sidebartextcolor': '#20435c',
        'sidebarlinkcolor': '#20435c',
        'footerbgcolor': '#000000',
        'relbarbgcolor': '#000000',
    }&quot; conf.py</literal_block><paragraph>Obviously, we could also have changed colors in the edit above.
The final alternative is to save the edited <literal>conf.py</literal> file somewhere
and reuse it the next time <literal>doconce sphinx_dir</literal> is run:</paragraph><literal_block xml:space="preserve">doconce sphinx_dir theme=scipy_lectures \
                   conf.py=../some/path/conf.py mydoc</literal_block><section ids="the-manual-sphinx-procedure" names="the\ manual\ sphinx\ procedure"><title>The manual Sphinx procedure</title><paragraph>If it is not desirable to use the autogenerated scripts explained
above, here is the complete manual procedure of generating a
Sphinx document from a file <literal>mydoc.do.txt</literal>.</paragraph><paragraph><emphasis>Step 1.</emphasis> Translate Doconce into the Sphinx format:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format sphinx mydoc</literal_block><paragraph><emphasis>Step 2.</emphasis> Create a Sphinx root directory
either manually or by using the interactive <literal>sphinx-quickstart</literal>
program. Here is a scripted version of the steps with the latter:</paragraph><literal_block xml:space="preserve">mkdir sphinx-rootdir
sphinx-quickstart &lt;&lt;EOF
sphinx-rootdir
n
_
Name of My Sphinx Document
Author
version
version
.rst
index
n
y
n
n
n
n
y
n
n
y
y
y
EOF</literal_block><paragraph>The autogenerated <literal>conf.py</literal> file
may need some edits if you want to specific layout (Sphinx themes)
of HTML pages. The <literal>doconce sphinx_dir</literal> generator makes an extended <literal>conv.py</literal>
file where, among other things, several useful Sphinx extensions
are included.</paragraph><paragraph><emphasis>Step 3.</emphasis> Copy the <literal>mydoc.rst</literal> file to the Sphinx root directory:</paragraph><literal_block xml:space="preserve">Terminal&gt; cp mydoc.rst sphinx-rootdir</literal_block><paragraph>If you have figures in your document, the relative paths to those will
be invalid when you work with <literal>mydoc.rst</literal> in the <literal>sphinx-rootdir</literal>
directory. Either edit <literal>mydoc.rst</literal> so that figure file paths are correct,
or simply copy your figure directories to <literal>sphinx-rootdir</literal>.
Links to local files in <literal>mydoc.rst</literal> must be modified to links to
files in the <literal>_static</literal> directory, see comment above.</paragraph><paragraph><emphasis>Step 4.</emphasis> Edit the generated <literal>index.rst</literal> file so that <literal>mydoc.rst</literal>
is included, i.e., add <literal>mydoc</literal> to the <literal>toctree</literal> section so that it becomes:</paragraph><literal_block xml:space="preserve">.. toctree::
   :maxdepth: 2

   mydoc</literal_block><paragraph>(The spaces before <literal>mydoc</literal> are important!)</paragraph><paragraph><emphasis>Step 5.</emphasis> Generate, for instance, an HTML version of the Sphinx source:</paragraph><literal_block xml:space="preserve">make clean   # remove old versions
make html</literal_block><paragraph>Sphinx can generate a range of different formats:
standalone HTML, HTML in separate directories with <literal>index.html</literal> files,
a large single HTML file, JSON files, various help files (the qthelp, HTML,
and Devhelp projects), epub, LaTeX, PDF (via LaTeX), pure text, man pages,
and Texinfo files.</paragraph><paragraph><emphasis>Step 6.</emphasis> View the result:</paragraph><literal_block xml:space="preserve">Terminal&gt; firefox _build/html/index.html</literal_block><paragraph>Note that verbatim code blocks can be typeset in a variety of ways
depending the argument that follows <literal>!bc</literal>: <literal>cod</literal> gives Python
(<literal>code-block:: python</literal> in Sphinx syntax) and <literal>cppcod</literal> gives C++, but
all such arguments can be customized both for Sphinx and LaTeX output.</paragraph></section></section><section ids="wiki-formats" names="wiki\ formats"><title>Wiki Formats</title><paragraph>There are many different wiki formats, but Doconce only supports three:
<reference name="Googlecode wiki" refuri="http://code.google.com/p/support/wiki/WikiSyntax">Googlecode wiki</reference><target ids="googlecode-wiki" names="googlecode\ wiki" refuri="http://code.google.com/p/support/wiki/WikiSyntax"/>,
<reference name="MediaWiki" refuri="http://www.mediawiki.org/wiki/Help:Formatting">MediaWiki</reference><target ids="mediawiki" names="mediawiki" refuri="http://www.mediawiki.org/wiki/Help:Formatting"/>, and
<reference name="Creole Wiki" refuri="http://www.wikicreole.org/wiki/Creole1.0">Creole Wiki</reference><target ids="creole-wiki" names="creole\ wiki" refuri="http://www.wikicreole.org/wiki/Creole1.0"/>.
These formats are called
<literal>gwiki</literal>, <literal>mwiki</literal>, and <literal>cwiki</literal>, respectively.
Transformation from Doconce to these formats is done by:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format gwiki mydoc.do.txt
Terminal&gt; doconce format mwiki mydoc.do.txt
Terminal&gt; doconce format cwiki mydoc.do.txt</literal_block><paragraph>The produced MediaWiki can be tested in the <reference name="sandbox of wikibooks.org" refuri="http://en.wikibooks.org/wiki/Sandbox">sandbox of
wikibooks.org</reference><target ids="sandbox-of-wikibooks-org" names="sandbox\ of\ wikibooks.org" refuri="http://en.wikibooks.org/wiki/Sandbox"/>. The format
works well with Wikipedia, Wikibooks, and
<reference name="ShoutWiki" refuri="http://doconcedemo.shoutwiki.com/wiki/Doconce_demo_page">ShoutWiki</reference><target ids="shoutwiki" names="shoutwiki" refuri="http://doconcedemo.shoutwiki.com/wiki/Doconce_demo_page"/>,
but not always well elsewhere
(see <reference name="this example" refuri="http://doconcedemo.jumpwiki.com/wiki/First_demo">this example</reference><target ids="this-example" names="this\ example" refuri="http://doconcedemo.jumpwiki.com/wiki/First_demo"/>).</paragraph><paragraph>Large MediaWiki documents can be made with the
<reference name="Book creator" refuri="http://en.wikipedia.org/w/index.php?title=Special:Book&amp;bookcmd=book_creator">Book creator</reference><target ids="book-creator" names="book\ creator" refuri="http://en.wikipedia.org/w/index.php?title=Special:Book&amp;bookcmd=book_creator"/>.
From the MediaWiki format one can go to other formats with aid
of <reference name="mwlib" refuri="http://pediapress.com/code/">mwlib</reference><target ids="mwlib" names="mwlib" refuri="http://pediapress.com/code/"/>. This means that one can
easily use Doconce to write <reference name="Wikibooks" refuri="http://en.wikibooks.org">Wikibooks</reference><target ids="wikibooks" names="wikibooks" refuri="http://en.wikibooks.org"/>
and publish these in PDF and MediaWiki format, while
at the same time, the book can also be published as a
standard LaTeX book, a Sphinx web document, or a collection of HTML files.</paragraph><paragraph>The Googlecode wiki document, <literal>mydoc.gwiki</literal>, is most conveniently stored
in a directory which is a clone of the wiki part of the Googlecode project.
This is far easier than copying and pasting the entire text into the
wiki editor in a web browser.</paragraph><paragraph>When the Doconce file contains figures, each figure filename must in
the <literal>.gwiki</literal> file be replaced by a URL where the figure is
available. There are instructions in the file for doing this. Usually,
one performs this substitution automatically (see next section).</paragraph></section><section ids="tweaking-the-doconce-output" names="tweaking\ the\ doconce\ output"><title>Tweaking the Doconce Output</title><paragraph>Occasionally, one would like to tweak the output in a certain format
from Doconce. One example is figure filenames when transforming
Doconce to reStructuredText. Since Doconce does not know if the
<literal>.rst</literal> file is going to be filtered to LaTeX or HTML, it cannot know
if <literal>.eps</literal> or <literal>.png</literal> is the most appropriate image filename.
The solution is to use a text substitution command or code with, e.g., sed,
perl, python, or scitools subst, to automatically edit the output file
from Doconce. It is then wise to run Doconce and the editing commands
from a script to automate all steps in going from Doconce to the final
format(s). The <literal>make.sh</literal> files in <literal>docs/manual</literal> and <literal>docs/tutorial</literal>
constitute comprehensive examples on how such scripts can be made.</paragraph></section></section><section ids="the-doconce-markup-language" names="the\ doconce\ markup\ language"><title>The Doconce Markup Language</title><paragraph>The Doconce format introduces four constructs to markup text:
lists, special lines, inline tags, and environments.</paragraph><section ids="lists" names="lists"><title>Lists</title><paragraph>An unordered bullet list makes use of the <literal>*</literal> as bullet sign
and is indented as follows:</paragraph><literal_block xml:space="preserve">* item 1

* item 2

  * subitem 1, if there are more
    lines, each line must
    be intended as shown here

  * subitem 2,
    also spans two lines

* item 3</literal_block><paragraph>This list gets typeset as</paragraph><block_quote><bullet_list bullet="*"><list_item><paragraph>item 1</paragraph></list_item><list_item><paragraph>item 2</paragraph><bullet_list bullet="*"><list_item><paragraph>subitem 1, if there are more
lines, each line must
be intended as shown here</paragraph></list_item><list_item><paragraph>subitem 2,
also spans two lines</paragraph></list_item></bullet_list></list_item><list_item><paragraph>item 3</paragraph></list_item></bullet_list></block_quote><paragraph>In an ordered list, each item starts with an <literal>o</literal> (as the first letter
in &quot;ordered&quot;):</paragraph><literal_block xml:space="preserve">o item 1
o item 2
  * subitem 1
  * subitem 2
o item 3</literal_block><paragraph>resulting in</paragraph><block_quote><enumerated_list enumtype="arabic" prefix="" suffix="."><list_item><paragraph>item 1</paragraph></list_item><list_item><paragraph>item 2</paragraph><bullet_list bullet="*"><list_item><paragraph>subitem 1</paragraph></list_item><list_item><paragraph>subitem 2</paragraph></list_item></bullet_list></list_item><list_item><paragraph>item 3</paragraph></list_item></enumerated_list></block_quote><paragraph>Ordered lists cannot have an ordered sublist, i.e., the ordering
applies to the outer list only.</paragraph><paragraph>In a description list, each item is recognized by a dash followed
by a keyword followed by a colon:</paragraph><literal_block xml:space="preserve">- keyword1: explanation of keyword1

- keyword2: explanation
  of keyword2 (remember to indent properly
  if there are multiple
  lines)</literal_block><paragraph>The result becomes</paragraph><block_quote><definition_list><definition_list_item><term>keyword1:</term><definition><paragraph>explanation of keyword1</paragraph></definition></definition_list_item><definition_list_item><term>keyword2:</term><definition><paragraph>explanation
of keyword2 (remember to indent properly
if there are multiple
lines)</paragraph></definition></definition_list_item></definition_list></block_quote></section><section ids="special-lines-1" names="special\ lines\ (1)"><title>Special Lines  (1)</title><paragraph>The Doconce markup language has a concept called <emphasis>special lines</emphasis>.
Such lines starts with a markup at the very beginning of the
line and are used to mark document title, authors, date,
sections, subsections, paragraphs., figures, movies, etc.</paragraph><paragraph><emphasis>Heading with Title and Author(s).</emphasis> Lines starting with <literal>TITLE:</literal>, <literal>AUTHOR:</literal>, and <literal>DATE:</literal> are optional and used
to identify a title of the document, the authors, and the date. The
title is treated as the rest of the line, so is the date, but the
author text consists of the name and associated institution(s) with
the syntax:</paragraph><literal_block xml:space="preserve">name at institution1 and institution2 and institution3</literal_block><paragraph>The <literal>at</literal> with surrounding spaces
is essential for adding information about institution(s)
to the author name, and the <literal>and</literal> with surrounding spaces is
essential as delimiter between different institutions.
An email address can optionally be included, using the syntax:</paragraph><literal_block xml:space="preserve">name Email: somename@site.net at institution1 and institution2</literal_block><paragraph>Multiple authors require multiple <literal>AUTHOR:</literal> lines. All information
associated with <literal>TITLE:</literal> and <literal>AUTHOR:</literal> keywords must appear on a single
line.  Here is an example:</paragraph><literal_block xml:space="preserve">TITLE: On an Ultimate Markup Language
AUTHOR: H. P. Langtangen at Center for Biomedical Computing, Simula Research Laboratory &amp; Dept. of Informatics, Univ. of Oslo
AUTHOR: Kaare Dump Email: dump@cyb.space.com at Segfault, Cyberspace Inc.
AUTHOR: A. Dummy Author
DATE: November 9, 2016</literal_block><paragraph>Note how one can specify a single institution, multiple institutions
(with <literal>&amp;</literal> as separator between institutions),
and no institution. In some formats (including <literal>rst</literal> and <literal>sphinx</literal>)
only the author names appear. Some formats have
&quot;intelligence&quot; in listing authors and institutions, e.g., the plain text
format:</paragraph><literal_block xml:space="preserve">Hans Petter Langtangen [1, 2]
Kaare Dump  (dump@cyb.space.com) [3]
A. Dummy Author

[1] Center for Biomedical Computing, Simula Research Laboratory
[2] Department of Informatics, University of Oslo
[3] Segfault, Cyberspace Inc.</literal_block><paragraph>Similar typesetting is done for LaTeX and HTML formats.</paragraph><paragraph>The current date can be specified as <literal>today</literal>.</paragraph><paragraph><emphasis>Table of Contents.</emphasis> A table of contents can be generated by the line:</paragraph><literal_block xml:space="preserve">TOC: on</literal_block><paragraph>This line is usually placed after the <literal>DATE:</literal> line.
The value <literal>off</literal> turns off the table of contents.</paragraph><paragraph><emphasis>Section Headings.</emphasis> Section headings are recognized by being surrounded by equal signs (=) or
underscores before and after the text of the headline. Different
section levels are recognized by the associated number of underscores
or equal signs (=):</paragraph><block_quote><bullet_list bullet="*"><list_item><paragraph>9 <literal>=</literal> characters for chapters</paragraph></list_item><list_item><paragraph>7 for sections</paragraph></list_item><list_item><paragraph>5 for subsections</paragraph></list_item><list_item><paragraph>3 for subsubsections</paragraph></list_item><list_item><paragraph>2 <emphasis>underscrores</emphasis> (only! - it looks best) for paragraphs
(paragraph heading will be inlined)</paragraph></list_item></bullet_list></block_quote><paragraph>Headings can be surrounded by as many blanks as desired.</paragraph><paragraph>Doconce also supports abstracts. This is typeset as a paragraph, but
<emphasis>must</emphasis> be followed by a section heading (everything up to the first
section heading is taken as part of the text of the abstract).</paragraph><paragraph>Here are some examples:</paragraph><literal_block xml:space="preserve">__Abstract.__ The following text just attempts to exemplify
various section headings.

Appendix is supported too: just let the heading start with &quot;Appendix: &quot;
(this affects only `latex` output, where the appendix formatting
is used - all other formats just leave the heading as it is written).

======= Example on a Section Heading =======

The running text goes here.


===== Example on a Subsection Heading =====

The running text goes here.

=== Example on a Subsubsection Heading ===

The running text goes here.

__A Paragraph.__ The running text goes here.</literal_block></section></section><section ids="special-lines-2" names="special\ lines\ (2)"><title>Special Lines  (2)</title><section ids="figures" names="figures"><title>Figures</title><comment xml:space="preserve">Note: need extra blank after FIGURE and MOVIE in !bc environments</comment><comment xml:space="preserve">because doconce treats !ec as part of the caption and moves the</comment><comment xml:space="preserve">!ec up to the caption line</comment><paragraph>Figures are recognized by the special line syntax:</paragraph><literal_block xml:space="preserve">FIGURE:[filename, height=400 width=600 frac=0.8] caption</literal_block><paragraph>The filename can be without extension, and Doconce will search for an
appropriate file with the right extension. If the extension is wrong,
say <literal>.eps</literal> when requesting an HTML format, Doconce tries to find another
file, and if not, the given file is converted to a proper format
(using ImageMagick's <literal>convert</literal> utility).</paragraph><paragraph>The height, width, and frac keywords can be included
if desired and may have effect for some formats: the height and width
are used for output in the formats <literal>html</literal>, <literal>rst</literal>, <literal>sphinx</literal>, while
the frac specification is used for <literal>latex</literal> and <literal>pdflatex</literal> to specify
the width of the image as a fraction of the text width.</paragraph><paragraph>The figure caption is optional. If omitted, the figure appears &quot;inline&quot;
in the text without any figure environment in LaTeX formats or HTML.
The caption may contain a label for referencing the figure.</paragraph><warning><paragraph>Note the comma
between the filename and the figure size specifications and
that there should be no space around the = sign. This syntax must
be strictly followed.</paragraph><paragraph>Note also that, like for <literal>TITLE:</literal> and <literal>AUTHOR:</literal> lines, all information
related to a figure line <emphasis>must be written on the same line</emphasis>. Introducing
newlines in a long caption will destroy the formatting (only the
part of the caption appearing on the same line as <literal>FIGURE:</literal> will be
included in the formatted caption).</paragraph></warning><target refid="fig-viz"/><figure ids="fig-viz" names="fig:viz"><image uri="figs/streamtubes.png" width="400"/><caption><emphasis>Streamtube visualization of a fluid flow</emphasis>  (fig:viz)</caption></figure><paragraph>Combining several image files into one, in a table fashion, can be done by the
<literal>montage</literal> program from the ImageMagick suite:</paragraph><literal_block xml:space="preserve">montage -background white -geometry 100% -tile 2x \
        file1.png file2.png ... file4.png result.png</literal_block><paragraph>The option <literal>-tile XxY</literal> gives <literal>X</literal> figures in the horizontal direction and
<literal>Y</literal> in the vertical direction (<literal>tile 2x</literal> means two figures per row
and <literal>-tile x2</literal> means two rows).</paragraph></section><section ids="movies" names="movies"><title>Movies</title><paragraph>Here is an example on the <literal>MOVIE:</literal> keyword for embedding movies. This
feature works well for the <literal>latex</literal>, <literal>html</literal>, <literal>rst</literal>, and <literal>sphinx</literal> formats.
Other formats try to generate some HTML file and link to that file
for showing the movie:</paragraph><literal_block xml:space="preserve">MOVIE: [filename, height=xxx width=yyy] possible caption</literal_block><comment xml:space="preserve">latex/PDF format can make use of the movie15 package for displaying movies,</comment><comment xml:space="preserve">or just plain \h`run: ...`_{...}</comment><raw format="html" xml:space="preserve">&lt;embed src=&quot;figs/mjolnir.mpeg&quot; width=600 height=470 autoplay=&quot;false&quot; loop=&quot;true&quot;&gt;&lt;/embed&gt;
&lt;p&gt;&lt;em&gt;&lt;/em&gt;&lt;/p&gt;</raw><comment xml:space="preserve">MOVIE: [figs/wavepacket.gif, width=600 height=470]</comment><comment xml:space="preserve">MOVIE: [figs/wavepacket2.mpeg, width=600 height=470]</comment><paragraph>The LaTeX format results in a file that can either make use of
the movie15 package (requires the PDF to be shown in Acrobat Reader)
or just a plain address to the movie. The HTML, reST, and
Sphinx formats will play
the movie right away by embedding the file in a standard HTML code,
provided the output format is HTML.
For all other formats a URL to an HTML file, which can play the code,
is inserted in the output document.</paragraph><paragraph>When movies are embedded in the PDF file via LaTeX and
the <literal>movie15</literal> package wanted, one has to turn on the preprocessor
variable <literal>MOVIE15</literal>. There is an associated variable
<literal>EXTERNAL_MOVIE_VIEWER</literal> which can be defined to launch an external
viewer when displaying the PDF file (in Acrobat Reader):</paragraph><literal_block xml:space="preserve">Terminal&gt; ptex2tex -DMOVIE15 -DEXTERNAL_MOVIE_VIEWER mydoc</literal_block><paragraph>The HTML, reST, and Sphinx formats can also treat filenames of the form
<literal>myframes*.png</literal>. In that case, an HTML file for showing the sequence of frames
is generated, and a link to this file is inserted in the output document.
That is, a simple &quot;movie viewer&quot; for the frames is made.</paragraph><paragraph>Many publish their scientific movies on YouTube or Vimeo, and Doconce recognizes
YouTube and Vimeo URLs as movies. When the output from Doconce
is an HTML file, the movie will
be embedded, otherwise a URL to the YouTube or Vimeo page is inserted.
You should equip the <literal>MOVIE:</literal> command with the right width and height
of <emphasis>embedded</emphasis> YouTube and Vimeo movies. The recipe goes as follows:</paragraph><enumerated_list enumtype="arabic" prefix="" suffix="."><list_item><paragraph>click on <emphasis>Share</emphasis> (and on YouTube then <emphasis>Embed</emphasis>)</paragraph></list_item><list_item><paragraph>note the height and width of the embedded movie</paragraph></list_item></enumerated_list><paragraph>A typical <literal>MOVIE</literal> command with a YouTube movie is then:</paragraph><literal_block xml:space="preserve">MOVIE: [http://www.youtube.com/watch?v=sI2uCHH3qIM, width=420 height=315]

MOVIE: [http://vimeo.com/55562330, width=500 height=278] CFD.</literal_block><paragraph>Note that there must be a blank line after every <literal>MOVIE:</literal> command.
The width and height parameters are not required, but leaving them out
may lead to movie sizes you do not want.</paragraph><raw format="html" xml:space="preserve">&lt;iframe width=&quot;420&quot; height=&quot;315&quot; src=&quot;http://www.youtube.com/embed/sI2uCHH3qIM&quot; frameborder=&quot;0&quot; allowfullscreen&gt;&lt;/iframe&gt;</raw><raw format="html" xml:space="preserve">&lt;iframe width=&quot;500&quot; height=&quot;278&quot; src=&quot;http://player.vimeo.com/video/55562330&quot; frameborder=&quot;0&quot; allowfullscreen&gt;&lt;/iframe&gt;

&lt;em&gt;CFD.&lt;/em&gt;</raw></section><section ids="copying-computer-code-from-source-files" names="copying\ computer\ code\ from\ source\ files"><title>Copying Computer Code from Source Files</title><paragraph>Another type of special lines starts with <literal>@@@CODE</literal> and enables copying
of computer code from a file directly into a verbatim environment, see
the section <reference name="Blocks of Verbatim Computer Code" refid="blocks-of-verbatim-computer-code">Blocks of Verbatim Computer Code</reference> below.</paragraph><target refid="inline-tagging"/></section><section ids="id4 inline-tagging" names="inline\ tagging inline:tagging"><title>Inline Tagging</title><paragraph>Doconce supports tags for <emphasis>emphasized phrases</emphasis>, <strong>boldface phrases</strong>,
and <literal>verbatim text</literal> (also called type writer text, for inline code),
&lt;font color=&quot;blue&quot;&gt;colored words&lt;/font&gt;,
plus LaTeX/TeX inline mathematics, such as v = sin(x).</paragraph><section ids="emphasized-words" names="emphasized\ words"><title>Emphasized Words</title><paragraph>Emphasized text is typeset inside a pair of asterisk, and there should
be no spaces between an asterisk and the emphasized text, as in:</paragraph><literal_block xml:space="preserve">*emphasized words*</literal_block><paragraph>Boldface font is recognized by an underscore instead of an asterisk:</paragraph><literal_block xml:space="preserve">_several words in boldface_ followed by *ephasized text*.</literal_block><paragraph>The line above gets typeset as
<strong>several words in boldface</strong> followed by <emphasis>ephasized text</emphasis>.</paragraph></section><section ids="inline-verbatim-text" names="inline\ verbatim\ text"><title>Inline Verbatim Text</title><paragraph>Verbatim text, typically used for short inline code,
is typeset between back-ticks:</paragraph><literal_block xml:space="preserve">`call myroutine(a, b)` looks like a Fortran call
while `void myfunc(double *a, double *b)` must be C.</literal_block><paragraph>The typesetting result looks like this:
<literal>call myroutine(a, b)</literal> looks like a Fortran call
while <literal>void myfunc(double *a, double *b)</literal> must be C.</paragraph><paragraph>It is recommended to have inline verbatim text on the same line in
the Doconce file, because some formats (LaTeX and <literal>ptex2tex</literal>) will have
problems with inline verbatim text that is split over two lines.</paragraph><note><paragraph>Watch out for mixing back-ticks and asterisk (i.e., verbatim and
emphasized code): the Doconce interpreter is not very smart so inline
computer code can soon lead to problems in the final format. Go back to the
Doconce source and modify it so the format to which you want to go
becomes correct (sometimes a trial and error process - sticking to
very simple formatting usually avoids such problems).</paragraph></note></section><section ids="links-to-web-addresses" names="links\ to\ web\ addresses"><title>Links to Web Addresses</title><paragraph>Web addresses with links are typeset as:</paragraph><literal_block xml:space="preserve">some URL like &quot;Search Google&quot;: &quot;http://google.com&quot;.</literal_block><paragraph>which appears as some URL like <reference name="Search Google" refuri="http://google.com">Search Google</reference><target ids="search-google" names="search\ google" refuri="http://google.com"/>.
The space after colon is optional, but it is important to enclose the
link and the URL in double quotes.</paragraph><paragraph>To have the URL address itself as link text, put an &quot;URL&quot; or URL
before the address enclosed in double quotes:</paragraph><literal_block xml:space="preserve">Click on this link: URL:&quot;http://code.google.com/p/doconce&quot;.</literal_block><paragraph>which gets rendered as
Click on this link: <reference name="http://code.google.com/p/doconce" refuri="http://code.google.com/p/doconce">http://code.google.com/p/doconce</reference><target dupnames="http://code.google.com/p/doconce" ids="id5" refuri="http://code.google.com/p/doconce"/>.</paragraph><paragraph>(There is also support for lazy writing of URLs: any http or https web address
with a leading space and a trailing space, comma, semi-colon, or question
mark (but not period!) becomes a link with the web address as link text.)</paragraph></section><section ids="mail-addresses" names="mail\ addresses"><title>Mail Addresses</title><paragraph>Links that launches a mail to a specified address is written as
ordinary URLs, typically as:</paragraph><literal_block xml:space="preserve">Send &quot;mail&quot;: &quot;mailto:hpl@simula.no&quot;
# Alternative:
to &quot;`hpl@simula.no`&quot;: &quot;mailto:hpl@simula.no&quot;.</literal_block><paragraph>which appears as Send <reference name="mail" refuri="mailto:hpl@simula.no">mail</reference><target ids="mail" names="mail" refuri="mailto:hpl@simula.no"/> to
<reference name="hpl@simula.no" refuri="mailto:hpl@simula.no">hpl@simula.no</reference><target ids="hpl-simula-no" names="hpl@simula.no" refuri="mailto:hpl@simula.no"/>.</paragraph></section><section ids="links-to-local-files" names="links\ to\ local\ files"><title>Links to Local Files</title><paragraph>Links to files ending in <literal>.txt</literal>, <literal>.html</literal>, <literal>.pdf</literal>, <literal>.py</literal>, <literal>.f</literal>,
<literal>.f77</literal>, <literal>.f90</literal>, <literal>.f95</literal>, <literal>.sh</literal>, <literal>.csh</literal>, <literal>.ksh</literal>, <literal>.zsh</literal>,
<literal>.c</literal>, <literal>.cpp</literal>, <literal>.cxx</literal>, <literal>.pl</literal>, and <literal>.java</literal> follows the same
setup:</paragraph><literal_block xml:space="preserve">see the &quot;Doconce Manual&quot;: &quot;manual.do.txt&quot;.</literal_block><paragraph>which appears as see the <reference name="Doconce Manual" refuri="manual.do.txt">Doconce Manual</reference><target ids="doconce-manual" names="doconce\ manual" refuri="manual.do.txt"/>.
However, linking to local files like this needs caution:</paragraph><block_quote><bullet_list bullet="*"><list_item><paragraph>In the <literal>html</literal> format the links work well if the files are
supplied with the <literal>.html</literal> with the same relative location.</paragraph></list_item><list_item><paragraph>In the <literal>latex</literal> and <literal>pdflatex</literal> formats, such links in PDF files
will unless the <literal>.tex</literal> file has a full URL specified through
a <literal>\hyperbaseurl</literal> command and the linked files are located correctly
relative to this URL. Otherwise full URL must be used in links.</paragraph></list_item><list_item><paragraph>In the <literal>sphinx</literal> format, links to local files do not work unless the
files reside in a <literal>_static</literal> directory (a warning is issued about this).</paragraph></list_item></bullet_list></block_quote><paragraph>As a consequence, we strongly recommend that one copies the relevant
files to a <literal>_static</literal> or <literal>_static-name</literal> directory and makes links to
files in this directory only (<literal>name</literal> is the nickname of the Doconce
document, usually the name of the parent directory or main document).
Other links to files should use the full URL. If Doconce is used
for HTML output only, then plain links to local files work fine.</paragraph><paragraph>If you want a link to a local source code file and have it
viewed in the browser rather than being downloaded, we recommend
to transform the source code file to HTML format by running
<literal>pygmentize</literal>, e.g.:</paragraph><literal_block xml:space="preserve">Terminal&gt; pygmentize -l bash -f html -O full,style=emacs \
          -o _static/make.sh.html subdir/make.sh</literal_block><paragraph>Then you can link to <literal>_static/make.sh.html</literal> instead of
<literal>subdir/make.sh</literal>. Here is an example where the reader
has the file available as <literal>src/myprog.py</literal> in her
software and the document links to <literal>_static/myprog.py</literal>:</paragraph><literal_block xml:space="preserve">See the code URL:&quot;src/myprog.py&quot; (&quot;view: &quot;_static/myprog.py.html&quot;).</literal_block><paragraph>Links to files with other extensions are typeset with
<emphasis>the filename as link text</emphasis>. The syntax consists of
the keyword URL, followed by a colon, and then the filename enclosed
in double quotes:</paragraph><literal_block xml:space="preserve">URL: &quot;manual.html&quot;</literal_block><paragraph>resulting in the link <reference name="manual.html" refuri="manual.html">manual.html</reference><target ids="manual-html" names="manual.html" refuri="manual.html"/>.</paragraph><comment xml:space="preserve">This is now automatically carried out by the autogenerated</comment><comment xml:space="preserve">script for sphinx:</comment><comment xml:space="preserve">For such local links to</comment><comment xml:space="preserve">work with the ``sphinx`` format, the ``.rst`` file needs a fix, carried</comment><comment xml:space="preserve">out by</comment><comment xml:space="preserve">!bc sys</comment><comment xml:space="preserve">doconce sphinxfix_localURLs mydoc.rst</comment><comment xml:space="preserve">!ec</comment><comment xml:space="preserve">(The files, such as ``manual.html``, are then copied to a subdirectory</comment><comment xml:space="preserve">``_static``, which must be manually copied to the Sphinx directory's</comment><comment xml:space="preserve">``_static`` directory - links in the ``.rst`` files are automatically</comment><comment xml:space="preserve">adjusted.)</comment></section><section ids="inline-comments" names="inline\ comments"><title>Inline Comments</title><paragraph>Doconce also supports inline comments in the text:</paragraph><literal_block xml:space="preserve">[name: comment]</literal_block><paragraph>where <literal>name</literal> is the name of the author of the command, and <literal>comment</literal> is a
plain text text. Note that there must be a space after the colon,
otherwise the comment is not recognized. (<strong>hpl 1</strong>: Inline comments
can span
several lines,
if desired.)</paragraph><paragraph>The name and comment are visible in the output unless <literal>doconce format</literal>
is run with a command-line argument <literal>--skip_inline_comments</literal>
(see the section <reference name="From Doconce to Other Formats" refid="from-doconce-to-other-formats">From Doconce to Other Formats</reference> for an example). Inline comments
are helpful during development of a document since different authors
and readers can comment on formulations, missing points, etc.
All such comments can easily be removed from the <literal>.do.txt</literal> file
(see the section <reference name="From Doconce to Other Formats" refid="from-doconce-to-other-formats">From Doconce to Other Formats</reference>).</paragraph><paragraph>Inline comments are typeset in a simple way (boldface name and the
comment in parenthesis), but in LaTeX very visible color boxes
are used (via the <literal>todonotes</literal> package).</paragraph></section><section ids="inline-mathematics" names="inline\ mathematics"><title>Inline Mathematics</title><paragraph>Inline mathematics is written as in LaTeX, i.e., inside dollar signs.
Many formats leave this syntax as it is (including to dollar signs),
hence nice math formatting is only obtained in LaTeX, HTML, MediaWiki,
and Sphinx (Epytext has some inline math support that is utilized).
However, mathematical expressions in LaTeX syntax often contains
special formatting commands, which may appear annoying in plain
text. Doconce therefore supports an extended inline math syntax where
the writer can provide an alternative syntax suited for formats close
to plain ASCII:</paragraph><literal_block xml:space="preserve">Here is an example on a linear system
${\bf A}{\bf x} = {\bf b}$|$Ax=b$,
where $\bf A$|$A$ is an $n\times n$|$nxn$ matrix, and
$\bf x$|$x$ and $\bf b$|$b$ are vectors of length $n$|$n$.</literal_block><paragraph>That is, we provide two alternative expressions, both enclosed in
dollar signs and separated by a pipe symbol, the expression to the
left is used in formats with LaTeX support (<literal>latex</literal>, <literal>pdflatex</literal>, <literal>html</literal>,
<literal>sphinx</literal>, <literal>mwiki</literal>), while the expression to the right is used for
all other formats.  The above text is typeset as &quot;Here is an example
on a linear system Ax=b, where A
is an nxn matrix, and x and b
are vectors of length n.&quot;</paragraph></section></section><section ids="comments" names="comments"><title>Comments</title><paragraph>Comments intended to be (sometimes) visible in the output document and
read by readers are known as <emphasis>inline comments</emphasis> in Doconce and
described in the section <reference name="Inline Tagging" refid="id4">Inline Tagging</reference>.</paragraph><paragraph>Here we address comments in the Doconce source file that are not
intended to be visible in the output document. Basic comment
lines start with the hash <literal>#</literal>:</paragraph><literal_block xml:space="preserve">#
# Here are some comment lines that do not affect any formatting.
# These lines are converted to comments in the output format.
#</literal_block><paragraph>Such comment lines may have some side effects in the <literal>rst</literal> and <literal>sphinx</literal>
formats because following lines are taken as part of the comment if
there is not a blank line after the comment.</paragraph><paragraph>The Mako preprocessor supports comments that are filtered out <emphasis>before</emphasis>
Doconce starts translating the document. Such comments are very valuable
as they will never interfere with the output format and they are only
present in the Doconce source. Mako has two types of comments:
lines starting with a double hash <literal>##</literal> and lines enclosed by
the <literal>&lt;%doc&gt;</literal> (beginning) and <literal>&lt;%doc/&gt;</literal> (closing) tags.</paragraph><paragraph>If you need a lot of comments in the Doconce file, consider using
Mako comments instead of the single hash, unless you want the
comments to be in the source code of the output document.</paragraph><paragraph>To comment out or remove large sections, consider using the Preprocess
preprocessor and an if-else block with a variable that is undefined
(typically something like a test <literal># #ifdef EXTRA</literal> in Preprocess).</paragraph></section><section ids="cross-referencing" names="cross-referencing"><title>Cross-Referencing</title><paragraph>References and labels are supported. The syntax is simple:</paragraph><literal_block xml:space="preserve">label{section:verbatim}   # defines a label
For more information we refer to Section ref{section:verbatim}.</literal_block><paragraph>This syntax is close that that of labels and cross-references in
LaTeX. When the label is placed after a section or subsection heading,
the plain text, Epytext, and StructuredText formats will simply
replace the reference by the title of the (sub)section.  All labels
will become invisible, except those in math environments.  In the
<literal>rst</literal> and <literal>sphinx</literal> formats, the end effect is the same, but
the &quot;label&quot; and &quot;ref&quot; commands are first translated to the proper
reST commands by <literal>doconce format</literal>. In the HTML and (Google
Code) wiki formats, labels become anchors and references become links,
and with LaTeX &quot;label&quot; and &quot;ref&quot; are just equipped with backslashes so
these commands work as usual in LaTeX.</paragraph><paragraph>It is, in general, recommended to use labels and references for
(sub)sections, equations, and figures only.
By the way, here is an example on referencing Figure <reference name="fig:viz" refid="fig-viz">fig:viz</reference>
(the label appears in the figure caption in the source code of this document).
Additional references to the sections <reference name="LaTeX Blocks of Mathematical Text" refid="latex-blocks-of-mathematical-text">LaTeX Blocks of Mathematical Text</reference> and <reference name="Macros (Newcommands)" refid="macros-newcommands">Macros (Newcommands)</reference> are
nice to demonstrate, as well as a reference to equations,
say Equations (myeq1)-(myeq2). A comparison of the output and
the source of this document illustrates how labels and references
are handled by the format in question.</paragraph><paragraph>Hyperlinks to files or web addresses are handled as explained
in the section <reference name="Inline Tagging" refid="id4">Inline Tagging</reference>.</paragraph><target refid="manual-genrefs"/></section><section ids="generalized-cross-referencing manual-genrefs" names="generalized\ cross-referencing manual:genrefs"><title>Generalized Cross-Referencing</title><paragraph>Sometimes a series of individual documents may be assembled to one
large document. The assembly impacts how references to sections
are written: when referring to a section in the same document, a label
can be used, while references to sections in other documents are
written differently, sometimes involving a link (URL) and a citation.
Especially if both the individual documents and the large assembly document
are to exist side by side, a flexible way of referencing is needed.
For this purpose, Doconce offers <emphasis>generalized references</emphasis> which allows
a reference to have two different formulations, one for internal
references and one for external references. Since LaTeX supports
references to labels in external documents via the <literal>xr</literal> package,
the generalized references in Doconce has a syntax that may utilize
the <literal>xr</literal> feature in LaTeX.</paragraph><paragraph>The syntax of generalized references reads:</paragraph><literal_block xml:space="preserve">ref[internal][cite][external]</literal_block><paragraph>If all <literal>label`_` references in the text ``internal</literal> are references
to labels in the present document, the above <literal>ref</literal> command is replaced
by the text <literal>internal</literal>. Otherwise, if cite is non-empty and the format
is <literal>latex</literal> or <literal>pdflatex</literal> one assumes that the references in <literal>internal</literal>
are to external documents declared by a comment line <literal>#
Externaldocuments: testdoc, mydoc</literal> (usually after the title, authors,
and date). In this case the output text is <literal>internal cite</literal> and the
LaTeX package <literal>xr</literal> is used to handle the labels in the external documents.
If none of the two situations above applies, the <literal>external</literal>
text will be the output.</paragraph><paragraph>Here is an example on a specific generalized reference:</paragraph><literal_block xml:space="preserve">As explained in
ref[Section ref{subsec:ex}][in &quot;Langtangen, 2012&quot;:
&quot;http://code.google.com/p/doconce/wiki/Description&quot;
cite{testdoc:12}][a &quot;section&quot;: &quot;testdoc.html#___sec2&quot; in
the document &quot;A Document for Testing Doconce&quot;: &quot;testdoc.html&quot;
cite{testdoc:12}], Doconce documents may include movies.</literal_block><paragraph>In LaTeX, this becomes:</paragraph><literal_block xml:space="preserve">As explained in
Section~\ref{subsec:ex} in
\href{{http://code.google.com/p/doconce/source/browse/test/testdoc.do.txt}}{Langtangen, 2012}
\cite{testdoc:12}, Doconce documents may include movies.</literal_block><paragraph>Note that there is a specific numbered reference to an external
document, if <literal>subsec:ex</literal> is not a label in the present document,
and that we add a citation in the usual way, but also include
a link to the document using the name of the other or some other
relevant link text. The link can be the same or different from
links used in the &quot;external&quot; part of the reference (LaTeX cannot
have links to local files, so a complete URL must be used).</paragraph><paragraph>Translation to Sphinx or reStructuredText results in:</paragraph><literal_block xml:space="preserve">As explained in
a `section &lt;testdoc.html#___sec2&gt;`_ in
the document `A Document for Testing Doconce &lt;testdoc.html&gt;`_
[testdoc:12]_, Doconce documents may include movies.</literal_block><paragraph>In plain HTML, this becomes:</paragraph><literal_block xml:space="preserve">As explained in
a &lt;a href=&quot;testdoc.html#___sec2&quot;&gt;section&lt;/a&gt; in
the document &lt;a href=&quot;testdoc.html&quot;&gt;A Document for Testing Doconce&lt;/a&gt;
&lt;a href=&quot;#testdoc:12&quot;&gt;[1]&lt;/a&gt;, Doconce documents may include movies.</literal_block><paragraph>The plain text format reads:</paragraph><literal_block xml:space="preserve">As explained in
a section (testdoc.html#___sec2) in
the document A Document for Testing Doconce (testdoc.html)
[1], Doconce documents may include movies.</literal_block><paragraph>And in Pandoc-exteded Markdown we have:</paragraph><literal_block xml:space="preserve">As explained in
a [section](testdoc.html#___sec2) in
the document [A Document for Testing Doconce](testdoc.html)
@testdoc:12, Doconce documents may include movies.</literal_block></section><section ids="index" names="index"><title>Index</title><paragraph>An index can be created for the <literal>latex</literal>, <literal>rst</literal>, and <literal>sphinx</literal> formats
by the <literal>idx</literal> keyword, following a LaTeX-inspired syntax:</paragraph><literal_block xml:space="preserve">idx{some index entry}
idx{main entry!subentry}
idx{`verbatim_text` and more}</literal_block><paragraph>The exclamation mark divides a main entry and a subentry. Backquotes
surround verbatim text, which is correctly transformed in a LaTeX setting to:</paragraph><literal_block xml:space="preserve">\index{verbatim\_text@\texttt{\rm\smaller verbatim\_text and more}}</literal_block><paragraph>Everything related to the index simply becomes invisible in plain
text, Epytext, StructuredText, HTML, and wiki formats.  Note: <literal>idx</literal>
commands should be inserted outside paragraphs, not in between the
text as this may cause some strange behaviour of reST and
Sphinx formatting.  As a recommended rule, index items are naturally
placed right after section headings, before the text begins, while
index items related to a paragraph should be placed above the
paragraph one a separate line (and not in between the text or between
the paragraph heading and the text body, although this works fine if
LaTeX is the output format). For paragraphs with <literal>===</literal> heading,
the index keywords should be placed above the heading.</paragraph><paragraph>The keywords in the index are automatically placed in a meta
tag in <literal>html</literal> output such that search engines can make use of the them.</paragraph></section><section ids="bibliography-2" names="bibliography\ (2)"><title>Bibliography  (2)</title><paragraph>Doconce applies the software tool <reference name="Publish" refuri="https://bitbucket.org/logg/publish">Publish</reference><target dupnames="publish" ids="id6" refuri="https://bitbucket.org/logg/publish"/> to handle the bibliography in a
document. With Publish it is easy to import BibTeX data and maintain a
database in a clean, self-explanatory textual format. From the Publish
format it is easy to go BibTeX and reST or straightforward Doconce
typesetting (and from there to HTML, plain text, wiki formats, and so
on).</paragraph><paragraph>Installing Publish is straightforward: just checkout the code on
<reference name="bitbucket.org" refuri="https://bitbucket.org/logg/publish">bitbucket.org</reference><target ids="bitbucket-org" names="bitbucket.org" refuri="https://bitbucket.org/logg/publish"/>, move to the
<literal>publish</literal> directory and run <literal>sudo python setup.py install</literal>.</paragraph><section ids="importing-your-data-to-the-publish-database" names="importing\ your\ data\ to\ the\ publish\ database"><title>Importing your data to the Publish database</title><paragraph>Many scientists have their bibliographic data in the BibTex format. Here we
assume that you have two files, <literal>refs1.bib</literal> and <literal>refs2.bib</literal>. These can
be imported to a Publish database, residing in the file <literal>papers.pub</literal>,
by the commands:</paragraph><literal_block xml:space="preserve">publish import refs1.bib
publish import refs2.bib</literal_block><paragraph>During import, Publish may ask you for accepting the name of new
institutions or journals. Publish already have a database of journals
and institutions/departments, but when you add new, you also get
a file <literal>venues.list</literal> (in the current working directory) which will be used
for future imports in this directory. Make sure you store <literal>publish.pub</literal>
and <literal>venues.list</literal> along with your Doconce document files (e.g., add them to
your version control system).</paragraph></section><section ids="requirements-to-input-data" names="requirements\ to\ input\ data"><title>Requirements to input data</title><note><paragraph>Note that Publish only accepts BibTeX files where the keys (author,
title, etc.) are in lower case and where the data are enclosed in
curly braces. You may need to edit your BibTeX files to meet this
demand.</paragraph></note><paragraph>The utility <literal>doconce fix_bibtex4publish file.bib</literal> fixes several known
issues with BibTeX files such that Publish has a better chance of
accepting the entries. Run this utility first, then run Publish,
respond to any requirements that Publish spits out, remove <literal>papers.pub</literal>
if it exists, and run the import statements again.</paragraph><paragraph>Although references are visible as numbers only in the
output, it is recommended to have apply a nice, consistent
typesetting of your keys. It is suggested to use the following scheme:</paragraph><literal_block xml:space="preserve">Langtangen_2003a          # single author
Langtangen_Pedersen_2002  # two authors
Langtangen_et_al_2002     # three or more authors</literal_block><paragraph>One can add a, b, c, and so forth if several keys feature the same
authors and year.</paragraph></section><section ids="adding-new-references-to-the-database" names="adding\ new\ references\ to\ the\ database"><title>Adding new references to the database</title><paragraph>When you get some new BibTeX references you simply put them in
a file, say <literal>refs3.pub</literal> and run the <literal>publish import refs3.pub</literal> command
to update the database. You may also consider editing the <literal>papers.pub</literal>
file directly when adding new references.</paragraph></section><section ids="exporting-the-database" names="exporting\ the\ database"><title>Exporting the database</title><paragraph>Export of everything in the database to
BibTeX is done by:</paragraph><literal_block xml:space="preserve">publish export mybibtexfile.bib</literal_block><paragraph>You can easily export subsets of the database, e.g., only papers associated
with a particular author (the Publish manual has details on how this is
done). Doconce will automatically export the database to BibTeX if
the output format is <literal>latex</literal> or <literal>pdflatex</literal>.</paragraph></section><section ids="referring-to-publications" names="referring\ to\ publications"><title>Referring to publications</title><paragraph>We use the command:</paragraph><literal_block xml:space="preserve">cite{key}</literal_block><paragraph>to refer to a publication with bibliographic key <literal>key</literal>.
Here is an example: <citation_reference ids="id7" refid="ref1">Ref1</citation_reference> discussed propagation of
large destructive water waves, <citation_reference ids="id8" refid="ref2">Ref2</citation_reference> gave
an overview of numerical methods for solvin the Navier-Stokes equations,
while the use of Backward Kolmogorov equations for analyzing
random vibrations was investigated in <citation_reference ids="id9" refid="ref3">Ref3</citation_reference>.
The book chapter <citation_reference ids="id10" refid="ref4">Ref4</citation_reference> contains information on
C++ software tools for programming multigrid methods. A real retro
reference is <citation_reference ids="id11" refid="ref5">Ref5</citation_reference> about a big FORTRAN package.
Multiple references are also possible, e.g., see
<citation_reference ids="id12" refid="ref1">Ref1</citation_reference> <citation_reference ids="id13" refid="ref4">Ref4</citation_reference>.</paragraph><paragraph>In LaTeX, the <literal>cite</literal> command is directly translated to the
corresponding LaTeX version of the command with a backslash; in reST
and Sphinx the citations becomes links, with the citation keys as
names; in HTML the citations are numbered from 1, 2, and so forth
according to their appearance, and the numbers appear as links; while
in other formats the citations are simply the keys inside square
brackets and the corresponding references are listed in the order they
are cited.</paragraph></section><section ids="specifying-the-publish-database" names="specifying\ the\ publish\ database"><title>Specifying the Publish database</title><paragraph>The specification of the Publish database file in the Doconce document
is done one a line containing <literal>BIBFILE: papers.pub</literal> (you may give
the database file another name and store it in another directory).
The references will be inserted at the place where this command appears.
Before the command you will often want to have a headline with
&quot;References&quot;, &quot;Bibliography&quot;, or similar.
Here is an example:</paragraph><literal_block xml:space="preserve">======= References =======

BIBFILE: papers.pub</literal_block><paragraph>In LaTeX and pdfLaTeX the <literal>papers.pub</literal> file is exported to BibTeX format
and included in the document, while in all other formats, suitable
text is produced from the database.</paragraph></section><section ids="latex-bibliography-style" names="latex\ bibliography\ style"><title>LaTeX bibliography style</title><paragraph>The bibliography style is &quot;plain&quot; in LaTeX output. To change this, just
edit the <literal>.p.tex</literal> file. For example:</paragraph><literal_block xml:space="preserve">doconce format latex mydoc
doconce replace 'bibliographystyle{plain}' 'bibliographystyle{abbrev}' mydoc.p.tex</literal_block></section></section><section ids="tables" names="tables"><title>Tables</title><paragraph>A table like</paragraph><table><tgroup cols="3"><colspec colwidth="12"/><colspec colwidth="12"/><colspec colwidth="12"/><thead><row><entry><paragraph>time</paragraph></entry><entry><paragraph>velocity</paragraph></entry><entry><paragraph>acceleration</paragraph></entry></row></thead><tbody><row><entry><paragraph>0.0</paragraph></entry><entry><paragraph>1.4186</paragraph></entry><entry><paragraph>-5.01</paragraph></entry></row><row><entry><paragraph>2.0</paragraph></entry><entry><paragraph>1.376512</paragraph></entry><entry><paragraph>11.919</paragraph></entry></row><row><entry><paragraph>4.0</paragraph></entry><entry><paragraph>1.1E+1</paragraph></entry><entry><paragraph>14.717624</paragraph></entry></row></tbody></tgroup></table><paragraph>is built up of pipe symbols and dashes:</paragraph><literal_block xml:space="preserve">|--------------------------------|
|time  | velocity | acceleration |
|--------------------------------|
| 0.0  | 1.4186   | -5.01        |
| 2.0  | 1.376512 | 11.919       |
| 4.0  | 1.1E+1   | 14.717624    |
|--------------------------------|</literal_block><paragraph>The pipes and column values do not need to be aligned (but why write
the Doconce source in an ugly way?). In the line below the heading,
one can insert the characters <literal>c</literal>, <literal>r</literal>, or <literal>l</literal> to specify the
alignment of the columns (centered, right, or left, respectively).
Similar character can be inserted in the line above the header to
algn the headings. Pipes <literal>|</literal> can also be inserted to indicate
vertical rules in LaTeX tables (they are ignored for other formats).
An example of centered headings (which is default anyway), first
column left-adjusted and the others right-adjusted looks like:</paragraph><literal_block xml:space="preserve">|--c--------c-----------c--------|
|time  | velocity | acceleration |
|--l--------r-----------r--------|
| 0.0  | 1.4186   | -5.01        |
| 2.0  | 1.376512 | 11.919       |
| 4.0  | 1.1E+1   | 14.717624    |
|--------------------------------|</literal_block><paragraph>Note that not all formats offer alignment of heading or entries
in tables (<literal>rst</literal> and <literal>sphinx</literal> are examples). Also note that
Doconce tables are very simple: neither entries nor
headings can span several columns or rows. When that functionality
is needed, one can make use of the preprocessor and if-tests on
the format and insert format-specific code for tables.</paragraph><paragraph>The command-line option <literal>--tables2csv</literal> (to <literal>doconce format</literal>)
makes Doconce dump each table to CSV format in a file <literal>table_X.csv</literal>,
where <literal>X</literal> is the table number. This feature makes it easy to
load tables into spreadsheet programs for further analysis.</paragraph><paragraph>Data in CSV format can be transformed to Doconce table format
by the <literal>doconce csv2table</literal> utility:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce csv2table somefile.csv &gt; table.do.txt</literal_block><paragraph>This is a quick way of writing tables. For example, we can
write a text file <literal>tmp.csv</literal> with:</paragraph><literal_block xml:space="preserve">time, velocity, acceleration
0.0, 1.4186, -5.01
2.0, 1.376512, 11.919
4.0, 1.1E+1, 14.717624</literal_block><paragraph>Running <literal>doconce csv2table tmp.csv</literal> creates the table:</paragraph><literal_block xml:space="preserve">|------c--------------c--------------c-------|
| time         | velocity     | acceleration |
|------c--------------c--------------c-------|
| 0.0          | 1.4186       | -5.01        |
| 2.0          | 1.376512     | 11.919       |
| 4.0          | 1.1E+1       | 14.717624    |
|--------------------------------------------|</literal_block></section><section ids="exercises-problems-projects-and-examples" names="exercises,\ problems,\ projects,\ and\ examples"><title>Exercises, Problems, Projects, and Examples</title><paragraph>Doconce has special support for four types of &quot;exercises&quot;, named
<emphasis>exercise</emphasis>, <emphasis>problem</emphasis>, <emphasis>project</emphasis>, or <emphasis>example</emphasis>.
These are all typeset as special kind of
sections. Such sections start with a subsection
headline, 5 <literal>=</literal> characters, and last up to the
next headline or the end of the file. The headline itself must
consists of the word &quot;Exercise&quot;, &quot;Problem&quot;, &quot;Project&quot;, or &quot;Example&quot;, followed
by a colon and a title of the exercise, problem, or project.
The next line(s) may contain a label and specification of the
name of result file (if the answer to the exercise is to be handed
in) and a solution file. The Doconce code looks like this:</paragraph><literal_block xml:space="preserve">===== Project: Determine the Distance to the Moon =====
label{proj:moondist}
file=earth2moon.pdf
solution=eart2moon_sol.do.txt

Here goes the running text of the project....</literal_block><paragraph>Doconce will recognize the exercise, problem, project, or example <emphasis>title</emphasis>,
the <emphasis>label</emphasis>, the <emphasis>result file</emphasis>, the <emphasis>solution</emphasis> (if any of
these three entities is present), and the <emphasis>running text</emphasis>. In addition,
one can add subexercise environments, starting with <literal>!bsubex</literal> and ending
with <literal>!esubex</literal>, on the beginning of separate lines.
Within the main exercise or
a subexercise, three other environments are possible: (full) solution,
(short) <emphasis>answer</emphasis>, and <emphasis>hints</emphasis>. The environments have begin-end
directives <literal>!bans</literal>, <literal>!eans</literal>, <literal>!bsol</literal>, <literal>!esol</literal>, <literal>!bhint</literal>, <literal>!ehint</literal>, which
all must appear on the beginning of a separate line (just as
<literal>!bc</literal> and <literal>!ec</literal>).</paragraph><paragraph>The solution environment allows inline
solution as an alternative to the <literal>solution=...</literal> directive mentioned above,
which requires that the solution is in a separate file. Comment lines
are inserted so that the beginning and end of answers and solutions can
be identified and removed if desired.</paragraph><paragraph>A full exercise set-up can be sketched as follows:</paragraph><literal_block xml:space="preserve">===== Exercise: Determine the Distance to the Moon =====
label{exer:moondist}
file=earth2moon.pdf

Here goes main body of text describing the exercise...

!bsubex
Subexercises are numbered a), b), etc.

!bans
Short answer to subexercise a).
!eans

!bhint
First hint to subexercise a).
!ehint

!bhint
Second hint to subexercise a).
!ehint
!esubex

!bsubex
Here goes the text for subexercise b).

!bhint
A hint for this subexercise.
!ehint

!bsol
Here goes the solution of this subexercise.
!esol
!esubex

!bremarks
At the very end of the exercise it may be appropriate to summarize
and give some perspectives. The text inside the !bremarks-!eremarks
directives is always typeset at the end of the exercise.
!eremarks

!bsol
Here goes a full solution of the whole exercise.
!esol
!ec

A recommended rule for using the different &quot;exercise&quot; types goes as follows:

  * Exercises are smaller problems directly related to the present chapter
    (e.g., with references to the text).
  * Problems are sufficiently independent of the chapter's text
    that they make sense on their own, separated from the rest of the docoment.
  * Projects are larger problems that also make sense on their own.
  * Examples are exercises, problems, or projects with full solutions.

The command line options `--without_answers` and `--without_solutions`
turn off output of answers and solutions, respectively, except for
examples.

Sometimes one does not want the heading of an exercise, problem, project,
or example to contain the keyword `Exercise:`, `Problem:`, `Project:`,
or `Example:`. By enclosing the keyword in braces, as in

!bc
===== {Problem}: Find a solution to a problem =====</literal_block><paragraph>the keyword is marked for being left out of the heading, resulting in
the heading &quot;Find a solution to a problem&quot;.</paragraph><paragraph>The various elements of exercises are collected in a special data
structure (list of dictionaries) stored in a file <literal>.mydoc.exerinfo</literal>,
if <literal>mydoc.do.txt</literal> is the name of the Doconce file.  The file contains
a list of dictionaries, where keys in the dictionary corresponds to
elements in the exercise: filename, solution file, answer, label, list
of hints, list of subexercises, closing remarks, and the main body of
text. From this data structure it is easy to generate stand-alone
documents with exercises, problems, and projects with or without
short answers and full solutions.</paragraph><paragraph>Tailored formatting of exercises in special output formats can make
use of the elements in an exercise.  For example, one can image web
formats where the hints are displayed one by one when needed and where
the result file can be uploaded. One can also think of mechanisms for
downloading the solution file if the result file meets certain
criteria.  Doconce does not yet generate such functionality in any
output format, but this is an intended future feature to be
impelemented.</paragraph><paragraph>For now, exercises, problems, projects, examples are typeset as ordinary
Doconce sections (this is the most general approach that will work for many
formats). One must therefore refer to an exercise, problem, project, or
example by its label, which normally will translate to the section number
(in LaTeX, for instance) or a link to the title of the section.
The <emphasis>title</emphasis> is typeset without any leading &quot;Exercise:&quot;, &quot;Problem:&quot;,
or &quot;Project:&quot; word, so that references like:</paragraph><literal_block xml:space="preserve">see Problem ref{...}</literal_block><paragraph>works well in all formats (i.e., no double &quot;Problem Problem&quot; appears).</paragraph><paragraph><emphasis>Remark.</emphasis> Examples are <emphasis>not</emphasis> typeset similarly to exercises unless one adds
the command-line option <literal>--examples_as_exercises</literal>. That is, without
this option, any heading and starting with <literal>Example:</literal> makes Doconce
treat the forthcoming text as ordinary text without any interpretation
of exercise-style instructions.
With the command-line option <literal>--examples_as_exercises</literal>,
one can use the <literal>!bsubex</literal> and <literal>!bsol</literal>
commands to indicate a subproblem and a solution. In this way, the
typesetting of the example looks like an exercise equipped with a solution.</paragraph><target refid="sec-verbatim-blocks"/></section><section ids="blocks-of-verbatim-computer-code sec-verbatim-blocks" names="blocks\ of\ verbatim\ computer\ code sec:verbatim:blocks"><title>Blocks of Verbatim Computer Code</title><paragraph>Blocks of computer code, to be typeset verbatim, must appear inside a
&quot;begin code&quot; <literal>!bc</literal> keyword and an &quot;end code&quot; <literal>!ec</literal> keyword. Both
keywords must be on a single line and <emphasis>start at the beginning of the
line</emphasis>.  Before such a code block there must be a plain sentence
(at least if successful transformation to reST and
ASCII-type formats is desired). For example, a code block cannot come
directly after a section/paragraph heading or a table.</paragraph><paragraph>Here is a plain code block:</paragraph><literal_block xml:space="preserve">!bc
% Could be a comment line in some file
% And some data
1.003 1.025
2.204 1.730
3.001 1.198
!ec</literal_block><paragraph>which gets rendered as:</paragraph><literal_block xml:space="preserve">% Could be a comment line in some file
% And some data
1.003 1.025
2.204 1.730
3.001 1.198</literal_block><paragraph>There may be an argument after the <literal>!bc</literal> tag to specify a certain
environment (for <literal>ptex2tex</literal>, <literal>doconce ptex2tex</literal>, or Sphinx) for
typesetting the verbatim code. For instance, <literal>!bc dat</literal> corresponds to
the data file environment and <literal>!bc cod</literal> is typically used for a code
snippet. There are some predefined environments explained below. If
there is no argument specifying the environment, one assumes some
plain verbatim typesetting (for <literal>ptex2tex</literal> this means the <literal>ccq</literal>
environment, which is defined in the config file <literal>.ptex2tex.cfg</literal>,
while for Sphinx it defaults to the <literal>python</literal> environment).</paragraph><paragraph>Since the config file for <literal>ptex2tex</literal> and command-line arguments for
the alternative <literal>doconce ptex2tex</literal> program can define what some environments
map onto with respect to typesetting, a similar possibility is
supported for Sphinx as well.  The argument after <literal>!bc</literal> is in case of
Sphinx output mapped onto a valid Pygments language for typesetting of
the verbatim block by Pygments. This mapping takes place in an
optional comment to be inserted in the Doconce source file, e.g.:</paragraph><literal_block xml:space="preserve"># sphinx code-blocks: pycod=python cod=fortran cppcod=c++ sys=console</literal_block><paragraph>Here, three arguments are defined: <literal>pycod</literal> for Python code,
<literal>cod</literal> also for Python code, <literal>cppcod</literal> for C++ code, and <literal>sys</literal>
for terminal sessions. The same arguments would be defined
in <literal>.ptex2tex.cfg</literal> for how to typeset the blocks in LaTeX using
various verbatim styles (Pygments can also be used in a LaTeX
context).</paragraph><paragraph>By default, <literal>pro</literal> is used for complete programs in Python, <literal>cod</literal> is
for a code snippet in Python, while <literal>xcod</literal> and <literal>xpro</literal> implies computer
language specific typesetting where <literal>x</literal> can be <literal>f</literal> for Fortran, <literal>c</literal>
for C, <literal>cpp</literal> for C++, <literal>sh</literal> for Unix shells, <literal>pl</literal> for Perl, <literal>m</literal> for
Matlab, <literal>cy</literal> for Cython, and <literal>py</literal> for Python.  The argument <literal>sys</literal>
means by default <literal>console</literal> for Sphinx and <literal>CodeTerminal</literal> (ptex2tex
environent) for LaTeX. Other specifications are <literal>dat</literal> for a data file
or print out, and <literal>ipy</literal> for interactive Python sessions (the latter
does not introduce any environment  in <literal>sphinx</literal> output, as interactive
sessions are automatically recognized and handled).  All these
definitions of the arguments after <literal>!bc</literal> can be redefined in the
<literal>.ptex2tex.cfg</literal> configuration file for ptex2tex/LaTeX and in the
<literal>sphinx code-blocks</literal> comments for Sphinx. Support for other languages
is easily added.</paragraph><comment xml:space="preserve">(Any sphinx code-block comment, whether inside verbatim code</comment><comment xml:space="preserve">blocks or outside, yields a mapping between bc arguments</comment><comment xml:space="preserve">and computer languages. In case of muliple definitions, the</comment><comment xml:space="preserve">first one is used.)</comment><paragraph>The enclosing <literal>!ec</literal> tag of verbatim computer code blocks must
be followed by a newline.  A common error in list environments is to
forget to indent the plain text surrounding the code blocks. In
general, we recommend to use paragraph headings instead of list items
in combination with code blocks (it usually looks better, and some
common errors are naturally avoided).</paragraph><paragraph>Here is a verbatim code block with Python code (<literal>pycod</literal> style):</paragraph><literal_block xml:space="preserve">!bc pycod
# regular expressions for inline tags:
inline_tag_begin = r'(?P&lt;begin&gt;(^|\s+))'
inline_tag_end = r'(?P&lt;end&gt;[.,?!;:)\s])'
INLINE_TAGS = {
    'emphasize':
    r'%s\*(?P&lt;subst&gt;[^ `][^*`]*)\*%s' % \
    (inline_tag_begin, inline_tag_end),
    'verbatim':
    r'%s`(?P&lt;subst&gt;[^ ][^`]*)`%s' % \
    (inline_tag_begin, inline_tag_end),
    'bold':
    r'%s_(?P&lt;subst&gt;[^ `][^_`]*)_%s' % \
    (inline_tag_begin, inline_tag_end),
}
!ec</literal_block><paragraph>The typeset result of this block becomes:</paragraph><literal_block xml:space="preserve"># regular expressions for inline tags:
inline_tag_begin = r'(?P&lt;begin&gt;(^|\s+))'
inline_tag_end = r'(?P&lt;end&gt;[.,?!;:)\s])'
INLINE_TAGS = {
    'emphasize':
    r'%s\*(?P&lt;subst&gt;[^ `][^*`]*)\*%s' % \
    (inline_tag_begin, inline_tag_end),
    'verbatim':
    r'%s`(?P&lt;subst&gt;[^ ][^`]*)`%s' % \
    (inline_tag_begin, inline_tag_end),
    'bold':
    r'%s_(?P&lt;subst&gt;[^ `][^_`]*)_%s' % \
    (inline_tag_begin, inline_tag_end),
}</literal_block><paragraph>And here is a C++ code snippet (<literal>cppcod</literal> style):</paragraph><literal_block xml:space="preserve">void myfunc(double* x, const double&amp; myarr) {
    for (int i = 1; i &lt; myarr.size(); i++) {
        myarr[i] = myarr[i] - x[i]*myarr[i-1]
    }
}</literal_block><comment xml:space="preserve">When showing copy from file in !bc envir, intent a character - otherwise</comment><comment xml:space="preserve">ptex2tex is confused and starts copying. However, here (in make.sh) we use</comment><comment xml:space="preserve">doconce ptex2tex which does not have this problem.</comment><paragraph>Computer code can be copied directly from a file, if desired. The syntax
is then:</paragraph><literal_block xml:space="preserve">@@@CODE myfile.f
@@@CODE myfile.f fromto: subroutine\s+test@^C\s{5}END1</literal_block><paragraph>The first line implies that all lines in the file <literal>myfile.f</literal> are
copied into a verbatim block, typset in a <literal>!bc Xpro</literal> environment, where
<literal>X</literal> is the extension of the filename, here <literal>f</literal> (i.e., the environment
becomes <literal>!bc fpro</literal> and will typically lead to some Fortran-style
formatting in Linux and Sphinx).  The
second line has a <literal>fromto:</literal> directive, which implies copying code
between two lines in the code, typset within a !`bc Xcod`
environment (again, <literal>X</literal> is the filename extension, implying the
type of file). Note that the <literal>pro</literal> and <literal>cod</literal> arguments are only used for LaTeX
and Sphinx output, all other formats will have the code typeset within
a plain <literal>!bc</literal> environment.) Two regular expressions, separated by the
<literal>@</literal> sign, define the &quot;from&quot; and &quot;to&quot; lines.  The &quot;from&quot; line is
included in the verbatim block, while the &quot;to&quot; line is not. In the
example above, we copy code from the line matching <literal>subroutine test</literal>
(with as many blanks as desired between the two words) and the line
matching <literal>C END1</literal> (C followed by 5 blanks and then the text END1). The
final line with the &quot;to&quot; text is not included in the verbatim block.</paragraph><paragraph>Let us copy a whole file (the first line above):</paragraph><literal_block xml:space="preserve">C     a comment

      subroutine test()
      integer i
      real*8 r
      r = 0
      do i = 1, i
         r = r + i
      end do
      return
C     END1

      program testme
      call test()
      return</literal_block><paragraph>Let us then copy just a piece in the middle as indicated by the <literal>fromto:</literal>
directive above:</paragraph><literal_block xml:space="preserve">subroutine test()
integer i
real*8 r
r = 0
do i = 1, i
   r = r + i
end do
return</literal_block><paragraph>Note that the &quot;to&quot; line is not copied into the Doconce file, but the
&quot;from&quot; line is. Sometimes it is convenient to also neglect the
&quot;from&quot; line, a feature that is allowed by replacing <literal>fromto:</literal> by
<literal>from-to</literal> (&quot;from with minus&quot;). This allows for copying very similar
code segments throughout a file, while still distinguishing between them.
Copying the second set of parameters from the text:</paragraph><literal_block xml:space="preserve"># --- Start Example 1 ---
c = -1
A = 2
p0 = 4
simulate_and_plot(c, A, p0)
# --- End Example 1 ---

# --- Start Example 2 ---
c = -1
A = 1
p0 = 0
simulate_and_plot(c, A, p0)
# --- End Example 2 ---</literal_block><paragraph>is easy with:</paragraph><literal_block xml:space="preserve">from-to: Start Example 2@End Example 2</literal_block><paragraph>With only <literal>fromto:</literal> this would be impossible.</paragraph><paragraph>(Remark for those familiar with <literal>ptex2tex</literal>: The from-to
syntax is slightly different from that used in <literal>ptex2tex</literal>. When
transforming Doconce to LaTeX, one first transforms the document to a
<literal>.p.tex</literal> file to be treated by <literal>ptex2tex</literal>. However, the <literal>@@@CODE</literal> line
is interpreted by Doconce and replaced by the mentioned
pro or cod environment which are defined in the <literal>ptex2tex</literal> configuration
file.)</paragraph><target refid="mathtext"/></section><section ids="latex-blocks-of-mathematical-text mathtext" names="latex\ blocks\ of\ mathematical\ text mathtext"><title>LaTeX Blocks of Mathematical Text</title><paragraph>Blocks of mathematical text are like computer code blocks, but
the opening tag is <literal>!bt</literal> (begin TeX) and the closing tag is
<literal>!et</literal>. It is important that <literal>!bt</literal> and <literal>!et</literal> appear on the beginning of the
line and followed by a newline:</paragraph><literal_block xml:space="preserve">!bt
\begin{align}
{\partial u\over\partial t} &amp;= \nabla^2 u + f, label{myeq1}\\
{\partial v\over\partial t} &amp;= \nabla\cdot(q(u)\nabla v) + g. label{myeq2}
\end{align}
!et</literal_block><paragraph>The support of LaTeX mathematics varies among the formats:</paragraph><block_quote><bullet_list bullet="*"><list_item><paragraph>Output in LaTeX (<literal>latex</literal> and <literal>pdflatex</literal> formats) has of course full
support of all LaTeX mathematics, of course.</paragraph></list_item><list_item><paragraph>The <literal>html</literal> format supports single equations and multiple equations
via the align environment, also with labels.</paragraph></list_item><list_item><paragraph>Markdown (<literal>pandoc</literal> format) allows single equations and inline mathematics.</paragraph></list_item><list_item><paragraph>MediaWiki (<literal>mwiki</literal> format) does not enable labels in equations and hence
equations cannot be referred to.</paragraph></list_item></bullet_list></block_quote><paragraph>The main conclusion is that for
output beyond LaTeX (<literal>latex</literal> and <literal>pdflatex</literal> formats), stick to
simple <literal>\[</literal> and <literal>\]</literal> or <literal>equation</literal> and <literal>align</literal> or <literal>align*</literal> environments,
and avoid referring to equations in MediaWikis.</paragraph><paragraph>Going from Doconce to MS Word is most easily done by outputting in
the <literal>latex</literal> format and then using the Pandoc program to translate
from LaTeX to MS Word (note that only a subset of LaTeX will be
translated correctly).</paragraph><paragraph>If the document targets formats with and without support of LaTeX
mathematics, one can use the preprocessor to typeset the mathematics
in two versions. After <literal>#if FORMAT in (&quot;latex&quot;, &quot;pdflatex&quot;, &quot;html&quot;,
&quot;sphinx&quot;, &quot;mwiki&quot;, &quot;pandoc&quot;)</literal> one places LaTeX mathematics, and after
<literal>#else</literal> one can write inline mathematics in a way that looks nice in
plain text and wiki formats without support for mathematical
typesetting. Such branching can be used with mako if-else statements
alternatively:</paragraph><literal_block xml:space="preserve">% if FORMAT in (&quot;latex&quot;, &quot;pdflatex&quot;, &quot;html&quot;, &quot;sphinx&quot;, &quot;mwiki&quot;, &quot;pandoc&quot;):
!bt
\[ \sin^2x + \cos^2x = 1,\]
!et
% else:
!bc
              sin^2(x) + cos^2(x) = 1,
!ec
% endif</literal_block><section ids="mathematics-for-powerpoint-openoffice" names="mathematics\ for\ powerpoint/openoffice"><title>Mathematics for PowerPoint/OpenOffice</title><paragraph>If you have LaTeX mathematics written in Doconce, it is fairly easy
to generate PNG images of all mathematical formulas and equations for
use with PowerPoint or OpenOffice presentations.</paragraph><block_quote><enumerated_list enumtype="arabic" prefix="" suffix="."><list_item><paragraph>Make a Sphinx version of the Doconce file.</paragraph></list_item><list_item><paragraph>Go to the Sphinx directory and load the <literal>conf.py</literal> file into
a browser.</paragraph></list_item><list_item><paragraph>Search for &quot;math&quot; and comment out the
<literal>'sphinx.ext.mathjax'</literal> (enabled by default) and
<literal>'matplotlib.sphinxext.mathmpl'</literal> (disabled by default)
lines, and uncomment the <literal>'sphinx.ext.pngmath'</literal> package.
This is the package that generates small PNG pictures
of the mathematics.</paragraph></list_item><list_item><paragraph>Uncomment the line with <literal>pngmath_dvipng_args =</literal> and
set the PNG resolution to <literal>-D 200</literal> when the purpose is to
generate mathematics pictures for slides.</paragraph></list_item><list_item><paragraph>Run <literal>make html</literal>.</paragraph></list_item><list_item><paragraph>Look at the HTML source file in the <literal>_build/html</literal>
directory: all mathematics are in <literal>img</literal> tags with <literal>src=</literal>
pointing to a PNG file and <literal>alt=</literal> pointing to the LaTeX
source for the formula in question. This makes it very
easy to find the PNG file that corresponding to a particular
mathematical expression.</paragraph></list_item></enumerated_list></block_quote><target refid="newcommands"/></section></section><section ids="macros-newcommands newcommands" names="macros\ (newcommands) newcommands"><title>Macros (Newcommands)</title><paragraph>Doconce supports a type of macros via a LaTeX-style <emphasis>newcommand</emphasis>
construction.  The newcommands defined in a file with name
<literal>newcommand_replace.tex</literal> are expanded when Doconce is filtered to
other formats, except for LaTeX (since LaTeX performs the expansion
itself).  Newcommands in files with names <literal>newcommands.tex</literal> and
<literal>newcommands_keep.tex</literal> are kept unaltered when Doconce text is
filtered to other formats, except for the Sphinx format. Since Sphinx
understands LaTeX math, but not newcommands if the Sphinx output is
HTML, it makes most sense to expand all newcommands.  Normally, a user
will put all newcommands that appear in math blocks surrounded by
<literal>!bt</literal> and <literal>!et</literal> in <literal>newcommands_keep.tex</literal> to keep them unchanged, at
least if they contribute to make the raw LaTeX math text easier to
read in the formats that cannot render LaTeX.  Newcommands used
elsewhere throughout the text will usually be placed in
<literal>newcommands_replace.tex</literal> and expanded by Doconce.  The definitions of
newcommands in the <literal>newcommands*.tex</literal> files <emphasis>must</emphasis> appear on a single
line (multi-line newcommands are too hard to parse with regular
expressions).</paragraph></section><section ids="admonitions" names="admonitions"><title>Admonitions</title><paragraph>Doconce offers strong support for admonition environments, such
as warning boxes, notification boxes, question boxes,
and summary boxes. The boxes normally have an icon, a heading,
and may also have a background color. A special box, the block,
has never any icon and can be used when an icon would be disturbing
or misleading.</paragraph><paragraph>The following admonition environments are available:
<literal>block</literal>, <literal>warning</literal>, <literal>notice</literal>, <literal>question</literal>, and <literal>summary</literal>.
The box is defined by begin and end tags such as <literal>!bnotice</literal> and <literal>!enotice</literal>.
The title of the box is fully customizable.</paragraph><paragraph>Here are a few examples:</paragraph><literal_block xml:space="preserve">!bwarning
Here is a warning!
!ewarning

!bnotice Hint
This is a hint.
!enotice

!bblock This is a block.
A block has never any icon.
!eblock

!bnotice Going deeper
This is text meant to provide more details. The box has the
layout of the notice box, but a custom title, here &quot;Going deeper&quot;.
!enotice

Finally some summary:

!bsummary
The main message is to utilize the admonition styles for
marking different parts of the text
!esummary</literal_block><paragraph>The above Doconce code is in the present format rendered as</paragraph><warning><paragraph>Here is a warning!</paragraph></warning><admonition classes="admonition-hint"><title>Hint</title><paragraph>This is a hint.</paragraph></admonition><admonition classes="admonition-this-is-a-block"><title>This is a block</title><paragraph>A block has never any icon.</paragraph></admonition><admonition classes="admonition-going-deeper"><title>Going deeper</title><paragraph>This is text meant to provide more details. The box has the
layout of the notice box, but a custom title, here &quot;Going deeper&quot;.</paragraph></admonition><paragraph>Finally some summary:</paragraph><admonition classes="admonition-summary"><title>Summary</title><paragraph>The main message is to utilize the admonition styles for
marking different parts of the text</paragraph></admonition><paragraph>The layout of admonitions depend on the format.
In <literal>rst</literal> and <literal>sphinx</literal> one applies the native admonitions, but
in <literal>sphinx</literal> the <literal>automake_sphinx.py</literal> script manipulates the HTML
file to set a gray background for all admonitions.
In <literal>html</literal> one has a command-line argument <literal>--html_admon</literal> that
can be set to different styles: <literal>--html_admon=white</literal> for
white background and small icons, <literal>--html_admon=gray</literal> for
larger icons with gray background and small font, <literal>--html_admon=yellow</literal>
and <literal>--html_admon=apricot</literal>
works as the <literal>gray</literal> style, but the color is different.
With <literal>--html_admon=colors</literal> one gets quite bright colors
as backgrounds for the different admonitions.</paragraph><paragraph>Some recommended combinations for admonitions in HTML are</paragraph><block_quote><bullet_list bullet="*"><list_item><paragraph><literal>--html_style=solarized</literal>, <literal>--html_admon=apricot</literal>, <literal>--pygments_html_style=perldoc</literal></paragraph></list_item><list_item><paragraph><literal>--html_style=blueish2</literal>, <literal>--html_admon=yellow</literal>, <literal>--no_pygments_html</literal></paragraph></list_item><list_item><paragraph><literal>--html_style=blueish2</literal>, <literal>--html_admon=yellow</literal>, <literal>--pygments_html_style=default</literal></paragraph></list_item><list_item><paragraph><literal>--html_style=bloodish</literal>, <literal>--html_admon=gray</literal>, <literal>--no_pygments_html</literal></paragraph></list_item><list_item><paragraph><literal>--html_style=bloodish</literal>, <literal>--html_admon=gray</literal>, <literal>--pygments_html_style=default</literal></paragraph></list_item><list_item><paragraph><literal>--html_style=vagrant</literal>, <literal>--pygments_html_style=default</literal>, <literal>--html_template=...</literal></paragraph></list_item></bullet_list></block_quote><paragraph>The <literal>vagrant</literal> HTML style has CSS files that override the definition
how the admons are typset. The <literal>notice</literal> environment is gray with an
icon (defined in <literal>vagrant.css</literal>), while the others are yellow (defined
in <literal>twitter_bootstrap.css</literal>). The <literal>--html_admon</literal> color has no effect
for the <literal>vagrant</literal> style.</paragraph><paragraph>In <literal>latex</literal> and <literal>pdflatex</literal>, the type of admonition is configured in the
<literal>.p.tex</literal> file through the <literal>ADMON</literal> preprocessor variable.
Several values are available:</paragraph><block_quote><bullet_list bullet="*"><list_item><paragraph><literal>paragraph</literal> is the simplest type of admonition and typeset
as plain text with an optional paragraph heading.</paragraph></list_item><list_item><paragraph><literal>colors1</literal> (inspired by the NumPy User Guide) applies different colors for
the different admons with an embedded icon.</paragraph></list_item><list_item><paragraph><literal>colors2</literal> is like <literal>colors1</literal> but the text is wrapped around the icon.</paragraph></list_item><list_item><paragraph><literal>graybox1</literal> is the default and gives rounded gray boxes with a potential
title and no icon.</paragraph></list_item><list_item><paragraph><literal>graybox2</literal> has square corners, gray background, and is narrower
than <literal>graybox1</literal>. One special feature of <literal>graybox2</literal> is the summary
admon, which has a different look with horizontal rules only,
and for A4 format, the summary box is half of the text width and
wrapped with running text around (if it does not contain verbatim text,
in that case the standard <literal>graybox2</literal> style is used). This small
summary box is effective in proposals to disperse small paragraphs
of key points around.</paragraph></list_item><list_item><paragraph><literal>graybox3</literal> has icons and a light gray background.</paragraph></list_item><list_item><paragraph><literal>yellowbox</literal> has icons and a light yellow background.</paragraph></list_item></bullet_list></block_quote></section><section ids="preprocessing-steps" names="preprocessing\ steps"><title>Preprocessing Steps</title><paragraph>Doconce allows preprocessor commands for, e.g., including files,
leaving out text, or inserting special text depending on the format.
Two preprocessors are supported: preprocess
(<reference name="http://code.google.com/p/preprocess" refuri="http://code.google.com/p/preprocess">http://code.google.com/p/preprocess</reference><target ids="http-code-google-com-p-preprocess" names="http://code.google.com/p/preprocess" refuri="http://code.google.com/p/preprocess"/>) and mako
(<reference name="http://www.makotemplates.org/" refuri="http://www.makotemplates.org/">http://www.makotemplates.org/</reference><target ids="http-www-makotemplates-org" names="http://www.makotemplates.org/" refuri="http://www.makotemplates.org/"/>). The former allows include and if-else
statements much like the well-known preprocessor in C and C++ (but it
does not allow sophisticated macro substitutions). The latter
preprocessor is a very powerful template system.  With Mako you can
automatically generate various type of text and steer the generation
through Python code embedded in the Doconce document. An arbitrary set
of <literal>name=value</literal> command-line arguments (at the end of the command line)
automatically define Mako variables that are substituted in the document.</paragraph><paragraph>Doconce will detect if preprocess or Mako commands are used and run
the relevant preprocessor prior to translating the Doconce source to a
specific format.</paragraph><paragraph>The preprocess and mako programs always have the variable <literal>FORMAT</literal>
defined as the desired output format of Doconce (<literal>html</literal>, <literal>latex</literal>,
<literal>plain</literal>, <literal>rst</literal>, <literal>sphinx</literal>, <literal>epydoc</literal>, <literal>st</literal>).  It is then easy to test on
the value of <literal>FORMAT</literal> and take different actions for different
formats. Below is an example:</paragraph><literal_block xml:space="preserve">First some math:

!bt
\begin{align}
x &amp;= 3
label{x:eq1}\\
y &amp;= 5
label{y:eq1}
\end{align}
!et
Let us now reason about this.

# Sphinx cannot refer to labels in align environments

# #if FORMAT in (&quot;latex&quot;, &quot;pdflatex&quot;, &quot;html&quot;)
From (\ref{x:eq})-(\ref{y:eq1}) we get that
# #elif FORMAT == &quot;sphinx&quot;
From
!bt
\[ x = 3 \]
!et
and
!bt
\[ y= 5 \]
!et
it follows that
# #else
From the above equations it follows that
# #endif
$x+y$ is 8.</literal_block><paragraph>A variable <literal>DEVICE</literal> is also defined. It equals <literal>screen</literal> by default,
but the command-line argument <literal>--device=paper</literal> can set <literal>DEVICE</literal> to
<literal>paper</literal> (or another value). Testing on <literal>DEVICE</literal> inside the document
makes it possible to test if the output is on paper media, a sreen,
or a particular device.</paragraph><paragraph>Other user-defined variables for the preprocessor can be set at
the command line as explained in the section <reference name="From Doconce to Other Formats" refid="from-doconce-to-other-formats">From Doconce to Other Formats</reference>.</paragraph><paragraph>More advanced use of mako can include Python code that may automate
the writing of parts of the document.</paragraph></section><section ids="splitting-documents-into-smaller-pieces" names="splitting\ documents\ into\ smaller\ pieces"><title>Splitting Documents into Smaller Pieces</title><paragraph>Long documents are conveniently split into smaller Doconce files.
However, there must be a master document including all the pieces,
otherwise references to sections and the index will not work properly.
The master document is preferably a file just containing a set of
preprocessor include statements of the form <literal>#include &quot;file.do.txt&quot;</literal>.
The preprocessor will put together all the pieces so that Doconce
sees a long file with the complete text.</paragraph><paragraph>For web documents it is often desired to split long pages into shorter
ones. This is done by the Doconce command <literal>!split</literal> placed at the
beginning of a line. The <literal>!split</literal> commands works with output in
<literal>html</literal>, <literal>rst</literal>, <literal>sphinx</literal>, <literal>latex</literal>, and <literal>pdflatex</literal>. The <literal>!split</literal> command
are normally placed before section headings. It is very actively used
when writing slides with Doconce. The <literal>doconce format</literal> command does not
recognize <literal>!split</literal> instructions: one needs to run <literal>doconce split_*</literal>
as a postprocess, where the <literal>*</literal> means <literal>html</literal>, <literal>rst</literal>, or <literal>beamer</literal>.</paragraph><paragraph><emphasis>HTML.</emphasis> Splitting an HTML document is done by:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format html mydoc
Terminal&gt; doconce split_html mydoc</literal_block><paragraph>The <literal>mydoc.html</literal> document created by the first command is replaced
by a new HTML file, representing the first part of the document,
after the second command. The various files that constitute the
parts of the document are listed after the <literal>split_html</literal> command.
The files have names <literal>mydoc.html</literal>, <literal>._part0000_mydoc.html</literal> (equal to
<literal>mydoc.html</literal>), <literal>._part0001_mydoc.html</literal>, <literal>._part0002_mydoc.html</literal>, and so
on. Recall that all the parts are needed if the HTML document is to be moved
to another location (you can always check <literal>.mydoc_html_file_collection</literal>
for a list of all the files that are needed to display this HTML
document).</paragraph><paragraph>The HTML documents have very simple navigation buttons for the previous
and next document. These are not customizable directly, but one can easily
look up the HTML code and use <literal>doconce replace</literal> to edit the links to
the images used for navigation. Some more colorful images arise from:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce replace 'prev1.png' 'Knob_Left.png' \
          mydoc.html ._part*_mydoc.html
Terminal&gt; doconce replace 'next1.png' 'Knob_Forward.png' \
          mydoc.html ._part*_mydoc.html</literal_block><paragraph>This works because the <literal>Knob*.png</literal> images live in the same place in the
Doconce repository as <literal>prev1.png</literal> and <literal>next1.png</literal>. Other images can be
used by replacing the whole URL.</paragraph><paragraph>With an HTML template one can have much more sophisticated navigation
between parts. One example is the template
in <literal>bundled/html_styles/style_vagrant/template_vagrant.html</literal> in the Doconce
source.</paragraph><paragraph><emphasis>reStructuredText and Sphinx.</emphasis> Here is a typical split of a large Sphinx document <literal>mydoc.rst</literal>
into smaller pieces:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce sphinx_dir author=&quot;Some Author&quot; \
          title=&quot;Short title&quot; theme=fenics dirname=mydir mydoc
Terminal&gt; doconce format sphinx mydoc
Terminal&gt; doconce split_rst mydoc
Terminal&gt; python automake_sphinx.py</literal_block><paragraph>The <literal>doconce format sphinx mydoc</literal> command is needed to produce <literal>mydoc.rst</literal>,
which is the starting point for the <literal>doconce split_rst</literal> command.
The various files that constitute the complete Sphinx document are
<literal>mydoc.rst</literal>, <literal>._part0000_mydoc.rst</literal>, <literal>._part0001_mydoc.rst</literal>, <literal>._part0002_mydoc.rst</literal>,
and so on. The <literal>automake_sphinx.py</literal> script ensures that the Sphinx document
is compiled correctly. If all links to local files are in a <literal>_static</literal>
directory, the whole Sphinx document exists in a complete version
in the compiled directory (usually <literal>sphinx-rootdir/_build/html</literal>) and
can easily be moved around.</paragraph><paragraph>LaTeX Beamer slides generated from Doconce source also apply <literal>!split</literal> to
indicate the start of individual slides. However, the split is
performed by the <literal>doconce slides_beamer</literal> command and does not result
in individual files like <literal>split_rst</literal> and <literal>split_html</literal> do.</paragraph></section><section ids="missing-features" names="missing\ features"><title>Missing Features</title><paragraph>Doconce does not aim to support sophisticated typesetting, simply because
sophisticated typesetting usually depend quite strongly on the particular
output format chosen. When a particular feature needed is not supported
by Doconce, it is recommended to hardcode that feature for a particular
format and use the if-else construction of the preprocessor. For example,
if a sophisticated table is desired in LaTeX output, do something like:</paragraph><literal_block xml:space="preserve"># #if FORMAT in (&quot;latex&quot;, &quot;pdflatex&quot;)
# insert native LaTeX code for fancy table
# #else
# insert a Doconce-formatted &quot;inline&quot; table
# #endif</literal_block><paragraph>Similarly, if certain adjustments are needed, like
pagebreaks in LaTeX, hardcode that in the Doconce format (and recall
that this is really LaTeX dependent - pagebreaks are not
relevant HTML formats).</paragraph><paragraph>Instead of inserting special code in the Doconce document, one can
alternatively script editing of the output from Doconce. That is,
we develop a Python or Bash script that runs the translation of
a Doconce document to a ready docoment in another format. Inside this
script, we may edit and fine-tune the output from Doconce.</paragraph></section><section ids="header-and-footer" names="header\ and\ footer"><title>Header and Footer</title><paragraph>Some formats use a header and footer in the document. LaTeX and
HTML are two examples of such formats. When the document is to be
included in another document (which is often the case with
Doconce-based documents), the header and footer are not wanted, while
these are needed (at least in a LaTeX context) if the document is
stand-alone. We have introduced the convention that if <literal>TITLE:</literal>
is found at the beginning of the line (i.e., the document
has a title), the header and footer are included, otherwise not.</paragraph><target refid="emacs-doconce"/></section><section ids="emacs-doconce-formatter emacs-doconce" names="emacs\ doconce\ formatter emacs:doconce"><title>Emacs Doconce Formatter</title><paragraph>The file <reference name=".doconce-mode.el" refuri="https://doconce.googlecode.com/hg/misc/.doconce-mode.el">.doconce-mode.el</reference><target ids="doconce-mode-el" names=".doconce-mode.el" refuri="https://doconce.googlecode.com/hg/misc/.doconce-mode.el"/> in the Doconce source distribution
gives a &quot;Doconce Editing Mode&quot; in Emacs.</paragraph><paragraph>Here is how to get the Doconce Editing Mode in Emacs: Download <reference name=".doconce-mode.el" refuri="https://doconce.googlecode.com/hg/misc/.doconce-mode.el">.doconce-mode.el</reference><target dupnames=".doconce-mode.el" ids="id14" refuri="https://doconce.googlecode.com/hg/misc/.doconce-mode.el"/> and save it in your home directory, then add these lines to <literal>~/.emacs</literal>:</paragraph><literal_block xml:space="preserve">(load-file &quot;~/.doconce-mode.el&quot;)</literal_block><paragraph>Emacs will now recognize files with extension <literal>.do.txt</literal> and enter
the Doconce Editing Mode.</paragraph><paragraph>The major advantage with the Doconce Editing Mode in Emacs is that
many keyboard shortcuts are defined:</paragraph><table><tgroup cols="2"><colspec colwidth="34"/><colspec colwidth="34"/><thead><row><entry><paragraph>Emacs key</paragraph></entry><entry><paragraph>Action</paragraph></entry></row></thead><tbody><row><entry><paragraph>Ctrl+c f</paragraph></entry><entry><paragraph>figure</paragraph></entry></row><row><entry><paragraph>Ctrl+c v</paragraph></entry><entry><paragraph>movie/video</paragraph></entry></row><row><entry><paragraph>Ctrl+c h1</paragraph></entry><entry><paragraph>heading level 1 (section/h1)</paragraph></entry></row><row><entry><paragraph>Ctrl+c h2</paragraph></entry><entry><paragraph>heading level 2 (subsection/h2)</paragraph></entry></row><row><entry><paragraph>Ctrl+c h3</paragraph></entry><entry><paragraph>heading level 2 (subsection/h3)</paragraph></entry></row><row><entry><paragraph>Ctrl+c hp</paragraph></entry><entry><paragraph>heading for paragraph</paragraph></entry></row><row><entry><paragraph>Ctrl+c me</paragraph></entry><entry><paragraph>math environment: !bt equation !et</paragraph></entry></row><row><entry><paragraph>Ctrl+c ma</paragraph></entry><entry><paragraph>math environment: !bt align !et</paragraph></entry></row><row><entry><paragraph>Ctrl+c ce</paragraph></entry><entry><paragraph>code environment: !bc !ec</paragraph></entry></row><row><entry><paragraph>Ctrl+c cf</paragraph></entry><entry><paragraph>code from file: @@@CODE</paragraph></entry></row><row><entry><paragraph>Ctrl+c table2</paragraph></entry><entry><paragraph>table with 2 columns</paragraph></entry></row><row><entry><paragraph>Ctrl+c table3</paragraph></entry><entry><paragraph>table with 3 columns</paragraph></entry></row><row><entry><paragraph>Ctrl+c table4</paragraph></entry><entry><paragraph>table with 4 columns</paragraph></entry></row><row><entry><paragraph>Ctrl+c exer</paragraph></entry><entry><paragraph>exercise outline</paragraph></entry></row><row><entry><paragraph>Ctrl+c slide</paragraph></entry><entry><paragraph>slide outline</paragraph></entry></row><row><entry><paragraph>Ctrl+c help</paragraph></entry><entry><paragraph>print this table</paragraph></entry></row></tbody></tgroup></table><paragraph>Typing <literal>Ctrl+c help</literal> prints the above table in Emacs. Try out
the different shortcuts and see how handy they are in learning
Doconce and saving much typing!</paragraph></section></section><section ids="writing-slides" names="writing\ slides"><title>Writing Slides</title><paragraph>It is a fast procedure to make slides from large amounts of Doconce
text, in particular for condensing running material for teaching or
just providing the slide set as an overview or study guide.
The slides can either be ordinary, separate slides - or just
a document with much briefer text and emphasis on bullet lists,
figures, mathematical formulas, admonitions, and little text.</paragraph><paragraph>Points:</paragraph><block_quote><bullet_list bullet="*"><list_item><paragraph>Only some pygments style are suited for a particular reveal.js/deck.js
theme</paragraph></list_item><list_item><paragraph>Only some admon styles are appropriate</paragraph></list_item><list_item><paragraph>Admon styles are erased in reveal</paragraph></list_item><list_item><paragraph>Use <literal>--keep_pygments_html_bg</literal> to avoid big changes in background
color for code</paragraph></list_item></bullet_list></block_quote><section ids="overview" names="overview"><title>Overview</title><paragraph>Basically, Doconce slides are ordinary Doconce text with <literal>!split</literal>
inserted before each slide. Nevertheless, contents of slide differ
considerably from ordinary running text. Some guidelines on
the elements within each slide are necessary to produce effective
slide sets:</paragraph><block_quote><bullet_list bullet="*"><list_item><paragraph>Use a subsection heading as slide heading (5 <literal>=</literal>).</paragraph></list_item><list_item><paragraph>Limit the amount of running text (as always).</paragraph></list_item><list_item><paragraph>Limit the amount of material so it fits within a slide
(inspect slides visually to move or delete content - just
an extra <literal>!split</literal> and a new heading is enough to make a new
slide).</paragraph></list_item><list_item><paragraph>Use the <literal>slidecell</literal> environment (see below) to create
a grid of slide cells (makes it easy to move figures and
bullet lists or text around).</paragraph></list_item><list_item><paragraph>Adjust the size of figures (<literal>width</literal> parameter for HTML,
<literal>frac</literal> parameter for LaTeX Beamer) so they become effective
on the slide.</paragraph></list_item></bullet_list></block_quote><paragraph>Doconce can generate two types of slides: HTML5+CSS3 type of
slides to be presented through a web browser, and classical
LaTeX Beamer slides.</paragraph></section><section ids="html5-slides" names="html5\ slides"><title>HTML5 Slides</title><comment xml:space="preserve">doconce-adjusted styles: easy to switch between styles since</comment><comment xml:space="preserve">font sizes are compatible</comment><paragraph>Not yet written...</paragraph><paragraph>Just a very preliminary sketch of commands:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format html myslides \
          --pygments_html_style=native --keep_pygments_html_bg
Terminal&gt; doconce slides_html myslides reveal \
          --html_slide_theme=darkgray</literal_block><section ids="potential-problems" names="potential\ problems"><title>Potential Problems</title><block_quote><bullet_list bullet="*"><list_item><paragraph>Some newer Firefox does not show rounded corners of the
admonition boxes, e.g., notice and warning (tested on Ubuntu)</paragraph></list_item><list_item><paragraph>Doconce performs some adjustments of the spacing around
equations. More edits (automate with <literal>doconce subst</literal>) might be needed.</paragraph></list_item></bullet_list></block_quote></section></section><section ids="latex-beamer-slides" names="latex\ beamer\ slides"><title>LaTeX Beamer Slides</title><paragraph>Not yet written...</paragraph><section ids="themes" names="themes"><title>Themes</title><paragraph>Four themes come with Doconce: <literal>X_Y</literal>, where <literal>X</literal> is <literal>blue</literal> or <literal>red</literal>
(the main color of the slides) and <literal>Y</literal> is <reference name="plain" refuri="http://hplgit.github.io/teamods/doconce/demo/demo_red_plain.pdf">plain</reference><target ids="plain" names="plain" refuri="http://hplgit.github.io/teamods/doconce/demo/demo_red_plain.pdf"/>
for simple layout and
<reference name="shadow" refuri="http://hplgit.github.io/teamods/doconce/demo/demo_blue_shadow.pdf">shadow</reference><target ids="shadow" names="shadow" refuri="http://hplgit.github.io/teamods/doconce/demo/demo_blue_shadow.pdf"/>
for shadowed boxes and more visual structure in the slides.</paragraph></section></section></section><section ids="mako-programming" names="mako\ programming"><title>Mako Programming</title><system_message backrefs="id15" level="2" line="3655" source="manual.rst" type="WARNING"><paragraph>Duplicate explicit target name: &quot;mako&quot;.</paragraph></system_message><paragraph>The <reference name="Mako" refuri="http://docs.makotemplates.org/">Mako</reference><target dupnames="mako" ids="id15" refuri="http://docs.makotemplates.org/"/> templating engine is used
as preprocessor for Doconce documents, but the <reference name="Preprocess" refuri="http://code.google.com/p/preprocess">Preprocess</reference><target dupnames="preprocess" ids="id16" refuri="http://code.google.com/p/preprocess"/> is run prior to Mako and is recommended for
including other files via <literal># #include &quot;filename&quot;</literal>. Preprocess is also
sufficient for if-else tests to steer which parts of the text that
are to be compiled. For more demanding tasks, use Mako, which resembles
a real programming language.</paragraph><warning><paragraph>Unfortunately, the combination of Mako and LaTeX mathematics may
lead to problems because Mako applies syntax like <literal>${var}</literal> to extract
variables or call functions, while LaTeX mathematics sometimes applies
the same syntax, e.g., <literal>${\cal O}(\Delta x^2)$</literal> which looks like a
Mako function call. This problem can give rise to strange error
messages from Mako, usually that a variable is not defined.</paragraph></warning><section ids="the-basics-of-mako" names="the\ basics\ of\ mako"><title>The Basics of Mako</title><paragraph>Just a preliminary sketch of some Mako code (next example is better!):</paragraph><literal_block xml:space="preserve"># Define variables
&lt;%
mycounter = 1
mydict = {}
%&gt;

# Assume MYVAR is given on the command line as MYVAR=mytext (e.g.)
% if MYVAR is not UNDEFINED:
The value of MYVAR is ${MYVAR}.
% endif

&lt;%
## Manipulation of variables
mycounter += 1
%&gt;

% if MYVAR in (2,4,6):
MYVAR is even integer in [2,6].
% elif MYVAR &gt; 1000000:
MYVAR is big.
% else:
MYVAR=${MYVAR}, mycounter=${mycounter}.
% endif

# Function
&lt;%
# Define Python function: FORMAT and DEVICE
# are always defined

def link(filename):
    url = &quot;https://github.com/some/path/to/' + filename + '&quot;'
    if DEVICE == 'screen':
        # make link to url
        return '&quot;filename&quot;:' + url
    elif DEVICE == 'paper':
        # write URL explicit on paper
        return 'URL:' + url
%&gt;

&lt;%doc&gt;
This
is
a
block
comment in Mako.
&lt;%doc/&gt;</literal_block><target refid="manual-theorem-envir"/></section><section ids="example-defining-a-theorem-environment manual-theorem-envir" names="example:\ defining\ a\ theorem\ environment manual:theorem:envir"><title>Example: Defining a Theorem Environment</title><paragraph>Doconce supports only basic formatting elements (headings, paragraphs,
lists, etc.), while LaTeX users are used to fancy environments for, e.g.,
theorems. A flexible strategy is to typeset theorems
using paragraph headings, which will look satisfactorily in all
formats, but add comment lines that can be replaced by LaTeX environments
via <literal>doconce replace</literal>. Theorems can be numbered using a variable in Mako.
Here is an example on raw Doconce code:</paragraph><literal_block xml:space="preserve">&lt;%
theorem_counter = 4
%&gt;

# begin theorem
label{theorem:fundamental1}
&lt;%
theorem_counter += 1
theorem_fundamental1 = theorem_counter
%&gt;

__Theorem ${theorem_counter}.__
Let $a=1$ and $b=2$. Then $c=3$.
# end theorem

# begin proof
__Proof.__
Since $c=a+b$, the result follows from straightforward addition.
$\Diamond$|$END$
# end proof

As we see, the proof of Theorem ${theorem_counter} is a modest
achievement.</literal_block><paragraph>The <literal>.p.tex</literal> output file now reads:</paragraph><literal_block xml:space="preserve">% begin theorem
label{theorem:fundamental1}


\paragraph{Theorem 5.}
Let $a=1$ and $b=2$. Then $c=3$.
% end theorem

% begin proof
\paragraph{Proof.}
Since $c=a+b$, the result follows from straightforward addition.
$\Diamond$
% end proof

As we see, the proof of Theorem 5 is a modest
achievement.</literal_block><paragraph>Note that with Mako variables we can easily create our own counters,
and this works in any format. In LaTeX we can use both the generated
numbers from Mako variables or we can use the labels.</paragraph><paragraph>The next step is to replace the <literal>% begin ...</literal> and <literal>% end ...</literal> lines with
the proper LaTeX expressions in the <literal>.p.tex</literal> file. Moreover, we
need to remove the paragraphs with <emphasis>Theorem</emphasis>.
The following Bash script does the job:</paragraph><literal_block xml:space="preserve">file=mydoc.p.tex
thpack='\\usepackage{theorem}\n\\newtheorem{theorem}{Theorem}[section]'
doconce subst '% insert custom LaTeX commands\.\.\.' $thpack $file
doconce subst '\\paragraph\{Theorem \d+\.\}' '' $file
doconce replace '% begin theorem' '\begin{theorem}' $file
doconce replace '% end theorem' '\end{theorem}' $file</literal_block><paragraph>More heavy editing is better done in a Python script that reads the
<literal>mydoc.p.tex</literal> file and performs string substitutions and regex
substitutions as needed.</paragraph><paragraph>The resulting <literal>mydoc.tex</literal> file now becomes:</paragraph><literal_block xml:space="preserve">\usepackage{theorem}
\newtheorem{theorem}{Theorem}[section]

...

\begin{theorem}
\label{theorem:fundamental1}



Let $a=1$ and $b=2$. Then $c=3$.
\end{theorem}

% begin proof
\paragraph{Proof.}
Since $c=a+b$, the result follows from straightforward addition.
$\Diamond$
% end proof

As we see, the proof of Theorem 5 is a modest
achievement.</literal_block><paragraph>Even better, HTML output looks nice as well.</paragraph><paragraph>Note that Doconce supports fancy environments for verbatim code via
the <literal>ptex2tex</literal> program with all its flexibility for choosing environments.
Also <literal>doconce ptex2tex</literal> has some flexibility for typesetting computer code.</paragraph></section></section><section ids="troubleshooting" names="troubleshooting"><title>Troubleshooting</title><section ids="disclaimer" names="disclaimer"><title>Disclaimer</title><paragraph>Doconce has some support for syntax checking.  If you encounter Python
errors while running <literal>doconce format</literal>, the reason for the error is
most likely a syntax problem in your Doconce source file. You have to
track down this syntax problem yourself. However, Doconce applies
regular expressions to a large extent for transforming text, and
regular expressions may sometimes fail. Therefore, there is a chance that legal
Doconce syntax is not treated properly.</paragraph></section><section ids="general-problems" names="general\ problems"><title>General Problems</title><section ids="spellcheck-reports-a-lot-of-mistakes-related-latex-math" names="spellcheck\ reports\ a\ lot\ of\ mistakes\ related\ latex\ math"><title>Spellcheck reports a lot of mistakes related LaTeX math</title><paragraph>The <literal>doconce spellcheck</literal> command should ignore LaTeX math, but if
the dollar signs for inline math are not correct (one missing, for instance),
a lot of math enters the text to be spellchecked. Invoke the
relevant <literal>tmp_missing_*</literal> file and search for the math expressions that
are reported as misspellings. This will fortunately give you hint of
what is wrong with the math typesetting.</paragraph></section><section ids="doconce-aborts-because-of-a-syntax-error-that-is-not-an-error" names="doconce\ aborts\ because\ of\ a\ syntax\ error\ that\ is\ not\ an\ error"><title>Doconce aborts because of a syntax error that is not an error</title><paragraph>Doconce searches for typical syntax errors and usually aborts the
execution if errors are found. However, it may happen,
especially in verbatim blocks, that Doconce reports syntax errors
that are not errors. To continue execution, simply add the
<literal>--no_abort</literal> option on the command line. You may send an email
to the Doconce author at <literal>hpl@simula.no</literal> and report the problem.</paragraph></section><section ids="the-mako-preprocessor-is-seemingly-not-run" names="the\ mako\ preprocessor\ is\ seemingly\ not\ run"><title>The Mako preprocessor is seemingly not run</title><paragraph>If you have lines starting with <literal>%</literal> inside code segments (for example,
SWIG code or Matlab comment lines), the Mako preprocessor will crash
because it thinks these lines are Mako statements. Doconce detects
this problem and avoids running Mako.  Examine the output from
Doconce: warnings are issued if Mako is not run.</paragraph></section><section ids="the-mako-preprocessor-is-fooled-by-doconce-text" names="the\ mako\ preprocessor\ is\ fooled\ by\ doconce\ text"><title>The Mako preprocessor is fooled by Doconce text</title><paragraph>Here are possible problems for Mako:</paragraph><block_quote><bullet_list bullet="*"><list_item><paragraph>Strings with <literal>'T&lt;%.1f'</literal> look as openings of Mako blocks (<literal>&lt;%</literal>); change
to <literal>'T &lt; %.1f'</literal> to avoid this confusion.</paragraph></list_item></bullet_list></block_quote></section><section ids="the-mako-preprocessor-claims-a-variable-is-undefined" names="the\ mako\ preprocessor\ claims\ a\ variable\ is\ undefined"><title>The Mako preprocessor claims a variable is undefined</title><paragraph>Very often such errors are related to typos when using Mako
variables or functions, or correct yet undesired LaTeX syntax.
For example, {cal O}(Delta x^2) is valid LaTeX, but
the dollar sign and curly braces confuse Mako. Rewrite such
mathematics. It is wise to not use <literal>${</literal> anywhere in LaTeX mathematics.
Create a newcommand if there are no other possible rewritings.
A known common problem is <literal>${}^+$</literal> type of indication of superscripts.
A suggested rewrite is <literal>$\,{}^+$</literal>.</paragraph><paragraph>The error message will ask you to rerun <literal>doconce</literal> with
<literal>--mako_strict_undefined</literal>, which will display the variable that
is confusing Mako. However, do not continue to use
<literal>--mako_strict_undefined</literal> while you are debugging because this
variable will then always be undefined in that mode.
Use <literal># #ifdef</literal> directives to comment out large portions of
the text and apply a &quot;bisection&quot; procedure to locate where
the Mako problem is (without <literal>--mako_strict_undefined</literal>).</paragraph></section><section ids="something-goes-wrong-in-the-preprocessing-step" names="something\ goes\ wrong\ in\ the\ preprocessing\ step"><title>Something goes wrong in the preprocessing step</title><paragraph>You can examine <literal>tmp_preprocess__filename</literal> and <literal>tmp_mako__filename</literal>,
where <literal>filename</literal> is the complete name of the Doconce file, to see
what the preprocessors actually to and if something is wrong in these
files before Doconce starts translating the text.
One or both of those files may be missing, but examine the beginning
of the output from Doconce to see exactly which preprocessors are run
and on which files.</paragraph></section><section ids="figure-captions-are-incomplete" names="figure\ captions\ are\ incomplete"><title>Figure captions are incomplete</title><paragraph>If only the first part of a figure caption in the Doconce file is seen
in the target output format, the reason is usually that the caption
occupies multiple lines in the Doconce file. The figure caption must
be written as <emphasis>one line</emphasis>, at the same line as the FIGURE keyword.</paragraph></section><section ids="preprocessor-directives-do-not-work" names="preprocessor\ directives\ do\ not\ work"><title>Preprocessor directives do not work</title><paragraph>Make sure the preprocessor instructions, in Preprocess or Mako, have
correct syntax. Also make sure that you do not mix Preprocess and Mako
instructions. Doconce will then only run Preprocess.</paragraph></section><section ids="problems-with-boldface-and-emphasize" names="problems\ with\ boldface\ and\ emphasize"><title>Problems with boldface and emphasize</title><paragraph>Two boldface or emphasize expressions after each other are not rendered
correctly. Merge them into one common expression.</paragraph></section><section ids="links-to-local-directories-do-not-work" names="links\ to\ local\ directories\ do\ not\ work"><title>Links to local directories do not work</title><paragraph>Links of the type:</paragraph><literal_block xml:space="preserve">see the &quot;examples directory&quot;: &quot;src/examples&quot;</literal_block><paragraph>do not work well. You need to link to a specific HTML file:</paragraph><literal_block xml:space="preserve">see the &quot;examples directory&quot;: &quot;src/examples/index.html&quot;</literal_block><paragraph>We recommend to put all files you link to in a <literal>_static</literal> directory
if you intend to use the <literal>sphinx</literal> output. This guarantees that all
your files are collected in the Sphinx directory tree bundle.
With plain <literal>html</literal> output only, you can link to whatever, but remember
to move all files you link to if you move the primary <literal>.html</literal> file.</paragraph></section><section ids="links-are-not-typeset-correctly" names="links\ are\ not\ typeset\ correctly"><title>Links are not typeset correctly</title><paragraph>Not all formats will allow formatting of the links. Verbatim words
in links are allowed if the whole link is typeset in verbatim:</paragraph><literal_block xml:space="preserve">see the directory &quot;`examples`&quot;: &quot;src/examples/index.html&quot;.</literal_block><paragraph>However, the following will not be typeset correctly:</paragraph><literal_block xml:space="preserve">see the &quot;`examples`&quot;: &quot;src/examples/index.html&quot; directory.</literal_block><paragraph>The back-ticks must be removed, or the text can be reformulated as
in the line above it.</paragraph></section><section ids="inline-verbatim-code-is-not-detected" names="inline\ verbatim\ code\ is\ not\ detected"><title>Inline verbatim code is not detected</title><paragraph>Make sure there is a space before the first back-tick.</paragraph></section><section ids="inline-verbatim-text-is-not-formatted-correctly" names="inline\ verbatim\ text\ is\ not\ formatted\ correctly"><title>Inline verbatim text is not formatted correctly</title><paragraph>Make sure there is whitespace surrounding the text in back-ticks.</paragraph></section><section ids="strange-non-english-characters" names="strange\ non-english\ characters"><title>Strange non-English characters</title><paragraph>The former reason for this problem is that Doconce could only work with latin1
(ISO-8859) encoding and not UTF-8. After May 2013, Doconce applies
UTF-8 both for HTML and LaTeX.</paragraph><paragraph>Check the encoding of the <literal>.do.txt</literal> file with the Unix <literal>file</literal> command
or with:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce guess_encoding myfile.do.txt</literal_block><paragraph>If the encoding is UTF-8, convert to latin1 using either of
the Unix commands:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce change_encoding utf-8 latin1 myfile.do.txt
Terminal&gt; iconv -f utf-8 -t latin1 myfile.do.txt --output newfile</literal_block></section><section ids="wrong-norwegian-charcters" names="wrong\ norwegian\ charcters"><title>Wrong Norwegian charcters</title><paragraph>When Doconce documents have characters not in the standard ASCII set,
the format of the file must be LATIN1 and not UTF-8. See
the section &quot;Strange non-English characters&quot; above for how to
run <literal>doconce change_encoding</literal> to change the encoding of the Doconce file.</paragraph></section><section ids="too-short-underlining-of-rest-headlines" names="too\ short\ underlining\ of\ rest\ headlines"><title>Too short underlining of reST headlines</title><paragraph>This may happen if there is a paragraph heading without
proceeding text before some section heading.</paragraph></section><section ids="found-bt-but-no-tex-blocks-extracted-bug" names="found\ !bt\ but\ no\ tex\ blocks\ extracted\ (bug)"><title>Found !bt but no tex blocks extracted (BUG)</title><paragraph>This message points to a bug, but has been resolved by removing blank lines
between the text and the first <literal>!bt</literal> (inserting the blanks again did not
trigger the error message again...).</paragraph></section><section ids="examples-are-typset-with-environment-delimiters-visible" names="examples\ are\ typset\ with\ environment\ delimiters\ visible"><title>Examples are typset with environment delimiters visible</title><paragraph>If you see an Example section containing <literal>!bsubex</literal>, <literal>!bsol</literal>, or other
begin and end tags for environments, it means that you have intended
to typeset examples as exercises, but forgotten the command-line
option <literal>--examples_as_exercises</literal>. The text in the example is typeset
as is unless this option is included.</paragraph></section><section ids="emacs-editing-does-not-work-properly-because-of-regexp-overflow" names="emacs\ editing\ does\ not\ work\ properly\ because\ of\ &quot;regexp\ overflow&quot;"><title>Emacs editing does not work properly because of &quot;regexp overflow&quot;</title><paragraph>Sometimes the Doonce editing mode (see the section <reference name="Emacs Doconce Formatter" refid="emacs-doconce-formatter">Emacs Doconce Formatter</reference>) in Emacs
leads to an error message ending with &quot;overflow in regexp matcher&quot;.
This error is due to some regular expression used in the Doconce editing
mode. The remedy is to split the file into smaller pieces and include
the pieces using the <literal>preprocess</literal> directive <literal>#include &quot;piece.do.txt&quot;</literal>.
The error message comes with the Doconce file contains too much text
for Emacs to handle.</paragraph></section></section><section ids="problems-with-code-or-tex-blocks" names="problems\ with\ code\ or\ tex\ blocks"><title>Problems with code or Tex Blocks</title><section ids="code-or-math-block-errors-in-rest" names="code\ or\ math\ block\ errors\ in\ rest"><title>Code or math block errors in reST</title><paragraph>First note that a code or math block must come after some plain
sentence (at least for successful output in reST), not directly
after a section/paragraph heading, table, comment, figure, or
movie, because the code or math block is indented and then become
parts of such constructions. Either the block becomes invisible or
error messages are issued.</paragraph><paragraph>Sometimes reST reports an &quot;Unexpected indentation&quot; at the beginning of
a code block. If you see a <literal>!bc</literal>, which should have been removed when
running <literal>doconce format sphinx</literal>, it is usually an error in the Doconce
source, or a problem with the rst/sphinx translator.  Check if the
line before the code block ends in one colon (not two!), a question
mark, an exclamation mark, a comma, a period, or just a newline/space
after text. If not, make sure that the ending is among the
mentioned. Then <literal>!bc</literal> will most likely be replaced and a double colon
at the preceding line will appear (which is the right way in reST to
indicate a verbatim block of text).</paragraph></section><section ids="strange-errors-around-code-or-tex-blocks-in-rest" names="strange\ errors\ around\ code\ or\ tex\ blocks\ in\ rest"><title>Strange errors around code or TeX blocks in reST</title><paragraph>If <literal>idx</literal> commands for defining indices are placed inside paragraphs,
and especially right before a code block, the reST translator
(rst and sphinx formats) may get confused and produce strange
code blocks that cause errors when the reST text is transformed to
other formats. The remedy is to define items for the index outside
paragraphs.</paragraph></section><section ids="something-is-wrong-with-a-verbatim-code-block" names="something\ is\ wrong\ with\ a\ verbatim\ code\ block"><title>Something is wrong with a verbatim code block</title><paragraph>Check first that there is a &quot;normal&quot; sentence right before
the block (this is important for reST and similar
&quot;ASCII-close&quot; formats).</paragraph></section><section ids="code-tex-block-is-not-shown-in-rest-format" names="code/tex\ block\ is\ not\ shown\ in\ rest\ format"><title>Code/TeX block is not shown in reST format</title><paragraph>A comment right before a code or tex block will treat the whole
block also as a comment. It is important that there is normal
running text right before <literal>!bt</literal> and <literal>!bc</literal> environments.</paragraph></section><section ids="verbatim-code-blocks-inside-lists-look-ugly" names="verbatim\ code\ blocks\ inside\ lists\ look\ ugly"><title>Verbatim code blocks inside lists look ugly</title><paragraph>Read the the section <reference name="Blocks of Verbatim Computer Code" refid="blocks-of-verbatim-computer-code">Blocks of Verbatim Computer Code</reference> above.  Start the
<literal>!bc</literal> and <literal>!ec</literal> tags in column 1 of the file, and be careful with
indenting the surrounding plain text of the list item correctly. If
you cannot resolve the problem this way, get rid of the list and use
paragraph headings instead. In fact, that is what is recommended:
avoid verbatim code blocks inside lists (it makes life easier).</paragraph></section><section ids="latex-code-blocks-inside-lists-look-ugly" names="latex\ code\ blocks\ inside\ lists\ look\ ugly"><title>LaTeX code blocks inside lists look ugly</title><paragraph>Same solution as for computer code blocks as described in the
previous paragraph. Make sure the <literal>!bt</literal> and <literal>!et</literal> tags are in column 1
and that the rest of the non-LaTeX surrounding text is correctly indented.
Using paragraphs instead of list items is a good idea also here.</paragraph></section></section><section ids="problems-with-rest-sphinx-output" names="problems\ with\ rest/sphinx\ output"><title>Problems with reST/Sphinx Output</title><section ids="title-level-inconsistent" names="title\ level\ inconsistent"><title>Title level inconsistent</title><paragraph>reST does not like jumps in the levels of headings. For example, you cannot
have a <literal>===</literal> (paragraph) heading after a <literal>=======</literal> (section) heading without
a <literal>=====</literal> (subsection) heading in between.</paragraph></section><section ids="lists-do-not-appear-in-rst-files" names="lists\ do\ not\ appear\ in\ .rst\ files"><title>Lists do not appear in .rst files</title><paragraph>Check if you have a comment right above the list. That comment
will include the list if the list is indentend. Remove the comment.</paragraph></section><section ids="error-message-undefined-substitution-from-rest" names="error\ message\ &quot;undefined\ substitution...&quot;\ from\ rest"><title>Error message &quot;Undefined substitution...&quot; from reST</title><paragraph>This may happen if there is much inline math in the text. reST cannot
understand inline LaTeX commands and interprets them as illegal code.
Just ignore these error messages.</paragraph></section><section ids="warning-about-duplicate-link-names" names="warning\ about\ duplicate\ link\ names"><title>Warning about duplicate link names</title><paragraph>Link names should be unique, but if (e.g.) &quot;file&quot; is used as link text
several places in a reST file, the links still work. The warning can
therefore be ignorned.</paragraph></section><section ids="inconsistent-headings-in-rest" names="inconsistent\ headings\ in\ rest"><title>Inconsistent headings in reST</title><paragraph>The <literal>rst2*.py</literal> and Sphinx converters abort if the headers of sections
are not consistent, i.e., a subsection must come under a section,
and a subsubsection must come under a subsection (you cannot have
a subsubsection directly under a section). Search for <literal>===</literal>,
count the number of equality signs (or underscores if you use that)
and make sure they decrease by two every time a lower level is encountered.</paragraph></section><section ids="no-code-environment-appears-before-bc-ipy-blocks" names="no\ code\ environment\ appears\ before\ &quot;bc\ ipy&quot;\ blocks"><title>No code environment appears before &quot;bc ipy&quot; blocks</title><paragraph>The <literal>!bc ipy</literal> directive behaves this way for <literal>sphinx</literal> output because
interactive sessions are automatically handled. If this is not
appropriate, shift to <literal>!bc cod</literal> or another specification of the
verbatim environment.</paragraph></section></section><section ids="problems-with-latex-output" names="problems\ with\ latex\ output"><title>Problems with LaTeX Output</title><section ids="latex-does-not-like-underscores-in-urls" names="latex\ does\ not\ like\ underscores\ in\ urls"><title>LaTeX does not like underscores in URLs</title><paragraph>Suppose you have a URL reference like:</paragraph><literal_block xml:space="preserve">..which can be found in the file &quot;my_file.txt&quot;:
&quot;http://some.where.net/web/dir/my_file.txt&quot;.</literal_block><paragraph>LaTeX will stop with a message about a missing dollar sign. The reason
is that underscores in link texts need to be preceded by a backslash.
However, this is incovenient to do in the Doconce source since the
underscore is misleading in other formats.
The remedy is to format the link text with inline verbatim tags (backticks):</paragraph><literal_block xml:space="preserve">..which can be found in the file &quot;`my_file.txt`&quot;:
&quot;http://some.where.net/web/dir/my_file.txt&quot;.</literal_block><paragraph>Verbatim text in links works fine with underscores.</paragraph></section><section ids="error-when-running-latex-you-must-have-pygmentize-installed" names="error\ when\ running\ latex:\ you\ must\ have\ 'pygmentize'\ installed"><title>Error when running latex: You must have 'pygmentize' installed</title><paragraph>This message points to the use of the minted style for typesetting verbatim
code. You need to include the <literal>-shell-escape</literal> command-line argument when
running <literal>latex</literal> or <literal>pdflatex</literal>:</paragraph><literal_block xml:space="preserve">Terminal&gt; latex -shell-escape file mydoc.tex
Terminal&gt; pdflatex -shell-escape file mydoc.tex</literal_block><paragraph>Using <literal>doconce ptex2tex</literal> will turn on the minted style if specified as
environment on the command line, while using <literal>ptex2tex</literal> requires the
preprocessor option <literal>-DMINTED</literal> to turn on the minted package.
When this package is included, <literal>latex</literal> or <literal>pdflatex</literal> runs the
<literal>pygmentize</literal> program and the <literal>shell-escape</literal> option is required.</paragraph></section><section ids="why-are-the-latex-section-headings-smaller-than-normal" names="why\ are\ the\ latex\ section\ headings\ smaller\ than\ normal?"><title>Why are the LaTeX section headings smaller than normal?</title><paragraph>Doconce inserts a special command to make the headings
more compact:</paragraph><literal_block xml:space="preserve">\usepackage[compact]{titlesec}</literal_block><paragraph>as explained in the <reference name="titlesec" refuri="http://www.ctex.org/documents/packages/layout/titlesec.pdf">titlesec</reference><target ids="titlesec" names="titlesec" refuri="http://www.ctex.org/documents/packages/layout/titlesec.pdf"/> package documentation.
To retrieve the standard LaTeX headings, comment out this line or remove it:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format pdflatex mydoc
Terminal&gt; doconce subst '\\usepack.+\{titlesec\} '' mydoc.p.tex</literal_block><paragraph>You can easily make the headings even smaller than the normal font
by replacing <literal>[compact]</literal> by <literal>[compact,small]</literal> as parameter specification
for <literal>titlesec</literal>.</paragraph></section><section ids="can-i-have-latex-figures-with-shadows" names="can\ i\ have\ latex\ figures\ with\ shadows?"><title>Can I have LaTeX figures with shadows?</title><paragraph>This is easy by including the <literal>fancybox</literal> and <literal>graphicx</literal> packages
and wrapping all <literal>\includegraphics</literal> in a shadow box:</paragraph><literal_block xml:space="preserve">Terminal&gt; doconce format pdflatex mydoc
Terminal&gt; doconce replace \
          'microtype}', 'microtype,fancybox,graphicx}' mydoc.p.tex
Terminal&gt; doconce subst '(\\includegraphics\[.+\}\})' \
          '\\shadowbox{\g&lt;1&gt;}' mydoc.p.tex</literal_block></section><section ids="how-can-i-use-my-fancy-latex-environments" names="how\ can\ i\ use\ my\ fancy\ latex\ environments?"><title>How can I use my fancy LaTeX environments?</title><paragraph>See the section <reference name="Example: Defining a Theorem Environment" refid="example-defining-a-theorem-environment">Example: Defining a Theorem Environment</reference> for how to make a custom
theorem environment also in Doconce, without using implementations
restricted to LaTeX.</paragraph></section><section ids="the-latex-file-does-not-compile" names="the\ latex\ file\ does\ not\ compile"><title>The LaTeX file does not compile</title><paragraph>If the problem is undefined control sequence involving:</paragraph><literal_block xml:space="preserve">\code{...}</literal_block><paragraph>the cause is usually a verbatim inline text (in back-ticks in the
Doconce file) spans more than one line. Make sure, in the Doconce source,
that all inline verbatim text appears on the same line.</paragraph></section><section ids="inline-verbatim-gives-error" names="inline\ verbatim\ gives\ error"><title>Inline verbatim gives error</title><paragraph>Check if the inline verbatim contains typical LaTeX commands, e.g.:</paragraph><literal_block xml:space="preserve">some text with `\usepackage{mypack}` is difficult because
ptex2tex will replace this by \code{\usepackage{mypack}} and
then replace this by
{\fontsize{10pt}{10pt}\verb!\usepackage{mypack!}}
which is wrong because ptex2tex applies regex that don't
capture the second }</literal_block><paragraph>The remedy is to place verbatim LaTeX commands in verbatim
blocks - that is safe.</paragraph><comment xml:space="preserve">Could have doconce configure file where inline verbatim is</comment><comment xml:space="preserve">configured to be \fontsize... directly, not via ptex2tex \code{}.</comment></section><section ids="errors-in-figure-captions" names="errors\ in\ figure\ captions"><title>Errors in figure captions</title><paragraph>Such errors typically arise from unbalanced curly braces, or dollar signs
around math, and similar LaTeX syntax errors.</paragraph><paragraph>(Note that verbatim font is likely to cause trouble inside figure captions,
but Doconce will automatically replace verbatim text in back-ticks by
a proper <literal>texttt</literal> command (since verbatim font constructions does not work
inside figure captions) and precede underscores by backslash.)</paragraph></section><section ids="chapters-are-ignored" names="chapters\ are\ ignored"><title>Chapters are ignored</title><paragraph>The default LaTeX style is &quot;article&quot;. If you chapters in the Doconce file,
you need to run <literal>ptex2tex</literal> with the option <literal>-DBOOK</literal> to set the LaTeX
documentstyle to &quot;book&quot;.</paragraph></section><section ids="i-want-to-tune-the-top-of-the-latex-file" names="i\ want\ to\ tune\ the\ top\ of\ the\ latex\ file"><title>I want to tune the top of the LaTeX file</title><paragraph>The top of the LaTeX file, as generated by Doconce, is very simple.
If this LaTeX code is not sufficient for your needs, there are
two ways out of it:</paragraph><enumerated_list enumtype="arabic" prefix="" suffix="."><list_item><paragraph>Make a little Bash script that performs a series of
<literal>doconce subst</literal> (regular expressions) or <literal>doconce replace</literal> (regular text)
substitutions to change the text automatically (you probably have to
repeat these edits so automating them is a good idea).</paragraph></list_item><list_item><paragraph>Place the title, author(s), and date of the Doconce file in a separate
file and use the preprocessor to include the rest. The rest is then
one or more Doconce files without title, author(s), and date. This
means that the <literal>doconce format latex</literal> command does not generate
the LaTeX intro (preamble) and outro, just the core text, for these
files.
Make a new file by hand with the appropriate LaTeX intro and outro
text and include the various text pieces in this file.
To make the LaTeX document, you compile all Doconce files
into LaTeX code, except the &quot;top&quot; Doconce file that includes the
others. That file is not used for LaTeX output, but
replaced by the hand-written LaTeX &quot;top&quot; file.</paragraph></list_item></enumerated_list></section></section><section ids="problems-with-gwiki-output" names="problems\ with\ gwiki\ output"><title>Problems with gwiki Output</title><section ids="strange-nested-lists-in-gwiki" names="strange\ nested\ lists\ in\ gwiki"><title>Strange nested lists in gwiki</title><paragraph>Doconce cannot handle nested lists correctly in the gwiki format.
Use nonnested lists or edit the <literal>.gwiki</literal> file directly.</paragraph></section><section ids="lists-in-gwiki-look-ugly-in-the-gwiki-source" names="lists\ in\ gwiki\ look\ ugly\ in\ the\ gwiki\ source"><title>Lists in gwiki look ugly in the gwiki source</title><paragraph>Because the Google Code wiki format requires all text of a list item to
be on one line, Doconce simply concatenates lines in that format,
and because of the indentation in the original Doconce text, the gwiki
output looks somewhat ugly. The good thing is that this gwiki source
is seldom to be looked at - it is the Doconce source that one edits
further.</paragraph></section></section><section ids="problems-with-html-output" names="problems\ with\ html\ output"><title>Problems with HTML Output</title><section ids="mathjax-formulas-are-not-properly-rendered" names="mathjax\ formulas\ are\ not\ properly\ rendered"><title>MathJax formulas are not properly rendered</title><paragraph>Here are some common problems:</paragraph><block_quote><bullet_list bullet="*"><list_item><paragraph>Two equations cannot have identical label (this error often arises
from copying and pasting equations)</paragraph></list_item><list_item><paragraph><literal>[</literal> and <literal>]</literal> brackets must sometimes be replaced by <literal>\lbrack</literal> and <literal>\rbrack</literal></paragraph></list_item></bullet_list></block_quote></section><section ids="how-can-i-change-the-layout-of-the-html-page" names="how\ can\ i\ change\ the\ layout\ of\ the\ html\ page?"><title>How can I change the layout of the HTML page?</title><paragraph>The easiest way is to edit the HTML style or the HTML code directly.
However, those edits are overwritten the next time you compile the
Doconce document to HTML. The edits should therefore be automated
using <literal>doconce subst</literal> (for regular expression editing) or <literal>doconce replace</literal>
(for plain text substitution editing) commands in the file where
you run <literal>doconce format html mydoc</literal>. For example, say you want narrower
gray admonition boxes with a much thicker boundary. The <literal>.alert</literal>
style must then be changed, more precisely the <literal>border</literal> and the <literal>width</literal>
specification:</paragraph><literal_block xml:space="preserve">doconce replace 'border:1px' 'border:11px' mydoc.html
doconce replace 'width: 75%;' 'width: 35%' mydoc.html</literal_block><paragraph>Another way to control the layout is to copy the style in the HTML
file into a <literal>.css</literal> file, edit that file as you like, and provide the
file as part of the compilation using the <literal>--css=mystyle.css</literal>
flag.</paragraph><paragraph>The standard of way of completely controlling the HTML format is to
use an HTML template. The Doconce source is then the body of text
(leave out <literal>TITLE:</literal> to get HTML without a header and footer). The
<literal>--html_template=filename</literal> command-line option will then embed the
Doconce text in the specified template file, where you can use style
sheets and desired constructs in the header and footer.  The template
can have &quot;slots&quot; for a title (<literal>%(title)s</literal>), a date (<literal>%(date)s</literal>), and
the main body of text (<literal>%(main)s</literal>).  For typesetting code, <literal>pygments</literal>
is used (if installed) and can be turned off by <literal>--no_pygments_html</literal>
(leaving code in gray boxes).</paragraph><paragraph>The easiest way is to get fancy layouts in HTML is to
use the <literal>sphinx</literal> format and one its many themes.</paragraph></section><section ids="why-do-figures-look-ugly-when-using-html-templates" names="why\ do\ figures\ look\ ugly\ when\ using\ html\ templates?"><title>Why do figures look ugly when using HTML templates?</title><paragraph>The HTML header that Doconce generates contain special styles for
figure captions and the horizontal rule above figures. When using
templates these styles are not defined, resulting in a rule that
spans the width and a centered caption. Changing the appearance
of the rule and caption can either be done by inserting styles or
simply by automatic editing of the HTML code in a little shell script:</paragraph><literal_block xml:space="preserve">doconce replace '&lt;p class=&quot;caption&quot;&gt;' \
 '&lt;p style=&quot;width: 50%; font-style: italic; color: blue&quot;&gt;' mydoc.html
doconce replace '&lt;hr class=&quot;figure&quot;&gt;' \
 '&lt;hr style=&quot;width: 50%&quot;&gt;' mydoc.html</literal_block></section></section><section ids="debugging" names="debugging"><title>Debugging</title><paragraph>Given a problem, extract a small portion of text surrounding the
problematic area and debug that small piece of text. Doconce does a
series of transformations of the text. The effect of each of these
transformation steps are dumped to a logfile, named
<literal>_doconce_debugging.log</literal>, if the to <literal>doconce format</literal> after the filename
is <literal>debug</literal>. The logfile is inteded for the developers of Doconce, but
may still give some idea of what is wrong.  The section &quot;Basic Parsing
Ideas&quot; explains how the Doconce text is transformed into a specific
format, and you need to know these steps to make use of the logfile.</paragraph></section></section><section ids="basic-parsing-ideas" names="basic\ parsing\ ideas"><title>Basic Parsing Ideas</title><comment xml:space="preserve">avoid list here since we have code in between (never a good idea)</comment><paragraph>The (parts of) files with computer code to be directly included in
the document are first copied into verbatim blocks.</paragraph><paragraph>All verbatim and TeX blocks are removed and stored elsewhere
to ensure that no formatting rules are not applied to these blocks.</paragraph><paragraph>The text is examined line by line for typesetting of lists, as well as
handling of blank lines and comment lines.
List parsing needs some awareness of the context.
Each line is interpreted by a regular expression:</paragraph><literal_block xml:space="preserve">(?P&lt;indent&gt; *(?P&lt;listtype&gt;[*o-] )? *)(?P&lt;keyword&gt;[^:]+?:)?(?P&lt;text&gt;.*)\s?</literal_block><paragraph>That is, a possible indent (which we measure), an optional list
item identifier, optional space, optional words ended by colon,
and optional text. All lines are of this form. However, some
ordinary (non-list) lines may contain a colon, and then the keyword
and text group must be added to get the line contents. Otherwise,
the text group will be the line.</paragraph><paragraph>When lists are typeset, the text is examined for sections, paragraphs,
title, author, date, plus all the inline tags for emphasized, boldface,
and verbatim text. Plain subsitutions based on regular expressions
are used for this purpose.</paragraph><paragraph>The final step is to insert the code and TeX blocks again (these should
be untouched and are therefore left out of the previous parsing).</paragraph><paragraph>It is important to keep the Doconce format and parsing simple.  When a
new format is needed and this format is not obtained by a simple edit
of the definition of existing formats, it might be better to convert
the document to reST and then to XML, parse the XML and
write out in the new format.  When the Doconce format is not
sufficient to getting the layout you want, it is suggested to filter
the document to another, more complex format, say reST or
LaTeX, and work further on the document in this format.</paragraph><section ids="typesetting-of-function-arguments-return-values-and-variables" names="typesetting\ of\ function\ arguments,\ return\ values,\ and\ variables"><title>Typesetting of Function Arguments, Return Values, and Variables</title><paragraph>As part of comments (or doc strings) in computer code one often wishes
to explain what a function takes of arguments and what the return
values are. Similarly, it is desired to document class, instance, and
module variables.  Such arguments/variables can be typeset as
description lists of the form listed below and <emphasis>placed at the end of
the doc string</emphasis>. Note that <literal>argument</literal>, <literal>keyword argument</literal>, <literal>return</literal>,
<literal>instance variable</literal>, <literal>class variable</literal>, and <literal>module variable</literal> are the
only legal keywords (descriptions) for the description list in this
context.  If the output format is Epytext (Epydoc) or Sphinx, such lists of
arguments and variables are nicely formatted:</paragraph><literal_block xml:space="preserve">- argument x: x value (float),
  which must be a positive number.
- keyword argument tolerance: tolerance (float) for stopping
  the iterations.
- return: the root of the equation (float), if found, otherwise None.
- instance variable eta: surface elevation (array).
- class variable items: the total number of MyClass objects (int).
- module variable debug: True: debug mode is on; False: no debugging
  (bool variable).</literal_block><paragraph>The result depends on the output format: all formats except Epytext
and Sphinx just typeset the list as a list with keywords.</paragraph><block_quote><definition_list><definition_list_item><term>module variable x:</term><definition><paragraph>x value (float),
which must be a positive number.</paragraph></definition></definition_list_item><definition_list_item><term>module variable tolerance:</term><definition><paragraph>tolerance (float) for stopping
the iterations.</paragraph></definition></definition_list_item></definition_list></block_quote></section></section><section ids="references" names="references"><title>References</title></section><section ids="bibliography" names="bibliography"><title>Bibliography</title><citation backrefs="id7 id12" ids="ref1" names="ref1"><label>Ref1</label><paragraph><strong>H. P. Langtangen and G. Pedersen</strong>. Propagation of Large Destructive Waves,
<emphasis>International Journal of Applied Mechanics and Engineering</emphasis>,
7(1),
pp. 187-204,
2002.</paragraph></citation><citation backrefs="id8" ids="ref2" names="ref2"><label>Ref2</label><paragraph><strong>H. P. Langtangen, K.-A. Mardal and R. Winther</strong>. Numerical Methods for Incompressible Viscous Flow,
<emphasis>Advances in Water Resources</emphasis>,
25,
pp. 1125-1146,
2002.</paragraph></citation><citation backrefs="id9" ids="ref3" names="ref3"><label>Ref3</label><paragraph><strong>H. P. Langtangen</strong>. Numerical Solution of First Passage Problems in Random Vibrations,
<emphasis>SIAM Journal of Scientific and Statistical Computing</emphasis>,
15,
pp. 997-996,
1994.</paragraph></citation><citation backrefs="id10 id13" ids="ref4" names="ref4"><label>Ref4</label><paragraph><strong>K.-A. Mardal, G. W. Zumbusch and H. P. Langtangen</strong>. Software Tools for Multigrid Methods,
Advanced Topics in Computational Partial Differential Equations -- Numerical Methods and Diffpack Programming,
edited by <strong>H. P. Langtangen and A. Tveito</strong>,
Springer,
2003.</paragraph></citation><citation backrefs="id11" ids="ref5" names="ref5"><label>Ref5</label><paragraph><strong>H. P. Langtangen</strong>. The FEMDEQS Program System,
<emphasis>Department of Mathematics, University of Oslo</emphasis>,
1989.</paragraph></citation></section></document>